# TIL: JSP

## 학습 날짜: 2025년 2월 18일

## 오늘 배운 내용 정리

### 정적 Resource: HTML

1. **HTML의 내용: DOM 구조 생성**
    - HTML 문서가 브라우저에서 로드될 때, 웹 브라우저는 HTML 파일을 읽고 이를 DOM(Document Object Model) 트리 구조로 변환한다. 이 구조는 HTML 문서의 요소들을 객체로 모델링하여 JavaScript로 동적으로 수정하거나 스타일을 적용할 수 있도록 한다.
    - 예를 들어, `<div>`, `<p>`, `<ul>` 등과 같은 HTML 요소들은 DOM 트리에서 각기 다른 객체로 표현되며, 이를 통해 요소들의 접근, 변경, 삭제 등이 가능해진다.
2. **CSS의 내용: CSSOM 생성**
    - CSS 파일은 브라우저에 의해 CSSOM(CSS Object Model)으로 변환된다. CSSOM은 스타일 정보를 객체로 표현한 것으로, 각 스타일 규칙들이 CSSOM 트리로 표현되어 HTML의 각 요소에 대한 스타일을 정의한다.
    - 예를 들어, `font-size`, `color`, `margin`과 같은 스타일 속성들은 CSSOM을 통해 HTML 요소들에 적용되어 시각적으로 표현된다.
3. **1 + 2 → Render Tree**
    - HTML과 CSS는 각각 DOM 트리와 CSSOM 트리로 변환된 후, 이 두 트리가 결합되어 Render Tree가 생성된다. Render Tree는 브라우저가 실제로 페이지를 렌더링하는 데 필요한 정보를 담고 있다.
    - Render Tree는 각 DOM 노드에 대해 실제로 화면에 그려야 할 스타일을 포함하며, 이 트리 구조를 기반으로 페이지를 렌더링할 때 필요한 계산과 요소의 위치를 결정한다.

---

### 세션 처리

- **HTTP 프로토콜과 세션 처리**
    - 기본적으로 HTTP는 **무상태(stateless)** 프로토콜이다. 즉, 각 요청은 독립적으로 처리되며, 서버는 이전 요청에 대한 정보를 기억하지 못한다. 예를 들어, 동일한 사용자가 여러 번 요청을 보낼 경우, 서버는 사용자가 이전에 무엇을 했는지 알 수 없다.
    - 이러한 특성은 웹 애플리케이션에서 사용자의 상태를 추적하거나 여러 페이지를 걸쳐 사용자 정보를 관리하는 데 문제가 된다. 이 문제를 해결하기 위해 세션(Session), 쿠키(Cookie), URL 재작성(URL rewriting) 등의 기술이 사용된다.
- **쿠키 (Cookie)**
    - **쿠키**는 클라이언트(사용자 브라우저)에 저장되는 데이터로, 이름-값 쌍의 형식으로 저장된다. 예를 들어, `name=홍길동; age=20`와 같이 데이터를 저장할 수 있다.
    - 서버는 **HTTP 응답(Response)** 헤더를 통해 쿠키를 클라이언트에 전달하고, 클라이언트는 이를 브라우저에 저장한 후, 이후의 요청 시 자동으로 같은 도메인에 대해 쿠키를 서버로 전송한다.
    - **단점**:
        - **보안성**: 쿠키는 암호화되지 않기 때문에 민감한 정보(예: 비밀번호)를 저장할 수 없다.
        - **데이터 구조화 부족**: 쿠키는 단순한 문자열로 데이터를 저장하므로 복잡한 데이터를 저장하거나 처리하기가 어렵다.
        - **용량 한계**: 쿠키는 각 도메인당 저장할 수 있는 데이터 용량이 제한적이다. 보통 4KB로 제한된다.
- **세션 (Session)**
    - **세션**은 웹 애플리케이션 서버(WAS)에 저장되는 사용자 상태 정보를 관리하는 기술이다. 세션은 서버의 메모리에서 관리되며, 각 클라이언트마다 고유한 **SessionId**가 할당된다. 이 SessionId는 쿠키를 통해 클라이언트에 저장되고, 이후의 모든 요청에서 자동으로 서버로 전송된다.
    - **세션 객체 관리**:
        - `HttpSession`은 세션을 관리하는 클래스이다. 세션 객체는 WAS의 메모리 힙에 저장된다.
        - `getSession()` 메소드를 사용하여 세션을 생성하거나 기존의 세션을 가져올 수 있다. 기본적으로 `getSession()`은 새로운 세션을 생성하는데, `false`로 설정하면 기존의 세션이 있을 경우 이를 반환하고, 없으면 `null`을 반환한다.
        - 세션에 값을 저장할 때는 `setAttribute()` 메소드를 사용하고, 저장된 값을 꺼낼 때는 `getAttribute()` 메소드를 사용한다.
        - `invalidate()` 메소드를 사용하면 세션을 무효화시킬 수 있다.
    
    ```java
    // WAS에게 session 저장 공간을 요청
    // getSession() -> default가 true. 기존 할당 session 객체 return 하거나 없으면 새로 만들기
    HttpSession session = req.getSession();
    // req.getSession(false) -> 기존에 할당된 session 객체가 있으면 return 하고, 없으면 null return
    
    session.setAttribute("user", user);
    // setAttribute를 이용해 값을 저장할 수 있다
    // getAttribute로 값을 다시 꺼내올 수 있다
    
    // invalidate() : 세션을 무효화시키는 것
    
    ```
    

### JSP (JavaServer Pages)

- **JSP의 개요**
    - **JSP**는 Java를 기반으로 한 서버 사이드 스크립팅 기술로, 동적인 웹 페이지를 생성하기 위해 사용된다. HTML 페이지 내에 Java 코드를 삽입하여 서버에서 처리된 결과를 클라이언트에게 전달하는 방식이다.
    - JSP는 주로 **HTML**과 **Java 코드**를 혼합하여 웹 페이지를 동적으로 생성하며, Java의 강력한 서버 사이드 처리 능력을 활용할 수 있다.
- **JSP 파일의 기본 구조**
    - JSP 파일은 `.jsp` 확장자를 가지며, HTML 마크업과 Java 코드를 함께 포함한다. Java 코드는 `<% ... %>` 태그 내에 작성된다.
    - JSP 페이지가 요청될 때, 서버는 해당 페이지를 컴파일하여 서블릿(Servlet) 클래스를 생성하고, 이후에는 이 서블릿 클래스를 실행하여 동적인 콘텐츠를 생성한다.
- **JSP 페이지의 동작 과정**
    1. **클라이언트 요청**: 사용자가 웹 브라우저를 통해 JSP 페이지를 요청한다.
    2. **서버 처리**: 서버는 해당 JSP 파일을 컴파일하여 서블릿 클래스를 생성하고, 이를 실행한다.
    3. **동적 콘텐츠 생성**: JSP 내의 Java 코드가 실행되어 동적인 콘텐츠가 생성된다. 예를 들어, 데이터베이스에서 정보를 가져오거나, 세션 정보에 따라 다른 콘텐츠를 표시하는 등의 작업이 이루어진다.
    4. **응답 전송**: 생성된 HTML 콘텐츠가 클라이언트에 전송된다.
- **JSP의 장점**
    - **HTML과 Java 코드의 결합**: JSP는 HTML과 Java 코드를 결합하여 동적인 웹 페이지를 쉽게 작성할 수 있도록 한다. Java 코드가 포함된 HTML 문서는 서버에서 처리 후 결과만 클라이언트에 전달된다.
    - **유지보수 용이성**: JSP는 Java로 작성되므로, Java의 강력한 기능을 활용할 수 있고, 복잡한 로직 처리를 서버에서 할 수 있어 클라이언트 측 코드 관리가 용이하다.
    - **서블릿과의 통합**: JSP는 서블릿 기술을 기반으로 하기 때문에, JSP 페이지에서 Java 코드와 서블릿을 효율적으로 결합하여 더 복잡한 애플리케이션을 개발할 수 있다.
- **JSP와 서블릿의 차이점**
    - JSP는 HTML 페이지와 Java 코드를 결합하여 동적 콘텐츠를 생성하지만, 서블릿은 Java 코드로만 이루어져 있어 HTML 생성 작업을 전혀 다르게 처리한다. JSP는 HTML과 Java 코드의 결합이 쉽고 직관적이라, 웹 페이지의 동적 콘텐츠를 생성하는 데 더 적합하다.
    - 서블릿은 비즈니스 로직을 처리하는 데 유리하며, JSP는 UI를 표현하는 데 유리하다. JSP와 서블릿은 함께 사용되며, 서블릿에서 로직을 처리하고 JSP에서 그 결과를 출력하는 방식으로 협력한다.
- **JSP의 주요 구성 요소**
    - **정적 HTML 콘텐츠**: JSP 페이지는 기본적으로 HTML을 포함하고, 그 위에 Java 코드를 삽입하여 동적인 결과를 생성한다.
    - **Java 코드**: Java 코드 블록은 `<% %>` 태그로 감싸서 삽입하며, 동적 데이터 처리나 비즈니스 로직을 수행할 수 있다.
    - **JSP 디렉티브**: 페이지 설정을 정의하는 데 사용되는 `page`나 `include`와 같은 지시어(directive)가 있다.
    - **JSP 액션 태그**: JSP는 `<jsp:useBean>`, `<jsp:setProperty>`, `<jsp:getProperty>`와 같은 태그를 제공하여, 객체의 사용과 속성 설정을 쉽게 할 수 있도록 돕는다.
- **JSP에서 데이터 처리 예시**
    - JSP는 사용자로부터 입력을 받거나, 서버 측에서 계산한 결과를 페이지에 출력하는 데 사용된다. 예를 들어, 로그인 처리를 JSP에서 하는 경우, 사용자가 입력한 ID와 비밀번호를 처리하고 결과를 동적으로 출력하는 방식이다.
    - 세션을 통해 로그인 상태를 유지하고, 데이터베이스에서 필요한 정보를 조회하여 동적인 결과를 생성할 수 있다.
