# TIL: 프로젝트 개발

## 학습 날짜: 2025년 2월 28일

## 오늘 배운 내용 정리

### **1. React에서 모달과 원형 그래프 사용하기**

- **목표**: 모달 창 안에 원형 그래프를 표시하려고 했음. 이를 위해 `react-chartjs-2` 라이브러리를 사용하여 데이터를 시각적으로 표현하는 방법을 배웠음.
- **핵심 내용**:
    - `AnalysisModal` 컴포넌트에서 원형 그래프와 라벨을 표시하는 방법을 배웠음.
    - `chart.js`를 이용해 데이터를 시계방향으로 정렬하여 원형 그래프에 표시.
    - 데이터를 `sectorId`별로 분류하고 `amount`로 총합을 구하여 원형 그래프의 각 항목을 표현.
    - CSS에서 `transform`과 `rotate` 속성을 사용하여 그래프의 라벨을 각 항목에 맞게 배치.

---

### **2. React 컴포넌트에서 데이터 전달 및 모달 창 제어**

- **목표**: `useFetchHousehold` 훅을 이용해 데이터를 받아오고, 이를 `Calendar`와 `AnalysisModal` 컴포넌트에 전달하는 방법을 배웠음.
- **핵심 내용**:
    - `useFetchHousehold` 훅을 통해 사용자 데이터(`events`)를 가져와 이를 `Calendar`와 `AnalysisModal`에 전달.
    - `AnalysisModal`에서 모달의 `show` prop을 이용해 모달이 열리고 닫히는 동작을 제어.
    - 모달 외부를 클릭하면 모달이 닫히도록 설정하기 위해 이벤트 처리 함수를 작성.

---

### **3. CSS 모듈을 활용한 스타일링**

- **목표**: CSS 모듈을 사용하여 특정 컴포넌트에만 스타일을 적용하는 방법을 배웠음.
- **핵심 내용**:
    - `AnalysisModal`의 크기를 키우기 위해 CSS 모듈을 사용하여 특정 모달만 크기를 조정.
    - 모달의 레이아웃을 설정하고, 원형 그래프가 잘 보이도록 스타일링.
    - 전역 스타일을 변경하지 않고 특정 컴포넌트에만 영향을 미치게 하기 위해 CSS 모듈을 사용.

---

### **4. React 아이콘 및 텍스트 배치**

- **목표**: `FaSearch` 아이콘을 텍스트와 함께 배치하면서 텍스트가 왼쪽 정렬되도록 설정.
- **핵심 내용**:
    - `title` 클래스를 사용하여 텍스트와 아이콘을 배치할 때 `flex`를 이용한 수평 및 수직 정렬 방법을 배웠음.
    - `justify-content: flex-start`를 사용하여 텍스트를 왼쪽 정렬.
    - `align-items: center`로 텍스트와 아이콘을 수직으로 맞추었으며, 아이콘과 텍스트 사이에 간격을 두는 방법을 배웠음.

---

### **5. 데이터 정렬 및 원형 그래프에 시계방향 배치**

- **목표**: 원형 그래프에 데이터를 `amount` 값에 따라 시계방향으로 정렬하여 표시하는 방법을 배웠음.
- **핵심 내용**:
    - 데이터를 `amount`에 따라 내림차순으로 정렬한 후, `Chart.js`에서 각 항목을 원형 그래프에 표시.
    - `sectorId`를 기반으로 라벨을 표시하고, `amount` 값을 기준으로 그래프의 비율을 설정.
    - `chart.js` 라이브러리의 `doughnut` 차트를 사용하여 원형 그래프를 그리는 법.

---

### **6. React에서 이벤트 처리 및 상태 관리**

- **목표**: React 컴포넌트에서 사용자 이벤트(`onClick`)에 따라 상태를 변경하는 방법을 배웠음.
- **핵심 내용**:
    - `setShowAnalysisModal(true)`로 `show` 상태를 변경하여 모달 창을 열도록 설정.
    - `useState` 훅을 사용해 모달의 열림/닫힘 상태를 관리.
    - `useEffect` 훅을 이용하여 로그인 여부를 확인하고, 로그인이 안된 경우 로그인 페이지로 리다이렉트.

---

### **배운 점**

- 데이터를 시각적으로 잘 표현하려면 차트 라이브러리와 CSS 스타일링을 잘 활용해야 한다.
- React에서 컴포넌트 간 데이터 전달 및 상태 관리를 잘 해야 UX/UI가 직관적이고 원활하다.
- 모달, 원형 그래프 등 시각적 요소를 잘 배치하고, 적절한 크기 조정 및 레이아웃을 통해 사용자 경험을 향상시킬 수 있다.
