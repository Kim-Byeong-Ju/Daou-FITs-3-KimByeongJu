# TIL: 이중포인터, 함수포인터, 구조체

## 학습 날짜: 2025년 3월 13일

## 1. 이중포인터 응용

### **개념**

- 이중 포인터는 **포인터를 가리키는 포인터**입니다. 즉, 포인터 변수의 주소를 저장하는 변수입니다.
- `int *ptr`은 정수를 가리키는 포인터이고, `int **dptr`은 포인터 `ptr`을 가리키는 포인터입니다.

### **선언과 사용**

```c
c
복사편집
int a = 10;
int *ptr = &a;  // 'a'의 주소 저장
int **dptr = &ptr;  // 'ptr'의 주소 저장

printf("%d\n", **dptr);  // 10 출력

```

- `dptr` → `ptr`이 가리키는 값 (`a`의 주소)
- `*dptr` → `ptr`이 가리키는 변수의 값 (`a`의 값)

### **2차원 배열에서의 활용**

이중 포인터는 2차원 배열을 동적으로 다룰 때 유용합니다.

```c
c
복사편집
int arr[3][3] = { {1,2,3}, {4,5,6}, {7,8,9} };
int *ptr[3] = { arr[0], arr[1], arr[2] };  // 각 행을 가리키는 포인터 배열
int **dptr = ptr;  // 포인터 배열을 가리키는 이중 포인터

printf("%d\n", dptr[1][2]);  // 6 출력 (2번째 행의 3번째 요소)

```

- `ptr`은 각 행을 가리키는 포인터 배열입니다.
- `dptr`은 `ptr`을 가리키는 이중 포인터이며, 이를 통해 2차원 배열의 요소를 동적으로 접근할 수 있습니다.

---

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define SIZE 3

int main() {
    int arr[SIZE][SIZE] = { {1,2,3},{4,5,6},{7,8,9} };
    int* ptr[SIZE] = { arr[0], arr[1], arr[2] }; // 행을 가리키는 포인터 배열

    // 90도 회전 (이중 포인터 활용)
    int temp[SIZE][SIZE];  // 임시 저장 배열
    for (int i = 0; i < SIZE; i++) {
        for (int j = 0; j < SIZE; j++) {
            temp[j][SIZE - 1 - i] = ptr[i][j]; // ptr[i][j]를 90도 회전된 위치로 이동
        }
    }

    // 원본 배열 arr에 회전된 데이터 반영
    for (int i = 0; i < SIZE; i++) {
        for (int j = 0; j < SIZE; j++) {
            ptr[i][j] = temp[i][j];
        }
    }

    // 결과 출력
    printf("90도 회전된 배열:\n");
    for (int i = 0; i < SIZE; i++) {
        for (int j = 0; j < SIZE; j++) {
            printf("%d ", arr[i][j]);
        }
        printf("\n");
    }

    return 0;
}

```

## 2. 함수포인터 응용

### **개념**

- 함수 포인터는 **함수의 주소를 저장하는 포인터**입니다.
- 이를 활용하면 함수 호출을 동적으로 수행할 수 있습니다.

### **선언과 사용**

```c
c
복사편집
int add(int a, int b) { return a + b; }

int main() {
    int (*fp)(int, int);  // 함수 포인터 선언
    fp = add;  // 함수 주소 저장

    printf("%d\n", fp(3, 5));  // 8 출력
}

```

- `int (*fp)(int, int)` → `int`를 반환하고 `int` 매개변수를 두 개 받는 함수 포인터
- `fp = add;` → `add` 함수의 주소를 `fp`에 저장
- `fp(3,5)` → `add(3,5)`와 동일한 호출

### **함수 포인터 배열**

- 여러 함수를 저장하고 동적으로 선택할 때 사용

```c
c
복사편집
#include <stdio.h>

int add(int a, int b) { return a + b; }
int sub(int a, int b) { return a - b; }
int mul(int a, int b) { return a * b; }
int divi(int a, int b) { return b ? a / b : 0; }

int main() {
    int (*fp[4])(int, int) = { add, sub, mul, divi };  // 함수 포인터 배열
    int choice, a = 10, b = 2;

    printf("연산 선택 (0: +, 1: -, 2: *, 3: /): ");
    scanf("%d", &choice);

    if (choice >= 0 && choice < 4)
        printf("결과: %d\n", fp[choice](a, b));  // 선택한 연산 수행

    return 0;
}

```

- `fp[4]`는 4개의 함수 포인터를 저장하는 배열입니다.
- `fp[choice](a, b)`를 호출하면 사용자가 선택한 연산이 실행됩니다.

---

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <ctype.h>

#define SIZE 100

// 문자열 길이를 구하는 함수
void get_length(char* str) {
    printf("결과: %d\n", (int)strlen(str));
}

// 문자열을 대문자로 변환하는 함수
void to_uppercase(char* str) {
    for (int i = 0; str[i]; i++) {
        str[i] = toupper(str[i]);
    }
    printf("결과: %s\n", str);
}

// 문자열을 소문자로 변환하는 함수
void to_lowercase(char* str) {
    for (int i = 0; str[i]; i++) {
        str[i] = tolower(str[i]);
    }
    printf("결과: %s\n", str);
}

int main() {
    void (*functions[3])(char*) = { get_length, to_uppercase, to_lowercase }; // 함수 포인터 배열
    int choice;
    char str[SIZE];

    while (1) {
        // 메뉴 출력
        printf("\n1. 문자열 길이 구하기\n");
        printf("2. 대문자로 변환하기\n");
        printf("3. 소문자로 변환하기\n");
        printf("0. 종료\n");
        printf("선택: ");
        scanf("%d", &choice);
        getchar(); // 개행 문자 제거

        if (choice == 0) {
            printf("프로그램을 종료합니다.\n");
            break;
        }

        if (choice < 1 || choice > 3) {
            printf("잘못된 입력입니다. 다시 선택하세요.\n");
            continue;
        }

        // 사용자 문자열 입력
        printf("변환할 문자열 입력: ");
        fgets(str, SIZE, stdin);
        str[strcspn(str, "\n")] = 0; // 개행 문자 제거

        // 함수 포인터 배열을 사용하여 함수 호출
        functions[choice - 1](str);
    }

    return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define MAX_SIZE 5

int main() {
    int row, col;
    int arr[MAX_SIZE][MAX_SIZE]; // 최대 크기의 2차원 배열
    int* ptr[MAX_SIZE]; // 각 행을 가리키는 포인터 배열

    // 행과 열의 크기 입력받기 (최대 5x5)
    printf("행과 열의 수를 입력하세요? ");
    scanf("%d %d", &row, &col);

    if (row > MAX_SIZE || col > MAX_SIZE || row <= 0 || col <= 0) {
        printf("잘못된 입력입니다. 행과 열은 최대 %d x %d 입니다.\n", MAX_SIZE, MAX_SIZE);
        return 1;
    }

    // 배열 값 자동 입력 (각 행에 i * 10 + j 저장)
    printf("배열의 초기값은 자동으로 입력됩니다.\n");
    for (int i = 0; i < row; i++) {
		     // 포인터 배열 초기화 (각 행의 시작 주소 저장)
		    ptr[i] = arr[i];
        for (int j = 0; j < col; j++) {
            ptr[i][j] = i * 10 + j;
            printf("%d ", ptr[i][j]);
        }
        printf("\n");
    }

    // 삭제할 행 입력받기
    int deleteRow;
    printf("삭제하려는 행의 인덱스 번호를 입력하세요? ");
    scanf("%d", &deleteRow);

    if (deleteRow < 0 || deleteRow >= row) {
        printf("잘못된 행 번호입니다.\n");
        return 1;
    }

    // 행 삭제 (deleteRow 이후의 행들을 한 칸씩 위로 이동)
    for (int i = deleteRow; i < row - 1; i++) {
        ptr[i] = ptr[i + 1]; // 포인터 배열의 참조 변경
    }
    row--; // 실제 행 개수를 하나 줄임

    // 수정된 배열 출력
    printf("수정된 배열 출력\n");
    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            printf("%d ", ptr[i][j]);
        }
        printf("\n");
    }

    return 0;
}

```
