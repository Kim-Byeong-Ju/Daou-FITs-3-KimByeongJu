# TIL: C프로젝트

## 학습 날짜: 2025년 3월 26일

## 오늘 배운 내용 정리

1. **OCI에서 게시글 등록을 위한 바인딩 처리**
    - `OCIStmtBindByName` 함수 사용 방법에 대해 질문함.
    - `SQLT_INT`와 `SQLT_STR` 바인딩 타입을 사용하여 `customer_uuid`, `title`, `content`를 바인딩하는 방법을 확인함.
    - `OCIStmtPrepare2`와 `OCIStmtExecute`를 사용하여 SQL 문을 준비하고 실행하는 과정에 대해 문의함.
2. **Oracle에서 `auto_increment` 설정**
    - 게시글의 `post_id` 필드에 `auto_increment` 설정을 하는 방법에 대해 물어봄.
    - Oracle에서는 `sequence` 객체와 `trigger`를 사용하여 자동 증가를 처리해야 한다는 점을 확인함.
3. **OCI 바인딩 오류 해결**
    - `OCIStmtBindByName` 함수에서 바인딩이 제대로 되지 않는 문제를 해결하기 위한 코드 수정 방법을 문의함.
    - 바인딩 시 반환 값을 확인하고, 오류 발생 시 메시지를 출력하여 문제를 해결할 수 있는 방법을 제시함.
4. **OCI 관련 변수 처리 방법**
    - OCI 변수(`envhp`, `svchp`, `errhp`)를 함수 인자로 넘기지 않고 처리하는 방법에 대해 논의함.
    - 이를 위해 전역 변수로 OCI 세션을 관리하는 방법을 확인함.
