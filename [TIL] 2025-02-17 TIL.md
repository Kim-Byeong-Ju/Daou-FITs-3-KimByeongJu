# TIL: Servlet 활용

## 학습 날짜: 2025년 2월 17일

## 오늘 배운 내용 정리

> **Servlet 활용**
> 

1. Web Project 생성(JDK, tomcat, Jakarta EE 버전 통일 → 현재 강의에서는 8버전으로)
2. Tomcat을 이용해서 Deploy(IntelliJ 구성 - 편집 - 배포에서 Application context를 수정해서 사용할 수 있음) 
3. Servlet이라고 불리는 Java program을 작성해서 web client를 호출해서 그 결과를 browser를 통해 받아보려면 어떻게 해야 할까?
    - Servlet은 interface이다. 그러나, servlet interface를 직접적으로 구현하는게 아님
    - Servlet - GenericServlet class - HttpServlet class 가 있다.
    - 이 때, HttpServlet class를 상속해서 Servlet을 구현한다.
    - 상속되는 method가 존재
        - init(): Servlet 객체가 생성될 때 호출되는 초기화를 담당하는 method(생성자 대신 init()를 이용해서 초기화 진행)
        - service(): 클라이언트가 Servlet을 실행할 때 호출되는 method. 이 service는 이미 하는 일이 구현되어 있어서 일반적으로 overriding 하지 않는다. 클라이언트의 요청이 GET 방식이면 doGet()을 호출하고, POST 방식이면 doPost()를 호출하는 것으로 구현되어 있다.
            - 클라이언트가 서버 쪽 프로그램을 호출할 때 2가지 방식이 존재
                - GET 방식
                - POST 방식
            - doGet(), doPost(): 이 두 개의 method를 overriding 해서 클라이언트가 원하는 작업을 수행하면 된다
        - destroy(): Sevlet instance가 WAS에서 삭제될 때 수행되는 method
            - 사용하고 있는 여러 resource들을 해제해야 한다
4. 클라이언트가 GET 방식으로 Sevlet을 호출했을 때 간단하게 response를 전송하는 Sevlet을 실제로 구현
    - 클라이언트가 Servlet을 호출하는 방법을 제공해야 한다.
    - 쉽게 말하면, Servlet에 논리적인 이름을 하나 부여해서 그걸 클라이언트가 이용해서 Servlet을 호출할 수 있도록 해야 한다.
    - 1)  xml 파일을 이용해서  그 설정을 잡을 수 있다. xml을 이용하기 때문에 번거롭지만, 전체적인 이름 관리를 하나의 파일에서 할 수 있기 때문에 관리하는 측면에서는 좋다
    - 2) annotation을 이용해서 이름을 설정할 수 있다. 이게 일반적인 방식. 직관적으로 사용할 수 있어서 훨씬 선호

1. doGet()에서 클라이언트에게 내용 전달
    - PrintWriter를 이용해서 클라이언트에게 데이터 연결 통로를 열고, 그 통로를 통해서 데이터를 전달하면 된다
    - ContentType을 잘 설정해야 함

1. 클라이언트가 Servlet에게 데이터를 전달
    - GET 방식
        - Query String을 이용해서 데이터를 전달하는 방식을 사용
            - Query String은 특수한 형식을 가진 문자열
        
        ```java
        ?변수-값&변수=값&변수=값
        물음표로 시작, 변수에 값 할당
        
        ?name=홍길동&age=20&address=서울
        ```
        
        ⇒ `http://localhost:8080/ServletProject/myServlet?name=홍길동&age=20&address=서울`
        
        - 서버쪽(Servlet)에서는 전달된 데이터를 받아야 한다 ⇒ getParameter(”변수명”)
        - 이렇게 데이터를 전달하는 방식은 보안성이 전혀 없다. 그래서, 128 bit 암호화를 이용해서 처리
        - 또한, url의 길이 제한으로 인해 크기가 큰 데이터를 전달하기에 무리가 있음
    - POST 방식
        - 보안성이 약간 있다.
        - request header에 포함되어 전달되기 때문에 길이의 제한이 없다.
        - HTML 특수한 태그를 이용해서 서버쪽 프로그램을 호출하는 구조
        - 클라이언트에게 보여줄 html 하나 만들기!
