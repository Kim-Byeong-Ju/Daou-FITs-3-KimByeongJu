# TIL: C언어 문자열 처리, 변수의 종류, 포인터

## 학습 날짜: 2025년 3월 11일

## 오늘 배운 내용 정리

### 문자열 처리 문제 풀이

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
int main()
{
	printf("%c", toupper('e'));

	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
int main()
{
	char str[] = "12ABCdeFG";
	for (int i = 0; i < sizeof(str) / sizeof(char); i++) {
		if (str[i] >= 'a' && str[i] <= 'z') printf("%c", toupper(str[i]));
		else printf("%c", tolower(str[i])); 
	}
	
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
int main()
{	
	char eng[] = "swimming";
	char kor[] = "가나다라마바";
	printf("영문 순방향 : %s\n", eng);
	printf("영문 역방향 : ");
	for (int i = strlen(eng) - 1; i >= 0; i--) {
		printf("%c", eng[i]);
	}
	printf("\n");
	printf("한글 순방향 : %s\n", kor);
	printf("한글 역방향 : ");
	for (int i = strlen(kor) - 2; i >= 0; i-=2) {
		printf("%c%c", kor[i], kor[i+1]);
	}
	printf("\n");
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{	
	char* s1 = "47476";
	char* s2 = "98765432";
	char* s3 = "3456.7634";
	printf("%d\n", atoi(s1));
	printf("%d\n", atol(s2));
	printf("%.4fs", atof(s3));
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{	
	int num = 3198;
	char n[50];
	printf("정수 : %d    문자열 : %s", num, _itoa(num, n, 10));
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
	char s1[] = "abcdefg";
	char s2[] = "012";
	printf("%s", strncpy(s1, s2, strlen(s2)));
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
	char s1[] = "abcdefg";
	char s2[] = "012";
	char str[20];
	memmove(str, s1, strlen(s1)+1);
	memmove(str+3, s2, strlen(s2));
	printf("%s", str);
	return 0;
}
```

### `memcpy` vs `memmove` 차이점

| 함수 | 메모리 겹칠 경우 안전성 | 속도 | 주요 사용 사례 |
| --- | --- | --- | --- |
| `memcpy` | **안전하지 않음** (겹치는 경우 문제 발생) | **더 빠름** | 겹치지 않는 메모리 복사 |
| `memmove` | **안전함** (겹쳐도 정상 동작) | 상대적으로 느림 | 겹칠 가능성이 있는 메모리 이동 |

### 변수의 종류

1. **지역변수(local variable), 자동변수(auto)**
    - 중괄호 내부, 함수의 매개변수에서 사용되는 변수이다.
    - 지역변수의 지역은 함수의 내부, 중괄호 내부를 말한다
    - 만든 곳을 벗어나면 사라진다.
    - 변수의 메모리 공간이 소멸 된다.
    - 지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기값이 저장된다
2. **전역변수(global)** 
    - 중괄호 외부에 선언되는 변수이다
    - 어느 지역에서든 참조할 수 있다.
    - 전처리기 밑에 선언하고 반드시 상수로 초기화 해야 한다.
    - 초기값을 지정하지 않으면 기본값이 0으로 자동 초기화 된다.
    - 모든 곳에 접근 가능하고 프로그램 종료 전에 메모리가 소멸되지 않는다
    - 잘못 이용하면 유지보수, 재사용등이 어려워 질 수 있다.
    - 이것의 단점을 보안한 것이 정적변수이다
3. **정적변수(static)** 
    - 프로그램 종료되기 전까지 메모리가 소멸되지 않는 변수이다
    - 함수를 벗어나도 변수가 사리지지 않고 유지된다
    - 초기화할 때 반드시 상수로 초기화 해야 한다
    - 초기화하지 않으면 디폴트값 0으로 자동 초기화 된다
    - 정적 변수의 종류
        - 정적 전역변수(static global variable) : 자신이 선언된 소스 파일에서만 사용할 수 있고
        외부에서는 가져다 쓸 수 없다. 즉 전역변수에 static을 붙이면 변수의 범위를 파일 범위로 제한하는 효과를 낸다.
        - 정적 지역변수(static local variable) : 중괄호 내부에서만 사용할 수 있다. 한번 초기화 되
        면 이후에 함수 호출시 값의 초기화를 무시한다.

### 포인터

포인터는 변수의 메모리 주소를 저장하는 변수

- 기본 예제
    
    ```c
    #include <stdio.h>
    int main() {
    int a = 10; // 정수형 변수
    int *p = &a; // a의 주소를 저장하는 포인터 변수
    printf("변수 a의 값: %d\n", a);
    printf("변수 a의 주소: %p\n", &a);
    printf("포인터 p가 가리키는 주소: %p\n", p);
    printf("포인터 p가 가리키는 값: %d\n", *p); // 포인터 역참조
    return 0;
    }
    ```
    
    - *p를 사용하면 p가 가리키는 주소에 저장된 값을 가져올 수 있습니다. &a를 사용하면 변수 a의 메모리 주소를 얻을 수 있습니다.
    
- 포인터와 배열
    
    ```c
    #include <stdio.h>
    int main() {
     int arr[3] = {1, 2, 3};
     int *p = arr; // 배열 이름은 첫 번째 원소의 주소
     printf("arr[0] = %d, *p = %d\n", arr[0], *p);
     printf("arr[1] = %d, *(p+1) = %d\n", arr[1], *(p+1));
     printf("arr[2] = %d, *(p+2) = %d\n", arr[2], *(p+2));
     return 0;
    }
    ```
    
    - arr[i]는 *(arr + i) 와 동일합니다.
