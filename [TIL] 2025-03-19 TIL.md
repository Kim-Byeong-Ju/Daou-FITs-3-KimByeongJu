# TIL: 비트필드, 비트연산, 파일 읽기

## 학습 날짜: 2025년 3월 19일

## 학습 중 어려움을 느낀 부분

## 오늘 배운 내용 정리

### **최적화의 종류**

최적화는 크게 **메모리 최적화, 속도 최적화, 가독성 최적화**로 나뉜다.

---

### **1. 메모리 최적화**

👉 **최소한의 메모리로 데이터를 저장하고 공간을 절약하는 것이 목표이다.**

✔ **방법:**

- *비트 필드(Bit Field)**를 사용하여 작은 데이터를 한 개의 정수형 변수에 비트 단위로 저장한다.
- *비트 연산자(&, |, ~)**를 활용하여 불필요한 메모리 사용을 줄인다.

---

### **2. 속도 최적화**

👉 **연산 속도를 높여 프로그램 실행 속도를 향상시키는 것이 목표이다.**

✔ **방법:**

1. **구조체 정렬을 CPU 워드 크기에 맞춘다.**
    - CPU는 **4바이트 또는 8바이트** 정렬을 선호하므로, 구조체 데이터를 **4바이트 단위**로 묶으면 성능이 향상된다.
    - 예제:
        
        ```c
        c
        복사편집
        typedef struct {
            uint32_t flags; // 4바이트 정렬로 성능 최적화
        } AccountStatus;
        
        ```
        
2. **비트 연산을 활용하여 속도를 개선한다.**
    - *비트 연산자(&, |, ~)**를 직접 사용하면 연산 속도가 향상된다.
    - 예제:
        
        ```c
        c
        복사편집
        #define FLAG_1 (1 << 0) // 00000001
        #define FLAG_2 (1 << 1) // 00000010
        
        uint8_t status = 0;
        status |= FLAG_1; // FLAG_1 활성화
        status &= ~FLAG_2; // FLAG_2 비활성화
        
        ```
        

---

### **3. 가독성 최적화**

👉 **코드를 이해하기 쉽게 개선하는 것이 목표이다.**

✔ **방법:**

- **enum과 #define을 활용하여 가독성을 높인다.**
- 매직 넘버 대신 의미 있는 상수를 사용하여 코드를 쉽게 이해할 수 있도록 한다.
- 예제:
    
    ```c
    c
    복사편집
    typedef enum {
        STATUS_OK = 0,
        STATUS_ERROR = 1,
        STATUS_PENDING = 2
    } Status;
    
    ```
    

---

### **비트 필드(Bit Field)와 비트 연산의 활용**

👉 **비트 필드는 구조체 내에서 변수를 비트 단위로 저장할 수 있도록 하는 기능이다.**

✔ **특징:**

- 여러 개의 불리언 값을 하나의 정수형 변수에 저장할 수 있다.
- 불필요한 메모리 낭비를 줄일 수 있다.

✔ **구조체 크기가 4바이트인 이유:**

- 비트 필드의 총 비트 수가 7비트여도, **CPU의 기본 워드 크기(32비트 = 4바이트)에 맞게 정렬되기 때문**이다.
- 일부 컴파일러는 패딩을 추가하여 4바이트로 정렬할 수도 있다.

✔ **비트 필드 예제:**

```c
c
복사편집
typedef struct {
    unsigned int flag1 : 1;
    unsigned int flag2 : 1;
    unsigned int flag3 : 1;
    unsigned int flag4 : 1;
    unsigned int reserved : 3;
} Flags;

```

- `flag1 ~ flag4`까지 총 4비트, `reserved` 3비트를 합쳐 **총 7비트만 사용**한다.
- 하지만, CPU 정렬 특성상 **4바이트(32비트)로 저장될 가능성이 높다.**

✔ **비트 연산을 사용하여 구조체 크기를 1바이트로 줄이기:**

```c
c
복사편집
typedef struct {
    uint8_t flags; // 1바이트
} Flags;

```

- `uint8_t(1바이트)`를 사용하고, **비트 연산(&, |, ~)을 활용하면 메모리를 더욱 절약**할 수 있다.

---

### realloc 응용 문제

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX 100
typedef struct {
	char stock_name[MAX];
	int amount;
	double price;
} Transaction;
typedef struct {
	char name[MAX];
	int account_number;
	int transaction_count;
	Transaction* transactions;
} Customer;
int main() {
	int n;
	Customer* customers = NULL;
	printf("고객 수를 입력하세요: ");
	scanf("%d", &n);
	customers = (Customer*)realloc(customers, n*sizeof(Customer));
	for (int i = 0; i < n; i++) {
		printf("고객 %d의 이름을 입력하세요: ", i + 1);
		scanf("%s", customers[i].name);
		printf("고객 %d의 계좌 번호를 입력하세요: ", i + 1);
		scanf("%d", &customers[i].account_number);
		printf("고객 %d의 거래내역 수: ", i + 1);
		scanf("%d", &customers[i].transaction_count);
		customers[i].transactions = NULL;  // 초기 NULL 설정 후 realloc
		customers[i].transactions = (Transaction*)realloc(customers[i].transactions, customers[i].transaction_count*sizeof(Transaction));
		for (int j = 0; j < customers[i].transaction_count; j++) {
			printf("주식 이름을 입력하세요: ");
			scanf("%s", customers[i].transactions->stock_name);
			printf("거래 수량을 입력하세요: ");
			scanf("%d", &customers[i].transactions->amount);
			printf("거래 가격을 입력하세요: ");
			scanf("%lf", &customers[i].transactions->price);
		}
	}
	printf("\n");
	for (int i = 0; i < n; i++) {
		printf("고객 %d: %s, 계좌번호: %d\n", i + 1, customers[i].name, customers[i].account_number);
		for (int j = 0; j < customers[i].transaction_count; j++) {
			printf("거래 %d: 주식 이름: %s, 거래 수량: %d, 거래 가격: %.2lf\n", j + 1, customers[i].transactions->stock_name, customers[i].transactions->amount, customers[i].transactions->price);
		}
		printf("\n");
	}
	for (int i = 0; i < n; i++) {
		free(customers[i].transactions);  // 대출 정보 메모리 해제
	}

	free(customers);
	return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_NAME 100
#define MAX_ACCOUNT 20

// 주식 정보를 저장하는 구조체
typedef struct {
    char name[MAX_NAME];  // 주식 종목명
    double avg_price;     // 평균 구매 가격
    double current_price; // 현재 가격
    int quantity;         // 보유 수량
} Stock;

// 고객 정보를 저장하는 구조체
typedef struct {
    char name[MAX_NAME];      // 고객 이름
    char account[MAX_ACCOUNT];// 계좌 번호
    Stock* portfolio;         // 보유 주식 목록 (동적 할당)
    int stock_count;          // 보유 주식 개수
} Customer;

// 고객 목록 관리 (동적 할당)
Customer* customers = NULL;
int customer_count = 0;

// 고객 추가
void add_customer() {
    customer_count++;
    customers = realloc(customers, customer_count * sizeof(Customer));

    printf("고객 이름 입력: ");
    scanf("%s", customers[customer_count - 1].name);
    printf("계좌 번호 입력: ");
    scanf("%s", customers[customer_count - 1].account);

    customers[customer_count - 1].portfolio = NULL;
    customers[customer_count - 1].stock_count = 0;
}

// 주식 추가
void add_stock(int customer_index) {
    Customer* customer = &customers[customer_index];

    customer->stock_count++;
    customer->portfolio = realloc(customer->portfolio, customer->stock_count * sizeof(Stock));

    Stock* new_stock = &customer->portfolio[customer->stock_count - 1];

    printf("종목명 입력: ");
    scanf("%s", new_stock->name);
    printf("평균 구매 가격 입력: ");
    scanf("%lf", &new_stock->avg_price);
    printf("현재 가격 입력: ");
    scanf("%lf", &new_stock->current_price);
    printf("보유 주식 수 입력: ");
    scanf("%d", &new_stock->quantity);
}

// 고객 포트폴리오 출력
void display_portfolio(int customer_index) {
    Customer* customer = &customers[customer_index];
    printf("\n고객: %s (%s)\n", customer->name, customer->account);

    double total_purchase = 0, total_current_value = 0;

    for (int i = 0; i < customer->stock_count; i++) {
        Stock* stock = &customer->portfolio[i];
        double purchase_value = stock->avg_price * stock->quantity;
        double current_value = stock->current_price * stock->quantity;

        total_purchase += purchase_value;
        total_current_value += current_value;

        printf("종목: %s | 구매가: %.2lf | 현재가: %.2lf | 보유량: %d | 구매 금액: %.2lf | 현재 가치: %.2lf\n",
            stock->name, stock->avg_price, stock->current_price, stock->quantity, purchase_value, current_value);
    }
    printf("총 구매 금액: %.2lf | 총 현재 가치: %.2lf\n", total_purchase, total_current_value);
}

// 동적 메모리 해제
void free_memory() {
    for (int i = 0; i < customer_count; i++) {
        free(customers[i].portfolio);
    }
    free(customers);
}

int main() {
    int choice, customer_index;

    while (1) {
        printf("\n1. 고객 추가\n2. 주식 추가\n3. 포트폴리오 보기\n4. 종료\n");
        printf("선택: ");
        scanf("%d", &choice);

        switch (choice) {
        case 1:
            add_customer();
            break;
        case 2:
            if (customer_count == 0) {
                printf("등록된 고객이 없습니다.\n");
                break;
            }
            printf("고객 번호 (0~%d) 선택: ", customer_count - 1);
            scanf("%d", &customer_index);
            if (customer_index >= 0 && customer_index < customer_count)
                add_stock(customer_index);
            else
                printf("잘못된 번호입니다.\n");
            break;
        case 3:
            if (customer_count == 0) {
                printf("등록된 고객이 없습니다.\n");
                break;
            }
            printf("고객 번호 (0~%d) 선택: ", customer_count - 1);
            scanf("%d", &customer_index);
            if (customer_index >= 0 && customer_index < customer_count)
                display_portfolio(customer_index);
            else
                printf("잘못된 번호입니다.\n");
            break;
        case 4:
            free_memory();
            return 0;
        default:
            printf("잘못된 입력입니다.\n");
        }
    }
}
```

### 파일

### **1. 운영체제별 파일 시스템**

- **Windows**: FAT32, NTFS, exFAT
- **Mac**: exFAT, APFS
- **Linux**: ext4

### 2. **C언어와 Java의 파일 처리 비교**

✅ **공통점**

1. 파일 입출력 기능 제공 (`fopen`, `fread`, `FileInputStream`, `BufferedReader` 등)
2. 파일 열기/닫기 필요 (`fopen/fclose`, `close()`)
3. 예외 처리 방식 차이 (`NULL` 체크 vs. `IOException` 예외 처리)
4. 텍스트 및 바이너리 파일 처리 가능

⚡ **차이점**

| 구분 | C 언어 | Java |
| --- | --- | --- |
| **파일 처리 방식** | 파일 포인터(`FILE *`) 사용 | 스트림(`InputStream`, `OutputStream`) 사용 |
| **입출력 방식** | 저수준, 수동 버퍼 관리 | 고수준, `BufferedReader/Writer` 사용 |
| **예외 처리** | `NULL` 반환 후 직접 확인 | `try-catch`로 예외 처리 |
| **파일 시스템 접근** | 직접 접근 (`fopen, fread, fwrite`) | 스트림 API 사용 (`FileInputStream`) |
| **버퍼링** | 직접 관리 | 자동 관리 (`BufferedReader`) |

### 3. **파일 처리에서 알아야 할 개념**

- **스트림(Stream)**: 프로그램과 입출력 장치를 연결하는 매개체
- **표준 스트림 (C 언어)**:
    - `stdin`(표준 입력, 키보드)
    - `stdout`(표준 출력, 모니터)
    - `stderr`(표준 에러 출력, 모니터)
- **버퍼(Buffer)**: 데이터를 효율적으로 처리하는 임시 저장 공간
    - `fflush(FILE *stream)`: 버퍼 비우기
- **EOF(End Of File)**: 파일 끝 확인 (`feof(FILE *stream)`)
- **파일 스트림 종류**
    - **읽기(Read)**, **쓰기(Write)**, **덧붙이기(Append)**, **읽기/쓰기(Read/Write)**
    - **텍스트 스트림(.txt)** vs. **바이너리 스트림(.jpg, .mp3, .exe 등)**

### **4. 파일 관련 주요 함수 (C 언어)**

- **파일 열기/닫기**: `fopen()`, `fclose()`
- **데이터 읽기/쓰기**: `fread()`, `fwrite()`, `fgets()`, `fputs()`, `fgetc()`, `fputc()`
- **파일 위치 조정**: `fseek()`, `feof()`

🔹 **결론**: C언어는 **파일 포인터**를 사용하여 직접 파일을 관리하며, **저수준** 입출력 방식이다. 반면, Java는 **스트림 API**를 활용한 **고수준**의 입출력을 제공하며, 자동 **예외 처리**와 **버퍼링 기능**을 지원한다.

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#define FILE_NAME "customers.txt"

typedef struct {
	int id;
	char name[50];
	double balance;
} Customer;

void addCustomer() {
	FILE* file = fopen(FILE_NAME, "a");
	if (file == NULL) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	Customer c;
	printf("고객 ID : ");
	scanf("%d", &c.id);
	printf("고객 이름 : ");
	scanf("%s", c.name);
	printf("잔액 : ");
	scanf("%lf", &c.balance);
	fprintf(file, "ID: %d, Name: %s, Balance: %.2lf\n", c.id, c.name, c.balance);
	fclose(file);
	printf(">> 고객 정보가 저장되었습니다\n");
};
void listCustomer() {
	FILE* file = fopen(FILE_NAME, "r");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	char line[100];
	printf("\n[고객 목록]\n");
	while (fgets(line, sizeof(line), file)) {
		printf("%s", line);
	}
	fclose(file);
};
void searchCustomer() {
	FILE* file = fopen(FILE_NAME, "r");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	int searchId;
	printf("검색할 고객 ID : ");
	scanf("%d", &searchId);
	char line[100];
	int found = 0;
	while (fgets(line, sizeof(line), file)) {
		if (strstr(line, "ID:")) {
			int id;
			sscanf(line, "ID: %d", &id);
			if (id == searchId) {
				printf("%s", line);
				found = 1;
				break;
			}
		}
	}
	if (!found) printf("해당 ID의 고객은 찾을 수 없습니다.");
	
};

int main() {
	char choice;
	while (1) {
		printf("\n[고객 정보 시스템]\n");
		printf("1. 신규 고객 추가 (N)\n");
		printf("2. 고객 목록 출력 (L)\n");
		printf("3. 고객 정보 검색 (S)\n");
		printf("4. 종료 (Q)\n");
		printf("선택: ");
		scanf(" %c", &choice);

		switch (choice) {
			case 'N': case 'n': addCustomer(); break;
			case 'L': case 'l': listCustomer(); break;
			case 'S': case 's': searchCustomer(); break;
			case 'Q': case 'q': return 1;
		
		}
	}
	return 0;
}

```
