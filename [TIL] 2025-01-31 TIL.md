# TIL: SQL 고급

## 학습 날짜: 2025년 1월 31일

## 오늘 배운 내용 정리

- COMMIT 이후 ROLLBACK 하는 방법
   ```sql
  FLASHBACK TABLE ACCOUNTS TO TIMESTAMP (SYSTIMESTAMP - INTERVAL '10' MINUTE);
  ALTER TABLE ACCOUNTS ENABLE ROW MOVEMENT;
  ```
  - FLASHBACK을 활용하여 이전 상태로 되돌리기

- SQL 프로젝트 ERD 작성
  ![증권사 데이터베이스](https://github.com/user-attachments/assets/f3bf955e-89f9-4522-8d56-120291865968)

- DB 샘플 데이터 생성
  1. USERS 테이블

```sql
INSERT INTO users (user_id, password, gender, name, phone_number, email, age, birth_date, income, investment_type, created_at, updated_at)
SELECT 
    'user' || LPAD(LEVEL, 4, '0') AS user_id,
    'password' || LPAD(LEVEL, 4, '0') AS password,
    CASE WHEN MOD(LEVEL, 2) = 0 THEN 'M' ELSE 'F' END AS gender,
    'User' || LPAD(LEVEL, 4, '0') AS name,
    '010-' || LPAD(MOD(LEVEL, 10000), 4, '0') || '-' || LPAD(MOD(LEVEL, 10000), 4, '0') AS phone_number,
    'user' || LPAD(LEVEL, 4, '0') || '@example.com' AS email,
    MOD(LEVEL, 80) + 20 AS age,
    TO_CHAR(DATE '1980-01-01' + (LEVEL * 10), 'YYYYMMDD') AS birth_date,
    MOD(LEVEL, 100) * 10000 AS income,
    CASE WHEN MOD(LEVEL, 3) = 0 THEN 'A' WHEN MOD(LEVEL, 3) = 1 THEN 'B' ELSE 'C' END AS investment_type,
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' DAY) AS created_at,
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' HOUR) AS updated_at
FROM 
    DUAL
CONNECT BY LEVEL <= 100;
```

1. STOCKS 테이블

```sql
INSERT INTO stocks (stock_id, name, status, exchange_code, description, sector, list_date, delist_date, created_at, updated_at)
SELECT 
    'STOCK' || LPAD(LEVEL, 4, '0') AS stock_id,
    'Stock ' || LPAD(LEVEL, 4, '0') AS name,
    CASE WHEN MOD(LEVEL, 2) = 0 THEN 'A' ELSE 'I' END AS status, -- A: Active, I: Inactive
    CASE WHEN MOD(LEVEL, 3) = 0 THEN 'NYSE' WHEN MOD(LEVEL, 3) = 1 THEN 'NASD' ELSE 'TSE' END AS exchange_code, -- 4자리로 조정
    'Description for Stock ' || LPAD(LEVEL, 4, '0') AS description,
    CASE WHEN MOD(LEVEL, 5) = 0 THEN 'TECH' WHEN MOD(LEVEL, 5) = 1 THEN 'FIN' WHEN MOD(LEVEL, 5) = 2 THEN 'HLTH' WHEN MOD(LEVEL, 5) = 3 THEN 'ENER' ELSE 'CONS' END AS sector,
    TO_CHAR(DATE '2000-01-01' + (LEVEL * 30), 'YYYYMMDD') AS list_date,
    CASE WHEN MOD(LEVEL, 10) = 0 THEN TO_CHAR(DATE '2000-01-01' + (LEVEL * 30 + 3650), 'YYYYMMDD') ELSE NULL END AS delist_date, -- 10%의 주식만 delist_date 설정
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' DAY) AS created_at,
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' HOUR) AS updated_at
FROM 
    DUAL
CONNECT BY LEVEL <= 100;
```

1. HOLDINGS 테이블

```sql
MERGE INTO holdings h
USING (
    SELECT 
        o.stock_id, 
        o.account_id, 
        -- 거래 타입에 따라 수량을 추가 또는 차감
        CASE WHEN o.type = 'BUY' THEN o.quantity
             WHEN o.type = 'SELL' THEN -o.quantity
             ELSE 0 
        END AS quantity,
        -- 총 가격을 계산
        o.quantity * o.price AS total_price
    FROM orders o
) o
ON (h.stock_id = o.stock_id AND h.account_id = o.account_id)  -- PK로 사용되는 복합키로 일치 확인
WHEN MATCHED THEN 
    -- 이미 해당 계좌와 종목에 대한 데이터가 존재하는 경우, 수량을 갱신하고 가격을 업데이트
    UPDATE SET 
        h.quantity = h.quantity + o.quantity,  -- 수량 누적 또는 차감
        h.total_price = h.total_price + o.total_price,  -- 총 가격 갱신
        h.updated_at = CURRENT_TIMESTAMP  -- 업데이트 타임스탬프
WHEN NOT MATCHED THEN
    -- 해당 계좌와 종목에 대한 데이터가 존재하지 않으면 새로 삽입
    INSERT (stock_id, account_id, quantity, total_price, created_at, updated_at)
    VALUES (o.stock_id, o.account_id, o.quantity, o.total_price, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);

```

1. ACCOUNTS 테이블

```sql
INSERT INTO accounts (account_id, user_id, account_number, status, type, password, open_date, close_date, deposit, withholding, created_at, updated_at)
SELECT 
    'ACCT' || LPAD(LEVEL, 4, '0') AS account_id,
    'user' || LPAD(MOD(LEVEL, 100), 4, '0') AS user_id, -- 100명의 사용자 중 랜덤하게 할당
    '0000-' || LPAD(MOD(LEVEL, 10000), 4, '0') || '-' || LPAD(MOD(LEVEL, 10000), 4, '0') AS account_number, -- 0000-0000-0000 형식
    CASE WHEN MOD(LEVEL, 2) = 0 THEN 'A' ELSE 'I' END AS status, -- A: Active, I: Inactive
    CASE WHEN MOD(LEVEL, 3) = 0 THEN 'SAV' WHEN MOD(LEVEL, 3) = 1 THEN 'CHK' ELSE 'INV' END AS type, -- SAV: Savings, CHK: Checking, INV: Investment
    'pass' || LPAD(LEVEL, 4, '0') AS password,
    TO_CHAR(DATE '2000-01-01' + (LEVEL * 30), 'YYYYMMDD') AS open_date,
    CASE WHEN MOD(LEVEL, 10) = 0 THEN TO_CHAR(DATE '2000-01-01' + (LEVEL * 30 + 3650), 'YYYYMMDD') ELSE NULL END AS close_date, -- 10%의 계좌만 close_date 설정
    ROUND(DBMS_RANDOM.VALUE(1000, 1000000), 2) AS deposit, -- 1,000부터 1,000,000 사이의 랜덤 금액
    ROUND(DBMS_RANDOM.VALUE(0, 10000), 2) AS withholding, -- 0부터 10,000 사이의 랜덤 금액
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' DAY) AS created_at,
    SYSTIMESTAMP - (LEVEL * INTERVAL '1' HOUR) AS updated_at
FROM 
    DUAL
CONNECT BY LEVEL <= 100;

```
