# TIL: DB모델링

## 학습 날짜: 2025년 1월 14일

## 오늘 배운 내용 정리

### 데이터 모델링

- 정규화
    - 쉽게 얘기하자면, 데이터가 꼬이는 것을 막기 위해 테이블을 잘게 쪼개는 과정
    - 데이터베이스의 설계 단계에서 데이터 중복을 제거하고 데이터 무결성을 유지하기 위한 과정
    - 데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계
    - 데이터 변경 시 발생할 수 있는 문제(삽입, 삭제, 갱신 이상) 최소화

- **정규형** vs **정규화**
    - 정규형은 정규화 규칙에 맞춰서 구조된 테이블을 의미
    - 정규화는 개선의 필요가 있는 테이블을 수정하는 과정

- 제 1정규형(1NF)
    - 모든 속성은 반드시 하나의 값만 가져야 한다.
    - 유사한 속성이 반복되는 경우도 1차 정규화의 대상. ex) 직업1, 직업2, 직업3 …

- 제 2정규형(2NF)
    - 엔터티의 모든 **일반 속성**은 반드시 모든 주식별자(PK)에 종속되어야 한다.
        - 일반 속성: 주식별자가 아닌 모든 속성
    - 이상 현상(삽입, 삭제, 갱신)이 발생할 수 있기 때문에 정규화가 필요

- 제 3정규형(3NF)
    - 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.
    - 마찬가지로, 이상 현상 발생할 수 있음
        - ex) 새로운 소속사가 생겼는데, 소속 연예인이 없을 때는 주식별자가 존재하지 않아서 추가할 수 없음.

- **정규형은 누적식. 제 3정규화는 제 2정규형은 만족했다는 뜻.**

[정규화 실습](https://www.notion.so/17be7852e4d68000b24df185b26fcd64?pvs=21)

- 반정규화
    - 데이터베이스 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
    - 성능 최적화와 효율적인 데이터 액세스를 위해 데이터 모델을 조정하는 것
    - 반정규화의 필요성
        - 정규화의 한계: 다수의 테이블이 조인이 필요한 쿼리에서는 성능 저하 발생. 읽는 속도가 중요한 상황에서 정규화된 데이터 모델이 비효율적일 수 있음
        - 성능 최적화: 쿼리 성능 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임. 주로 OLAP 시스템에서 활용
    - 반정규화 적용 방법
        - 속성 추가
        - 테이블 병합
        - 중복 데이터 저장: 데이터 분석 속도를 위해 요약 데이터를 미리 계산하고 테이블에 저장
        - 이력 데이터 관리
    - 반정규화의 장점
        - 성능 개선
        - 간단한 쿼리
        - 특정 요구사항 대응
    - 반정규화의 단점
        - 데이터 중복
        - 데이터 무결성 문제
        - 유연성 감소

- 파생속성
    - 다른 속성에 영향을 받아 발생하는 속성으로서 보통 계산된 값들이 이에 해당된다. 다른 속성에 영향을 받기 때문에 프로세스 설계 시 데이터 정합성을 유지하기 위해 유의해야 할 점이 많으며 가급적 파생속성을 적게 정의하는 것이 좋다.
    - ex) 나이(생년월일에 의해 계산된다.), 근무기간(입사일에 의해 계산된다.)

- OLTP(Online Transaction Processing)
    - 실시간으로 다수의 트랜잭션을 처리하는 시스템으로, 주로 운영환경에서 사용
    - 목적: 데이터 입력, 저장, 수정 및 조회를 빠르게 처리
    - 실시간 처리: 다수의 사용자 요청을 신속하게 처리
    - 정규화된 데이터 구조: 데이터 중복 최소화 및 데이터 무결성 유지
    - 트랜잭션 관리: ACID 속성(원자성, 일관성, 고립성, 지속성)
    - 구성 요소
        - 관계형 데이터베이스
        - 애플리케이션
        - 사용자
- OLAP(Online Analytical Processing)
    - 대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템
    - 목적: 데이터 분석, 보고소 생성, 비즈니스 인사이트 제공
    - 복잡한 분석: 집계, 요약, 패턴 분석 쿼리를 통해 비즈니스 인사이트 제공
    - 대규모 데이터 처리: 데이터 웨어하우스 및 데이터 마트를 활용하여 분석
    - 다차원 데이터 분석: 시간, 지역, 제품 등 여러 차원을 기준으로 데이터 요약
    - 구성 요소
        - 데이터 웨어하우스
        - OLAP 서버
        - 사용자 인터페이스
- OLTP와 OLAP 상호작용
    - OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용
    - OLTP에서 생성된 데이터를 OLAP로 추출하여 분석
    - ETL 프로세스: 데이터 웨어하우스로 데이터를 전송하기 위해 사용
    - OLTP → ETL → OLAP

- 논리 모델
    - 데이터베이스 설계의 한 단계로, 데이터의 구조와 관계를 기술하는 모델
    - 특정 DBMS에 종속되지 않으며, 데이터의 논리적 구조를 나타냄
    - 엔터티와 속성 정의
    - 관계와 제약 조건 표현
    - 데이터 중복 제거를 위한 정규화 적용
- 물리 모델
    - 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델
    - 데이터의 저장소, 성능, 보안을 고려하여 실제 구현
    - 데이터 타입과 제약 조건 지정
    - 인덱스, 파티션, 저장소 구조 설계
    - DBMS의 기술적 제약 및 성능 최적화 반영
- 논리 모델 → 물리 모델
    - 논리 모델에서 정의된 엔터티와 관계를 기반으로 물리적 테이블 생성
    - 속성을 데이터 타입과 제약 조건으로 매핑
    - DB 성능을 고려하여 인덱스와 파티션 추가
    - 변환 시 고려사항
        - DB 크기와 예상 트래픽
        - 읽기 및 쓰기 작업의 비율
        - DBMS 기능 및 제약

### SQL 기초

- SQL이란?
    - Structured Query Language
    - 관계형 데이터베이스에서 데이터를 검색, 추가, 수정, 삭제(CRUD) 하는 데 사용
    - 데이터 정의, 조작, 제어를 위한 명령어 집합 제공

- SQL 분류
    - DDL: 데이터베이스 구조 정의
        - CREATE, ALTER, DROP, TRUNCATE
    - DML: 데이터를 조작(삽입, 수정, 삭제)
        - SELECT, INSERT, UPDATE, DELETE
    - DCL: 데이터 접근 권한 제어
    - TCL: 트랜잭션 관리(Commit, Rollback)

- SELECT
    - SQL에서 데이터를 검색하는 명령어
    - 기본 문법
        
        ```sql
        SELECT [컬럼명] FROM [테이블명];
        ```
        
- ALIAS
    - SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름
    - 결과 집합의 가독성을 높이고, 복잡한 쿼리를 간결하게 작성할 수 있도록 돕는 기능
    - 특징
        - 테이블이나 컬럼 이름 대신 사용할 수 있음
        - 데이터베이스 구조에 영향을 주지 않음
        - 컬럼 ALIAS의 경우 AS 생략 가능
    - 사용하는 이유
        - 연산 결과에 의미를 부여
        - 출력 결과의 컬럼 이름을 변경하여 가독성을 높임
    - 문법
        
        ```sql
        SELECT [컬럼명] (AS) 별칭
        FROM [테이블명];
        ```
        
    - 테이블 ALIAS
        - 테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
        - JOIN 쿼리에서 테이블 이름 축약하여 사용
        
        ```sql
        SELECT [컬럼명] FROM [테이블명] 별칭;
        ```
        
- NULL
    - 데이터베이스에서 NULL은 “값이 없음”을 의미
    - 숫자, 문자 등 어떤 데이터도 저장되지 않은 상태
    - 데이터가 “비어 있다”고 간주되며, 이는 0이나 빈 문자열과는 다름
    - NULL 값은 다른 값과 비교할 수 없음
    - 비교 연산 수행하면 항상 FALSE를 반환하기 때문에 아무런 데이터도 나오지 않는다.

- WHERE
    - 조건을 지정하여 데이터를 필터링
    - SELECT, UPDATE, DELETE와 함께 사용
    - WHERE절 연산자
        - 비교 연산자
            - =: 값이 같은지 비교
            - <>: 값이 다른지 비교
            - >, <: 값이 크거나 작은지 비교
            - ≥, ≤: 값이 크거나 같음 또는 작거나 같음을 비교
        - 논리 연산자
            - AND: 여러 조건을 모두 만족해야 함.
            - OR: 여러 조건 중 하나만 만족하면 됨.
            - 우선순위: 나온 순서에 관계 없이 **AND부터!** OR를 먼저 적용시키려면 괄호를 집어넣어야 함
            - NOT: 조건의 결과를 반전시킴
        - 특수 연산자
            - LIKE: 특정 패턴을 가진 데이터를 검색
                - %: 0개 이상의 문자를 대체
                - _: 1개의 문자를 대체
                - %, _라는 문자열을 출력하려면?
                    - **ESCAPE** 문법 활용
                    - 예시: ‘C\%A%’ ESCAPE ‘\’ → ‘C%A’로 시작하는 모든 문자열
                    - ESCAPE 조건을 준 문자열 뒤에 오는 문자열은 그 자체로 포함
                    - ‘Data_2024’로 시작하는 데이터를 검색하려면?
                        
                        ```sql
                        SELECT FILE_NAME 
                        FROM FILES 
                        WHERE FILE_NAME LIKE ‘Data\_2024%’ ESCAPE ‘\’
                        ```
                        
            - IN: 지정된 값 중 하나와 일치하는 데이터를 찾음. OR 조건으로 대체 가능
            - BETWEEN: 지정된 범위 내에 있는 값을 찾음. 범위는 시작 값과 끝 값 포함
            - IS NULL: NULL 값을 가진 데이터를 찾음. NOT을 붙이면 NULL 값이 아닌 데이터를 찾음
        - 연산자 우선순위
            1. 괄호()
            2. 비교 연산자
            3. 논리 연산자 NOT
            4. 논리 연산자 AND
            5. 논리 연산자 OR
    - 트랜잭션
        - 데이터베이스에서 하나의 논리적 작업 단위를 의미
        - 일련의 작업이 모두 성공하거나 실패하는 것을 보장
        - 데이터 무결성과 일관성을 유지하기 위한 기본 단위로 사용
        - 트랜잭션의 특징
            - 원자성: 트랜잭션은 모두 실행되거나, 모두 실행되지 않아야 함. 실패 시 모든 변경사항을 ROLLBACK하여 원래 상태로 복구
                - 예시: 계좌 이체에서 송금과 입금 작업 중 하나라도 실패하면 전체 트랜잭션 취소
            - 일관성: 트랜잭션이 완료된 후 데이터베이스는 항상 일관된 상태를 유지해야 함. 데이터 무결성을 보장.
                - 예시: 데이터베이스의 모든 외래 키 관계가 트랜잭션 이후에도 유지
            - 고립성: 여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리. 트랜잭션 격리 수준으로 구현
                - 딜레마: 정합성을 높이지만, 성능은 떨어질 수 있음
                - 트랜잭션에서 COMMIT의 텀을 중간중간 추가하여 대기가 길어지지 않도록 설정하는 것이 좋음
                - 예시: 한 트랜잭션이 수정 중인 데이터를 다른 트랜잭션이 볼 수 없음
            - 지속성: 트랜잭션이 성공적으로 완료되면 변경된 데이터는 영구적으로 저장. 시스템 장애 후에도 데이터는 유지
                - 예시: 트랜잭션이 COMMIT 된 후 디스크에 기록된 데이터는 손실되지 않음
        - 트랜잭션 설계 시 유의사항
            - 작업 단위를 명확히 정의: 범위를 좁게 설계하여 성능과 오류 가능성 최소화
            - 최소한의 데이터 변경: 필요 이상의 데이터를 수정하거나 삭제하지 않도록 제한
            - 장기 트랜잭션 방지: 트랜잭션이 너무 오래 실행되지 않도록 설계하여 DB LOCK 문제 방지
            - 적절한 격리 수준 설정: 시스템 성능과 데이터 정확성 간의 균형을 유지
