# TIL: C언어 함수, 배열, 문자열 처리

## 학습 날짜: 2025년 3월 10일

## 오늘 배운 내용 정리

### 1. C언어의 매크로 상수

C언어에서 매크로 상수를 정의하는 방법과 이를 사용하는 이유에 대해 배웠다. 매크로 상수는 `#define`을 사용하여 선언하며, 컴파일 시 값이 변경되지 않는 고정된 상수를 정의하는 데 유용하다.

### ✅ 매크로 상수의 정의 방법

```
#define PI 3.14159
#define MAX_SIZE 100
```

위와 같이 정의하면, `PI`는 3.14159로, `MAX_SIZE`는 100으로 대체된다.

### ✅ 매크로 상수를 사용하는 이유

- **가독성 향상**: 의미 있는 이름을 부여하여 코드의 이해도를 높일 수 있다.
- **유지보수 편의성**: 여러 곳에서 동일한 값을 사용할 때, 한 곳만 수정하면 전체 코드에 반영된다.
- **컴파일 시간 처리**: 매크로 상수는 전처리기 단계에서 치환되므로 실행 중 메모리를 차지하지 않는다.

### 2. 배열과 포인터의 활용

C언어에서 배열을 선언하고 사용하는 방법을 익혔다. 또한 포인터를 활용하여 배열 요소에 접근하는 방법을 배웠다.

### ✅ 배열의 선언과 사용

```
int arr[5] = {1, 2, 3, 4, 5};
```

배열의 요소는 `arr[0]`, `arr[1]`과 같은 방식으로 접근할 수 있다.

### ✅ 포인터를 활용한 배열 접근

```
int *ptr = arr; // arr의 첫 번째 요소 주소를 가리킴
printf("%d", *(ptr + 1)); // arr[1] 값 출력 (2)
```

배열의 이름 자체가 첫 번째 요소의 주소를 가리키므로, 포인터를 사용하면 반복문을 활용한 데이터 처리도 용이하다.

### ✅ 배열 크기 구하는 방법

```
int size = sizeof(arr) / sizeof(arr[0]);
```

이 방법을 사용하면 배열이 몇 개의 요소를 가지는지 계산할 수 있다.

배운 내용을 바탕으로 실제 코드에서 매크로 상수를 활용하고, 배열과 포인터를 적극적으로 사용할 수 있도록 연습이 필요하다.

### 3. 실습 문제

```c
#include <stdio.h>
#define START 1
#define END 100
#define PRINT(c) printf("%d", c);
#define PI 3.141592
#define AREA PI*r*r //r은 변수의 이름
int main() {
	double r;
	printf("반지름을 입력 ");
	scanf_s("%d", &r);
	printf("원의 둘레 : %.lf", AREA);
}
```

```c
#include <stdio.h>
#define AND &
#define OR |
#define XOR ^
#define NOT ~
int main() {
	int x = 5;
	int y = 6;
	printf("X AND y = %d\n", x AND y);
	printf("X OR y = %d\n", x OR y);
	printf("X XOR y = %d\n", x XOR y);
	printf("NOT X = %d", NOT x);
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>      /* printf */
#include <time.h>       /* time_t, struct tm, difftime, time, mktime */
#include <stdlib.h>
int main()
{
	time_t t;
	struct tm* tm_info;

	time(&t);

	tm_info = localtime(&t);
	printf("현재 날짜와 시간 : %04d-%02d-%02d %02d:%02d:%02d\n", tm_info->tm_year + 1900, tm_info->tm_mon + 1, tm_info->tm_mday, tm_info->tm_hour, tm_info->tm_min, tm_info->tm_sec);
	return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int day[13] = { 0, 31,28,31,30,31,30,31,30,31,31,30,31 };
	int month;
	printf("1월부터 몇 월까지 총일을 구할까요? ");
	scanf("%d", &month);
	int days = 0;
	for (int i = 1; i <= month; i++) {
		days += day[i];
	}
	printf("1월부터 %d월까지는 총 %d일 있습니다", month, days);

	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int day[13] = { 0, 31, 28, 31, 30, 31, 30, 31, 30, 31, 31, 30, 31 };
    int foundMonths[12]; // 최대 12개월 저장 가능
    int count = 0;

    // 30일까지 있는 달을 foundMonths 배열에 저장
    for (int i = 1; i <= 12; i++) {
        if (day[i] == 30) {
            foundMonths[count++] = i;
        }
    }

    // 출력
    printf("12달 중 30일까지 있는 달은 ");
    for (int i = 0; i < count; i++) {
        if (i > 0) printf(","); // 쉼표 추가 (첫 번째 값에는 추가하지 않음)
        printf("%d", foundMonths[i]);
    }
    printf(" 총 %d달입니다.\n", count);

    return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int a[2][3] = { {1, 2, 3}, { 4,5,6 } };
    int b[2][3] = { {10, 20, 30}, { 40,50,60 } };
    int c[2][3];
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 3; j++) {
            c[i][j] = a[i][j] + b[i][j];
            printf("%d ", c[i][j]);
        }
        printf("\n");
    }

    return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int max = -999;
    int freeze[10] = { 15,0,-20,-30,50,-5,-120,-5,10,-12 };
    for (int i = 0; i < 10; i++) {
        if (freeze[i] > max) {
            max = freeze[i];
       }
    }
    printf("%d", max);
    return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>  // 표준편차 계산을 위해 포함

double avg(int arr[], int size);
double std(int arr[], int size);

int main() {
    int x[5] = { 55, 21, 32, 47, 19 };
    int size = sizeof(x) / sizeof(x[0]);  // 배열 크기 계산

    printf("평균 : %.1f\n", avg(x, size));
    printf("표준편차 : %.1f\n", std(x, size));

    return 0;
}

double avg(int arr[], int size) {
    int sum = 0;
    for (int i = 0; i < size; i++) {
        sum += arr[i];
    }
    return (double)sum / size;  // 형 변환 추가
}

double std(int arr[], int size) {
    double mean = avg(arr, size);
    double sum = 0;

    for (int i = 0; i < size; i++) {
        sum += (arr[i] - mean) * (arr[i] - mean);
    }
    return sqrt(sum / size);  // 표준편차는 제곱근을 구해야 함
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int a[2][3] = { {1, 2, 3}, {4, 5, 6} };
    int b[3][4] = { {3,4,5,6}, {1,2,4,3}, {4,2,6,1} };
    int c[2][4] = { 0 };  // 결과 행렬 초기화

    // 행렬 곱 계산
    for (int i = 0; i < 2; i++) {         // A의 행 개수
        for (int j = 0; j < 4; j++) {     // B의 열 개수
            for (int k = 0; k < 3; k++) { // A의 열 개수 == B의 행 개수
                c[i][j] += a[i][k] * b[k][j];
            }
        }
    }

    // 결과 행렬 출력
    printf("행렬 곱 결과:\n");
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 4; j++) {
            printf("%d ", c[i][j]);  // 각 요소 출력
        }
        printf("\n");  // 한 행이 끝나면 줄바꿈
    }
    return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define STUDENT 10
#define N 10
int main()
{
	int paper[STUDENT][N] = { {1, 3, 2, 3, 4, 2, 3, 1, 4, 3},
								{1, 2, 2, 2, 4, 2, 3, 1, 4, 2},
								{4, 3, 2, 3, 4, 2, 3, 1, 4, 2},
								{1, 3, 2, 2, 4, 3, 3, 4, 4, 2},
								{1, 3, 2, 3, 4, 2, 3, 3, 4, 1},
								{1, 1, 2, 4, 4, 3, 3, 2, 4, 3},
								{1, 3, 2, 3, 4, 2, 3, 1, 4, 2},
								{1, 3, 2, 3, 3, 2, 3, 1, 4, 2},
								{2, 3, 3, 3, 3, 2, 4, 1, 4, 2},
								{3, 4, 4, 2, 4, 1, 2, 1, 4, 2}
	};
	int answer[N] = { 1, 3, 2, 3, 4, 2, 3, 1, 4, 2 };
	int total[STUDENT] = { 0 }, id[STUDENT], rank[STUDENT];
	for (int i = 0; i < 10; i++) id[i] = i + 1;
	printf("\n\t 문항별 채점 결과 \n");
	printf("\n =================================================== \n");
	printf(" 문항");
	for (int i = 0; i < N; i++) printf("%4d", i);
	printf("   점수");
	printf("\n =================================================== \n");
	for (int i = 0; i < STUDENT; i++) {
		printf("%3d번", i+1);
		id[i] = i + 1;
		for (int j = 0; j < 10; j++) {
			if (paper[i][j] == answer[j]) {
				total[i] += 1;
				printf("%4s", "O");
			}
			else {
				printf("%4s", "X");
			}
		}
		printf("%5d점", total[i]);
		printf("\n");
	}
	printf("\n ------------------------------------------------------\n\n");
	int swap, temp;
	for (int pass = 1; pass < STUDENT; pass++) {
		swap = 0;
		for (int i = 0; i < STUDENT - pass; i++) {
			if (total[i] < total[i + 1]) {
				temp = total[i];
				total[i] = total[i + 1];
				total[i + 1] = temp;
				temp = id[i];
				id[i] = id[i + 1];
				id[i + 1] = temp;
				swap = 1;
			}
		}
		if (swap == 0) break;
	}
	// 석차 구하기
	for (int i = 0; i < STUDENT; i++) {
		rank[i] = 1;
		for (int j = 0; j < STUDENT; j++) {
			if (total[i] < total[j]) {
				rank[i]++;
			}
		}
	}
	printf("\n\t 정렬 및 석차 \n");
	printf("\t ================ \n");
	printf("\t 번호 점수 석차 \n");
	printf("\t ================ \n");
	for (int i = 0; i < STUDENT; i++) {
		printf("\t %2d번  %2d점  %2d등 \n", id[i], total[i], rank[i]);
	}
	return 0;
}

```
