# TIL: C언어 파일 입출력

## 학습 날짜: 2025년 3월 20일

## 오늘 배운 내용 정리

## 파일 입출력 함수 정리

### 1. **fprintf() 함수 - 파일에 데이터 쓰기**

- 형식: `fprintf(파일포인터, "형식 문자열", 변수);`
- 예제: 회원 5명의 나이를 입력받아 파일(`age.txt`)에 저장

```c
c
복사편집
FILE *fp = fopen("age.txt", "w");
fprintf(fp, "%d\n", age);
fclose(fp);

```

---

### 2. **fscanf() 함수 - 파일에서 데이터 읽기**

- 형식: `fscanf(파일포인터, "형식 문자열", &변수);`
- 예제: 구조체 데이터를 입력받고, 파일(`user.txt`)에 저장

```c
c
복사편집
struct person { char name[7], gender[3]; int age; };
FILE *fp = fopen("user.txt", "w");
fscanf(fp, "%s %s %d", user[i].name, user[i].gender, &user[i].age);
fclose(fp);

```

---

### 3. **파일의 끝 확인하기**

### 방법 1: `fscanf()` 반환값 활용

```c
c
복사편집
while (fscanf(fp, "%d", &age) != EOF) { printf("%d ", age); }

```

### 방법 2: `feof()` 함수 활용

```c
c
복사편집
if (feof(fp)) { printf("파일 끝\n"); fclose(fp); }

```

- 파일 끝에 도달했는지 확인 후 데이터 읽기 중단

---

### 4. **fputc() 함수 - 문자 단위 파일 쓰기**

- 형식: `fputc(문자, 파일포인터);`

```c
c
복사편집
fputc('A', fp);

```

---

### 5. **fgetc() 함수 - 문자 단위 파일 읽기**

- 형식: `fgetc(파일포인터);`

```c
c
복사편집
char ch = fgetc(fp);

```

- EOF(-1) 반환 시 파일 끝에 도달한 것

---

### 6. **파일 복사하기**

- `fgetc()`로 읽고 `fputc()`로 파일에 쓰기

```c
c
복사편집
FILE *fp1 = fopen("원본.txt", "r");
FILE *fp2 = fopen("복사본.txt", "w");
char ch;
while ((ch = fgetc(fp1)) != EOF) { fputc(ch, fp2); }
fclose(fp1);
fclose(fp2);

```

이와 같은 파일 입출력 함수를 활용하면 다양한 데이터를 파일에 저장하고 읽을 수 있음.

### 실습 문제 풀이

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#define FILE_NAME "accounts.txt"

typedef struct {
	int id;
	char name[50];
	double balance;
} Account;

void addAccounts() {
	FILE* file = fopen(FILE_NAME, "a");
	if (file == NULL) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	Account a;
	printf("계좌 ID : ");
	scanf("%d", &a.id);
	printf("계좌 소유자 이름 : ");
	scanf("%s", a.name);
	printf("초기 잔액 : ");
	scanf("%lf", &a.balance);
	fprintf(file, "%d %s %.2lf\n", a.id, a.name, a.balance);
	fclose(file);
	printf(">> 계좌가 성공적으로 개설되었습니다\n");
};
void listAccount(){
	FILE* file = fopen(FILE_NAME, "r");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	char line[100];
	printf("\n[계좌 목록]\n");
	Account acc;
	while (fscanf(file , "%d %s %lf", &acc.id, acc.name, &acc.balance) == 3) {
		printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", acc.id, acc.name, acc.balance);
	}
	fclose(file);
};
void searchAccount() {
	FILE* file = fopen(FILE_NAME, "r");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	int searchId;
	printf("검색할 계좌 ID : ");
	scanf("%d", &searchId);
	char line[100];
	int found = 0;
	Account acc;
	while (fscanf(file, "%d %s %lf", &acc.id, acc.name, &acc.balance) == 3) {
		if (acc.id == searchId) {
			printf("ID: %d | 이름: %s | 잔액: %.2lf원\n", acc.id, acc.name, acc.balance);
			found = 1;
			break;
			}
		}
	fclose(file);
	if (!found) printf("해당 ID의 계좌는 찾을 수 없습니다.");
};

void depositAccount() {
	FILE* file = fopen(FILE_NAME, "r");
	FILE* temp = fopen("temp.txt", "w");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	int found = 0, searchId;
	printf("입금할 계좌 ID : ");
	scanf("%d", &searchId);
	double deposit_amount;
	printf("입금할 금액: ");
	scanf("%lf", &deposit_amount);
	Account acc;
	while (fscanf(file, "%d %s %lf", &acc.id, acc.name, &acc.balance) == 3) {
		if (searchId == acc.id) {
			acc.balance += deposit_amount;
			found = 1;
		}
		fprintf(temp, "%d %s %.2lf\n", acc.id, acc.name, acc.balance);
	}
	fclose(file);
	fclose(temp);
	if (found) {
		remove("accounts.txt");
		rename("temp.txt", "accounts.txt");
		printf(">> %d번 계좌에 %.2lf원이 입금되었습니다.", searchId, deposit_amount);
	}
	else {
		remove("temp.txt");
	}
}

void withdrawAccount() {
	FILE* file = fopen(FILE_NAME, "r");
	FILE* temp = fopen("temp.txt", "w");
	if (!file) {
		printf("파일을 열 수 없습니다.");
		return;
	}
	int found = 0, searchId;
	printf("출금할 계좌 ID : ");
	scanf("%d", &searchId);
	double withdraw_amount;
	printf("출금할 금액: ");
	scanf("%lf", &withdraw_amount);
	Account acc;
	while (fscanf(file, "%d %s %lf", &acc.id, acc.name, &acc.balance) == 3) {
		if (searchId == acc.id) {
			if (acc.balance >= withdraw_amount) {
				acc.balance -= withdraw_amount;
				found = 1;
			}
			else {
				printf("계좌 잔액이 충분하지 않습니다. \n");
				fclose(file);
			}
		}
		fprintf(temp, "%d %s %.2lf\n", acc.id, acc.name, acc.balance);
	}
	fclose(file);
	fclose(temp);
	if (found) {
		remove("accounts.txt");
		rename("temp.txt", "accounts.txt");
		printf(">> %d번 계좌에 %.2lf원이 출금되었습니다.", searchId, withdraw_amount);
	}
	else {
		remove("temp.txt");
	}
}

void transferAccount() {
	FILE* file = fopen(FILE_NAME, "r");
	FILE* temp = fopen("temp.txt", "w");
	if (!file) {
		printf("파일을 열 수 없습니다.\n");
		return;
	}
	if (!temp) {
		printf("임시 파일을 열 수 없습니다.\n");
		fclose(file);
		return;
	}

	int withdrawFound = 0, depositFound = 0;
	int depositId, withdrawId;
	double amount;

	printf("송금할 계좌 ID: ");
	scanf("%d", &depositId);
	printf("수취할 계좌 ID: ");
	scanf("%d", &withdrawId);

	if (depositId == withdrawId) {
		printf("송금, 수취 계좌가 동일합니다.\n");
		fclose(file);
		fclose(temp);
		remove("temp.txt");
		return;
	}

	printf("이체 금액: ");
	scanf("%lf", &amount);

	Account acc;
	while (fscanf(file, "%d %s %lf", &acc.id, acc.name, &acc.balance) == 3) {
		if (withdrawId == acc.id) {
			if (acc.balance < amount) {
				printf("이체 실패! 잔액 부족\n");
				fclose(file);
				fclose(temp);
				remove("temp.txt");
				return;
			}
			acc.balance -= amount;
			withdrawFound = 1;
		}
		if (depositId == acc.id) {
			acc.balance += amount;
			depositFound = 1;
		}
		fprintf(temp, "%d %s %.2lf\n", acc.id, acc.name, acc.balance);
	}

	fclose(file);
	fclose(temp);

	if (depositFound && withdrawFound) {
		remove(FILE_NAME);
		rename("temp.txt", FILE_NAME);
		printf(">> %d번 계좌에서 %d번 계좌로 %.2lf원이 송금되었습니다.\n", withdrawId, depositId, amount);
	}
	else {
		remove("temp.txt");
		printf("이체 실패! 계좌 정보를 확인하세요.\n");
	}
}
int main() {
	char choice;
	while (1) {
		printf("\n[고객 정보 시스템]\n");
		printf("1. 신규 계좌 개설 (N)\n");
		printf("2. 고객 목록 출력 (L)\n");
		printf("3. 계좌 검색 (S)\n");
		printf("4. 입금 (D)\n");
		printf("5. 출금 (W)\n");
		printf("6. 계좌 이체 (T)\n");
		printf("7. 종료 (Q)\n");
		printf("선택: ");
		scanf(" %c", &choice);

		switch (choice) {
		case 'N': case 'n': addAccounts(); break;
		case 'L': case 'l': listAccount(); break;
		case 'S': case 's': searchAccount(); break;
		case 'D': case 'd': depositAccount(); break;
		case 'W': case 'w': withdrawAccount(); break;
		case 'T': case 't': transferAccount(); break;
		case 'Q': case 'q': return 1;
		}
	}
	return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define FILE_NAME "transactions.dat"
#define CSV_FILE "transactions.csv"

// 거래 내역 구조체
typedef struct {
    int customerID;       // 고객 ID
    char stockCode[10];   // 종목 코드
    char type;           // 거래 유형 ('B' = 매수, 'S' = 매도)
    int quantity;        // 거래 수량
    double price;        // 체결가
} Transaction;

// 거래 내역 추가 함수
void addTransaction() {
    FILE* file = fopen(FILE_NAME, "ab"); // 이진 파일 append 모드
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return;
    }

    Transaction t;
    printf("고객 ID: ");
    scanf("%d", &t.customerID);
    printf("종목 코드: ");
    scanf("%s", t.stockCode);
    printf("거래 유형 (B = 매수, S = 매도): ");
    scanf(" %c", &t.type);
    printf("거래 수량: ");
    scanf("%d", &t.quantity);
    printf("체결가: ");
    scanf("%lf", &t.price);

    fwrite(&t, sizeof(Transaction), 1, file);
    fclose(file);
    printf("거래 내역이 저장되었습니다.\n");
}

// 특정 고객 거래 내역 조회 함수
void viewTransactions() {
    FILE* file = fopen(FILE_NAME, "rb");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return;
    }

    int searchID;
    printf("조회할 고객 ID 입력: ");
    scanf("%d", &searchID);

    Transaction t;
    int found = 0;
    printf("\n-------------------거래 내역-------------------\n");
    while (fread(&t, sizeof(Transaction), 1, file)) {
        if (t.customerID == searchID) {
            printf("고객 ID: %d, 종목 코드: %s, 유형: %c, 수량: %d, 체결가: %.2lf\n",
                t.customerID, t.stockCode, t.type, t.quantity, t.price);
            found = 1;
        }
    }
    if (!found) printf("거래 내역이 없습니다.\n");
    fclose(file);
}

// 거래 내역을 CSV 파일로 변환하는 함수
void exportToCSV() {
    FILE* file = fopen(FILE_NAME, "rb");
    FILE* csvFile = fopen(CSV_FILE, "w");
    if (!file || !csvFile) {
        printf("파일을 열 수 없습니다.\n");
        return;
    }

    fprintf(csvFile, "고객ID,종목코드,거래유형,수량,체결가\n");
    Transaction t;
    while (fread(&t, sizeof(Transaction), 1, file)) {
        fprintf(csvFile, "%d,%s,%c,%d,%.2lf\n", t.customerID, t.stockCode, t.type, t.quantity, t.price);
    }

    fclose(file);
    fclose(csvFile);
    printf("거래 내역이 CSV 파일로 저장되었습니다.\n");
}

// 메뉴 함수
void menu() {
    int choice;
    do {
        printf("\n===== 증권 거래 시스템 =====\n");
        printf("1. 거래 내역 추가\n");
        printf("2. 특정 고객 거래 내역 조회\n");
        printf("3. 거래 내역 CSV 변환\n");
        printf("4. 종료\n");
        printf("선택: ");
        scanf("%d", &choice);

        switch (choice) {
        case 1:
            addTransaction();
            break;
        case 2:
            viewTransactions();
            break;
        case 3:
            exportToCSV();
            break;
        case 4:
            printf("프로그램을 종료합니다.\n");
            break;
        default:
            printf("잘못된 입력입니다. 다시 선택하세요.\n");
        }
    } while (choice != 4);
}

int main() {
    menu();
    return 0;
}

```
