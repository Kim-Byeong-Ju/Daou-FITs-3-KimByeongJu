# TIL: C언어 DB 연동

## 학습 날짜: 2025년 3월 21일

## 오늘 배운 내용 정리

### 1. C언어에서 DB 연결하는 방법

C언어에서 데이터베이스(DB)와 연결하기 위해 Oracle의 OCI(Oracle Call Interface)를 사용했다. OCI는 Oracle 데이터베이스와 연결하고 SQL을 실행할 수 있는 API 집합이다. 이를 통해 C언어로 Oracle 데이터베이스에 접근하여 다양한 작업을 수행할 수 있다.

### 2. DB 연결을 위한 환경 설정

1. **OCI 환경 초기화**: OCI 환경을 초기화하기 위해 `OCIEnvCreate` 함수를 사용한다. 이 함수는 OCI 환경을 설정하고, DB와 상호작용을 위한 기본 구조체들을 생성한다.
2. **에러 핸들러 생성**: `OCIHandleAlloc` 함수를 사용하여 `OCIError` 타입의 핸들러를 생성한다. 이는 DB 작업 중 발생할 수 있는 오류를 처리하기 위해 사용된다.
3. **서비스 컨텍스트 생성**: `OCISvcCtx`는 DB 서비스와의 연결을 관리하는 핸들러다. 이를 생성하고 `OCILogon` 함수를 통해 사용자 인증을 진행하여 DB에 접속한다.

```c
c
복사편집
OCIEnv *env;
OCIError *err;
OCISvcCtx *svc;
OCIHandleAlloc(env, (dvoid**)&err, OCI_HTYPE_ERROR, 0, NULL);
OCIHandleAlloc(env, (dvoid**)&svc, OCI_HTYPE_SVCCTX, 0, NULL);
OCILogon(env, err, &svc, username, strlen((char*)username), password, strlen((char*)password), dbname, strlen((char*)dbname));

```

### 3. SQL 구문 준비 및 실행

1. **SQL 구문 준비**: `OCIStmtPrepare` 함수를 사용하여 SQL 구문을 준비한다. 이 함수는 SQL 문을 파싱하고 준비하는 역할을 한다.
2. **바인딩**: SQL 구문에서 사용될 파라미터들을 바인딩하기 위해 `OCIBindByName` 함수를 사용한다. 이를 통해 SQL 구문에서 특정 파라미터에 값을 할당할 수 있다.
3. **SQL 실행**: `OCIStmtExecute` 함수를 사용하여 준비된 SQL 구문을 실행한다. 실행 결과는 `OCI_SUCCESS` 상태일 경우 성공적으로 수행되었음을 의미한다.

```c
c
복사편집
OCIStmtPrepare(stmt, err, (text*)insert_sql, strlen(insert_sql), OCI_NTV_SYNTAX, OCI_DEFAULT);
OCIBindByName(stmt, &bind, err, (text*)":id", -1, (dvoid*)&customer_id, sizeof(customer_id), SQLT_INT, NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
OCIStmtExecute(svc, stmt, err, 1, 0, NULL, NULL, OCI_DEFAULT);

```

### 4. CRUD 예제

1. **CREATE (데이터 삽입)**: `INSERT INTO customers ...` 구문을 사용하여 새로운 고객 데이터를 삽입한다. 삽입할 값을 바인딩하고, `OCIStmtExecute`로 실행한다.
2. **READ (데이터 조회)**: `SELECT * FROM customers WHERE customer_id = :id` 구문을 사용하여 특정 고객 정보를 조회한다. 조회된 데이터는 `OCIStmtFetch`를 통해 가져온다.
3. **UPDATE (데이터 수정)**: `UPDATE customers SET name = :name, phone_number = :phone, email = :email WHERE customer_id = :id` 구문을 사용하여 고객 정보를 수정한다. 수정할 필드는 바인딩된 변수들로 업데이트할 값을 지정한다.
4. **DELETE (데이터 삭제)**: `DELETE FROM customers WHERE customer_id = :id` 구문을 사용하여 특정 고객 데이터를 삭제한다.

### 5. 주요 함수 설명

- **OCIEnvCreate**: OCI 환경을 초기화한다.
- **OCIHandleAlloc**: OCI 객체를 할당한다.
- **OCILogon**: DB 서버에 로그인한다.
- **OCIStmtPrepare**: SQL 구문을 준비한다.
- **OCIBindByName**: SQL 구문 내 파라미터를 바인딩한다.
- **OCIStmtExecute**: SQL 구문을 실행한다.
- **OCIStmtFetch**: SQL 조회 결과를 가져온다.
- **OCILogoff**: DB 서버와의 연결을 종료한다.

### 6. 예외 처리

OCI 함수들이 반환하는 오류 코드에 따라 적절히 예외를 처리하는 것이 중요하다. `OCIErrorGet` 함수를 사용하여 오류 메시지를 출력하고, 필요한 경우 프로그램을 종료할 수 있다.

```c
c
복사편집
OCIErrorGet(err, 1, NULL, &errcode, errbuf, sizeof(errbuf), OCI_HTYPE_ERROR);
printf("OCI Error: %s\n", errbuf);

```
