# TIL: Javascript(JQeury)

## 학습 날짜: 2025년 2월 19일

## 오늘 배운 내용 정리

### **jQuery**

> `jQuery`는 모든 브라우저에서 동작하는 클라이언트 자바스크립트 라이브러리
> 

**사용 목적**

- DOM과 관련된 처리 쉽게 구현
- 일관된 이벤트 연결 쉽게 구현
- 시각적 효과 쉽게 구현
- Ajax 애플리케이션 쉽게 개발

**jQuery Selector**

- `전체 선택자`
    - ‘*’ 을 활용
    - 가장 기본적인 선택자
    - 하위에 있는 모든 element를 선택
    - ex) 모든 문서 객체의 color 스타일 속성을 ‘red’로 변경
        
        ```java
        function  myFunc() {
            $('*').css("color", "blue");
        }
        ```
        
- `타입 선택자`
    - 원하는 tag 명을 이용해서 선택
    - 하나 이상의 태그 선택자를 동시에 사용하고 싶을 때는 콤마( `,` )로 선택자 구분
    - ex) h1, div 태그에 대해서 color 스타일 속성 ‘red’로 변경
        
        ```java
        function  myFunc() {
            $('h1, div').css("color", "blue");
        }
        ```
        
- `아이디 선택자`
    - HTML 페이지 내에 존재하는 id속성의 값은 각각 다르다. ( unique )
    - id 속성의 값을 이용하여 문서 객체를 선택하는 선택자
    - 유일한 문서객체를 선택하게 되며 속도 면에서 가장 빠르고 편하다.
    - ex) id가 pusan인 문서 객체를 삭제
        
        ```java
        function  myFunc() {
            $("#pusan").remove();
        }
        ```
        
- `클래스 선택자`
    - 원하는 class를 가진 element를 선택
    - unique 하지 않음
    - ex) myClass 문서 객체들의 배경색 변경
        
        ```java
        
        function  myFunc() {
        	  $('.myClass').css('background', 'blue');
            
        }
        ```
        
- `자식 선택자와 후손 선택자`
    - 기본 선택자의 뒤에 붙여 사용하며 기본 선택자의 범위 제한
    - 자식 선택자는 `>` 기호로 표시
    - 후손 선택자는 `' '` 공백으로 표시
    
    ```java
    // 자식 선택자
    function  myFunc() {
        $("ol > li").remove();
    }
    
    // 후손 선택자
    function myFunc() {
    		$("div li").remove();0
    }
    
    ```
    
- `동위 선택자`
    - ‘+’ 는 바로 다음에 나오는 형제 1개
    - ‘~’ 는 바로 다음에 나오는 형제 포함 싹 다
    - element A + element B : element A 바로 다음에 위치하는 element B를 선택
    - element A ~ element B : element A 다음에 위치하는 모든 element B를 선택
    
    ```java
    
    function  myFunc() {
        alert($("#kang + li").text());
        alert($("#kang ~ li").text());
    }
    ```
    
- `속성 선택자`
    - element[attribute] : 특정 속성을 가지고 있는 문서객체 선택
    - element[attribute=value] : 속성값이 value값과 같은 문서객체 선택
    - element[attribute~=value] : 속성값이 value값을 단어로서 포함하는 문서객체 선택
    - element[attribute^=value] : 속성값이 value값으로 시작하는 문서객체 선택
    - element[attribute$=value] : 속성값이 value값으로 끝나는 문서객체 선택
    - element[attribute*=value] : 속성값이 value값을 포함하는 문서객체 선택

**jQuery Method**

- each() method
    - 선택된 각각의 element를 반복 처리하기 위한 method
    - 선택된 하나씩 특수한 작업을 처리
- $.each(object, function(index,item) { } )
- $(selector).each(function(index,item) { } )
    
    ```java
    function myFunc() {
        // 1. checkbox를 찾아요!
        // $("#region").val($("[type=checkbox]:checked + span").text())
        // check 된 지역을 찾아서 console에 다음과 같이 출력할
        $("[type=checkbox]:checked + span").each(function (idx, item) {
            // index는 0, 1, 2, ... for 문의 첨자 역할
            // item : 선택된 문서객체(Document Object). jQuery 객체가 아님
            // jQuery 객체로 바꿔서 사용
            console.log($(item).text() + "이/가 선택되었어요!");
        })
    }
    ```
    
- css() method
- addClass() method
- attr() method
- removeAttr() method
- html() method
- text() method
- remove()
- empty() method
- $() : 새로운 element 생성
- $(A).append(B), $(A).prepend(B)
    - append는 제일 마지막 자식으로 추가
    - prepend는 제일 첫 자식으로 추가
- $(A).after(B), $(A).before(B)
    - after는 바로 다음 형제로 붙임
    - before는 바로 앞 형제로 붙임
- clone() method
