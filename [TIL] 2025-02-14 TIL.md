# TIL: Java Servlet

## 학습 날짜: 2025년 2월 14일

## 오늘 배운 내용 정리

> **Servlet**
> 

Servlet(서블릿)은 **Java를 사용하여 웹 애플리케이션을 개발할 때 서버 측에서 실행되는 프로그램**. 클라이언트(웹 브라우저)의 요청을 처리하고, 동적인 웹 페이지를 생성하는 역할을 한다.

Servlet은 `javax.servlet.http.HttpServlet` 클래스를 상속하여 구현하며, HTTP 요청(`GET`, `POST` 등)에 따라 적절한 응답을 생성.

---

**1. Servlet의 특징**

- **서버 측에서 실행** → 클라이언트 요청을 받아 동적으로 응답을 생성
- **HTTP 기반** → `HttpServlet`을 상속하여 `doGet()`, `doPost()` 등을 재정의
- **멀티스레드 처리** → 하나의 서블릿 인스턴스를 여러 요청이 공유하여 성능 향상
- **JSP와 상호 운용 가능** → JSP는 서블릿으로 변환되어 실행됨

---

**2. Servlet의 동작 과정**

**클라이언트 요청**

- 사용자가 웹 브라우저에서 `http://localhost:8080/myapp/myservlet`을 요청

**서블릿 컨테이너(Tomcat 등)가 요청을 받음**

- 서블릿 컨테이너는 요청을 분석하고 해당 서블릿을 실행

**서블릿 실행 및 응답 생성**

- `doGet()` 또는 `doPost()` 실행 후 HTML 또는 JSON 등의 응답을 생성

**클라이언트에 응답 전송**

- 생성된 응답을 HTTP를 통해 클라이언트로 전달

---

**3. Servlet의 주요 메서드**

**① 생명주기 관련 메서드**

| 메서드 | 설명 |
| --- | --- |
| `init()` | 서블릿이 처음 로드될 때 한 번만 실행 (초기화 작업 수행) |
| `service()` | 모든 요청을 처리하는 메서드 (HTTP 요청 방식에 따라 `doGet()`, `doPost()` 호출) |
| `destroy()` | 서버가 종료되거나 서블릿이 제거될 때 한 번 실행 |

**② 요청 처리 메서드**

| 메서드 | 설명 |
| --- | --- |
| `doGet(HttpServletRequest req, HttpServletResponse resp)` | GET 요청을 처리 |
| `doPost(HttpServletRequest req, HttpServletResponse resp)` | POST 요청을 처리 |
| `doPut()`, `doDelete()` | RESTful API 개발 시 사용 |

---

**4. Servlet 코드 예제**

```java
java
복사편집
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;

@WebServlet("/hello")  // URL 매핑
public class HelloServlet extends HttpServlet {

    @Override
    public void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html");  // 응답 타입 설정
        PrintWriter out = resp.getWriter();
        out.println("<html><body>");
        out.println("<h1>Hello, Servlet!</h1>");
        out.println("</body></html>");
        out.close();
    }
}

```

- 위 코드는 `http://localhost:8080/hello`로 GET 요청을 받으면 `"Hello, Servlet!"`을 HTML로 출력

---

### **5. Servlet과 JSP의 차이**

| 구분 | Servlet | JSP |
| --- | --- | --- |
| 위치 | 서버 사이드(Java 코드) | 서버에서 실행되지만 HTML 중심 |
| 코드 방식 | Java 코드로 HTML을 생성 | HTML에 Java 코드를 포함 |
| 주 사용처 | 백엔드 비즈니스 로직 | 프론트엔드 뷰 생성 |
- 실무에서는 JSP보다는 Servlet + JSP 또는 Spring Framework(MVC 패턴)를 많이 사용

> **기본 개념 용어**
> 

**Internet**

![image.png](attachment:9d59c711-026b-42f2-8dc9-52b0100189b0:image.png)

- LAN(Local Area Network)들의 집합체. 전세계적인 네트워크 망
- 데이터 통신을 하려면 주소를 이용해야 한다(논리적 주소인 IP Address)
    - NIC(Network Interface Card)에 부여됨
- 실제로는 물리적인 주소(MAC address)
- 어떤 논리적 주소와 물리적 주소가 연결되는지 식별
- IP address는 컴퓨터를 구분하기 위한 숫자. 숫자 대신 문자로 표현하는 것이 DNS

**PORT**

- IP 주소는 하나의 컴퓨터에 할당된 번호
- 이 컴퓨터에서 network를 사용하는 여러 개의 프로그램이 동시에 실행되고 있다고 가정하면 이 때, IP 주소 만을 가지고는 이 컴퓨터의 특정 network 프로그램에 접근할 수 없다.
- 이런 상황에서 해당 프로그램들을 구분할 수 있게 해 주는 것이 바로 port 번호

**HyperText**

- `HyperText`(하이퍼텍스트)는 사용자의 선택에 따라 관련된 정보 쪽으로 옮겨갈 수 있도록 조직화된 정보를 의미. 이러한 관련 정보쪽으로 옮겨 갈수록 도와주는 것이 바로 링크, 또는 하이퍼링크
- 웹이란 결국은 많은 수의 하이퍼텍스트 링크에 의해 거대한 양의 정보가 서로 연결되어 있는 것

**World Wide Web(WWW, W3)**

- 인터넷에 연결된 컴퓨터들을 통해 사람들이 정보를 공유할 수 있는 전세계적인 정보 공간을 의미하며 간단히 웹(Web)이라고 부른다.
- 하이퍼텍스트는 웹 브라우저라 불리는 프로그램을 통해 웹 서버에서 문서나 웹 페이지 등의 정보 조각을 읽어 들여 출력하는 형태로 보이게 된다. 그러고 나서 사용자는 각 페이지에 있는 하이퍼링크를 따라 다른 문서로 이동하거나, 그 페이지를 서비스하고 있는 서버로 일련의 정보를 보낼 수도 있다.
- 하이퍼링크를 따라 이동하는 행위를 흔히 웹 서핑 혹은 웹 브라우징이라 한다. 그리고 관련된 내용들이 모여있는 웹 페이지들의 집합을 웹 사이트라 한다.

**HTTP**

- `HyperText Transfer Protocol`의 약자로 `Web`에서 정보를 주고 받기 위해 사용하는 프로토콜
- 주로 HTML 문서를 주고 받는 데 사용하며, `Client-Server` 구조를 이용하고 서버 프로세스의 기본 포트번호는 80번을 사용
- 클라이언트와 서버 사이에 이루어지는 요청/응답(`request`/`response`) 프로토콜
- HTTP 프로토콜에서는 클라이언트의 데이터 요청에 대한 응답 후 바로 연결을 해제 합니다. 지속적인 연결을 하고 있으면서 데이터를 주고 받는 것이 아니라 필요할 때마다 연결하고 데이터를 받자마자 바로 연결을 끊어버리는 방식이 바로 HTTP 프로토콜의 연결 메커니즘. 이런 프로토콜을 `stateless protocol`이라고 부르는데 장점은 많은 사용자가 동시에 서버 접속이 가능해진다는 점이며 단점은 각 사용자의 행위를 `tracking`하기 힘들다.

**Web Browser(client)**

- 웹 브라우저는 웹 서버로부터 받은 HTML 문서나 파일과 연동하고 출력하는 응용 소프트웨어
- 개인용 컴퓨터에서 주로 쓰는 웹 브라우저에는 MS의 인터넷 익스플로러(Edge), 모질라 파이어폭스, 오페라사의 오페라, 애플의 사파리, 구글의 크롬 등이 있다.
- 웹 브라우저는 웹 서버로부터 웹 페이지를 가져오기 위해 HTTP 프로토콜을 이용하고 웹 페이지를 가져올 뿐 아니라 웹 서버에 정보를 송신하기도 한다.

**Web Server**

- 웹 브라우저(web client)로부터 HTTP 요청을 받아들이고, HTML 문서와 같은 웹 페이지를 전송해주는 서비스 프로그램을 지칭
- 또한 위의 기능을 제공하는 컴퓨터 프로그램을 실행하는 컴퓨터(하드웨어)를 웹 서버라고 부르기도 한다.
- 일반적으로 정적 리소스에 대한 요청을 수행
- WAS(Web Application Server): HTTP 프로토콜을 통해 사용자 컴퓨터나 장치에 어플리케이션을 수행해주는 미들웨어, 주로 동적 서버 컨텐츠를 수행하는 것

**HTTP Client-Server 구조**

![image.png](attachment:dd4722a8-bfb9-40af-8a5b-fa350dcab6b6:image.png)

**HTTP Request**

![image.png](attachment:af614aed-c525-4371-9757-401abb97626f:image.png)

 **HTTP Response**

![image.png](attachment:9ef6ee13-3853-4b54-ac15-3f4e6deaaca2:image.png)
