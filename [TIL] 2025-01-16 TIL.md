# TIL: SQL 실습

## 학습 날짜: 2025년 1월 16일

## 학습 중 어려움을 느낀 부분

- 오라클 JOIN
    
    ```sql
    SELECT *
    FROM EMPLOYEES E, DEPARTMENTS D
    WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID(+)
    AND D.LOCATION = 'New York';
    ```
    
    - 직원 테이블(EMPLOYEES)을 기준으로 부서 테이블(DEPARTMENTS) OUTER JOIN 한 코드
    - (+) 가 붙은 테이블을 옆에 JOIN 시킨다는 의미로 해석하면 된다. 즉, 반대 테이블이 기준 테이블이 되는 것이다!
    
- 복습하면서 깨달은 내용
    - OUTER JOIN **기준 테이블**
        - 1:N 관계에서 1에 해당하는 테이블이 기준 테이블이 된다
        - ex) 고객 테이블(1)과 주문 테이블(N)을 JOIN 하려고 할 때, 주문 테이블에는 있지만 고객 테이블에는 없는 고객ID는 존재하지 않는 것이 원칙이다.
            - 설령, 존재한다면 이는 데이터의 무결성에 위반한 사례이다.

- NVL 자료형 통일
    - NVL로 NULL값을 처리할 때, 자료형을 통일시켜야 에러 없이 결과를 출력할 수 있다.
    - EX: `NVL(TO_CHAR(SALARY), ‘N/A’)`

- TRUNC(버림), FLOOR(내림) 차이
    - 양수일 때는 같으나, 음수일 때는 다른 결과를 출력함
    - -55.4 → TRUNC(-55.4) = -55 / FLOOR(-55.4) = -56

- 

## 오늘 배운 내용 정리

- NATURAL JOIN에서는 ALIAS를 붙이면 에러가 발생한다

- 실습 문제

```sql
-- 1번
SELECT NAME AS EMPLOYEE_NAME, LOCATION
FROM EMPLOYEES E
LEFT JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

-- 2번
SELECT E1.NAME AS EMPLOYEE_NAME, E2.NAME AS MANAGER_NAME
FROM EMPLOYEES E1
LEFT JOIN EMPLOYEES E2
ON E1.MANAGER_ID = E2.EMPLOYEE_ID;

-- 3번
SELECT NAME, NVL(TO_CHAR(SALARY), 'N/A') AS SALARY
FROM EMPLOYEES;

-- 4번
SELECT NAME, SALARY
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID AND LOCATION = 'Chicago';

-- 5번
SELECT E.NAME, SALARY, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID AND SALARY BETWEEN 5000 AND 10000;

-- 6번
SELECT NAME, HIRE_DATE
FROM EMPLOYEES
WHERE HIRE_DATE >= SYSDATE - (INTERVAL '3' MONTH);

-- 7번
SELECT E.NAME, DEPARTMENT_NAME
FROM DEPARTMENTS D
JOIN EMPLOYEES E
ON D.MANAGER_ID = E.EMPLOYEE_ID;

-- 8번
SELECT JOB_ID, COUNT(JOB_ID) AS COUNT_JOB_ID
FROM EMPLOYEES
GROUP BY JOB_ID;

-- 9번
SELECT DEPARTMENT_NAME, CEIL(AVG(SALARY)) AS AVG_SALARY
FROM DEPARTMENTS D, EMPLOYEES E
WHERE D.DEPARTMENT_ID = E.DEPARTMENT_ID
GROUP BY DEPARTMENT_NAME;

-- 10번
SELECT SUM(SALARY) AS TOTAL_SALARY
FROM EMPLOYEES;

-- 11번
SELECT AMOUNT, NAME
FROM LOANS L, CUSTOMERS C
WHERE L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY AMOUNT DESC;

-- 12번
SELECT STATUS, COUNT(CUSTOMER_ID) COUNT_CUSTOMER
FROM LOANS
GROUP BY STATUS;

-- 13번
SELECT DEPARTMENT_ID, COUNT(EMPLOYEE_ID) AS EMPLOYEE_COUNT
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY EMPLOYEE_COUNT ASC
FETCH FIRST 1 ROWS ONLY;
--HAVING COUNT(EMPLOYEE_ID) = (SELECT MIN(COUNT(EMPLOYEE_ID)) FROM EMPLOYEES WHERE DEPARTMENT_ID IS NOT NULL GROUP BY DEPARTMENT_ID);

-- 14번
SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') MONTHS, COUNT(CUSTOMER_ID) NEW_CUSTOMERS
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY MONTHS DESC;

-- 15번
SELECT SC1.CATEGORY_NAME CHILD_CATEGORY, SC2.CATEGORY_NAME PARENT_CATEGORY
FROM SHOPPING_CATEGORIES SC1
LEFT JOIN SHOPPING_CATEGORIES SC2
ON SC1.PARENT_CATEGORY_ID = SC2.CATEGORY_ID;

-- 16번
SELECT B.NAME, TRUNC(SUM(L.AMOUNT)) 대출총합
FROM LOANS L, BRANCHES B
WHERE L.BRANCH_ID = B.BRANCH_ID AND B.NAME = 'Branch 8'
GROUP BY B.NAME;

-- 17번
SELECT JOB_ID,
        ROUND(AVG(SALARY)) AVG_SALARY, 
        CASE
        WHEN AVG(SALARY) >= 8000 THEN 'HIGH'
        WHEN AVG(SALARY) >= 6000 THEN 'MEDIUM'
        ELSE 'LOW' 
        END AS RATE
FROM EMPLOYEES
GROUP BY JOB_ID
HAVING AVG(SALARY) >= 5000
ORDER BY AVG_SALARY DESC;

-- 18번
SELECT C.NAME, SUM(T.AMOUNT)
FROM TRANSACTIONS T
JOIN ACCOUNTS A
ON T.ACCOUNT_ID = A.ACCOUNT_ID
LEFT JOIN CUSTOMERS C
ON C.CUSTOMER_ID = A.CUSTOMER_ID
GROUP BY C.NAME
ORDER BY C.NAME;

-- 19번
SELECT D.DEPARTMENT_NAME, ROUND(AVG(SYSDATE-E.HIRE_DATE)) AVG_WORKING_DAYS
FROM EMPLOYEES E
JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME
ORDER BY D.DEPARTMENT_NAME;

-- 20번
SELECT *
FROM (
    SELECT NAME, AMOUNT
    FROM CUSTOMERS C, LOANS L
    WHERE C.CUSTOMER_ID = L.CUSTOMER_ID
    ORDER BY AMOUNT DESC, NAME
    )
WHERE ROWNUM <= 5;
```
