# TIL: Java 주요 class

## 학습 날짜: 2025년 2월 7일

## 학습 중 어려움을 느낀 부분

**`동적 바인딩`** 

<aside>

- 인스턴스를 생성할 때, type을 상위클래스로 한다면 필드, 메서드는 type을 따라간다. 그러나, override된 메서드가 있을 경우에는 최후의 메서드를 따라간다!
- 상위클래스의 메서드를 사용하고 싶으면, super 키워드를 사용하면 된다.
</aside>

## 오늘 배운 내용 정리

> **String**
> 
- Heap에 String Pool 영역이 존재
- “”을 이용해서 문자열 객체를 생성하면 String Pool에 들어간다.
- 이 때, String pool에 문자열이 하나 생성되면 해당 문자열은 불변의 속성을 가져서 하나의 주소값을 가진다.
- `equals` 메서드는 메모리 주소가 아닌 **값을 비교한다**.

```java
package inheritance;

public class StringTest {
  public static void main(String[] args) {
      String name = "홍길동";
      // 객체가 생성되는 heap 영역에 String Pool 이라는 영역이 존재
      // "" 이걸 이용해서 문자열 객체를 생성하면 String Pool에 들어간다
      // 만약 new keyword와 생성자를 이용해서 문자열 객체를 생성하면 일반 Heap 영역에 들어간다
      // String은 immutable(불변성) 특성이 있다.
      String test = "홍길동";
      String obj = new String("홍길동");
      System.out.println(name == test); // true
      System.out.println(obj == test);  // false
      System.out.println(test.equals(obj)); // true
  }
}

```

> **Array**
> 
- 배열은 같은 type의 data 여러 개를 저장할 수 있는 자료구조
- 배열을 사용하기 위해서는 배열을 의미하는 기호 ‘[]’ 사용
- 배열은 객체이다
- 배열을 만들기 위해서 new keyword 사용

```java
class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		int arr1[] = new int[3];
		Student arr2[] = new Student[3];
	}

}
```

- **Array 계열 자료 구조 종류**

| List 계열 | Map 계열 | Set 계열 |
| --- | --- | --- |
| ArrayList class | HashMap class | HashSet class |

> **ArrayList**
> 
- 대부분은 배열과 유사한 특징
- 배열과의 차이
    - **크기가 유동적이다.**
    - **다른 data type을 저장할 수 있다.** 하지만, 일반적으로 generic을 사용하여 같은 data type을 저장
    - **Instance만 저장할 수 있다.**
    

```java
package collectiontest;

import java.util.ArrayList;

public class ArrayListTest {

    public static void main(String[] args) {
        // 배열
        String[] stringArray = new String[5];

        // ArrayList
        ArrayList list = new ArrayList();

        // 객체이기 때문에 당연히 method를 통해 이 객체를 제어
        list.add("홍길동"); // String 객체를 첫번째 칸에 저장
        list.add("Java");
        list.add(100); // primitive type은 list에 저장할 수 없다.
                       // 원래는 안되는데 autoboxing이 발생. Integer 객체가 만들어져서 list에 저장
                       // list.add(new Integer(100));
        list.add(true);
        for (Object o : list) {
            System.out.println(o);
        }
        System.out.println(list);
    }
}

```

> **Map**
> 
- 집합형 자료구조
- HashMap 활용
- key, value 쌍으로 저장
- key, value 둘 다 객체만 사용
- 순서 개념이 없다

```java
package collectiontest;

import java.util.Map;
import java.util.HashMap;

public class HashMapTest {
    public static void main(String[] args) {
        Map<Integer, String> map = new HashMap<Integer, String>();

        map.put(1, "홍길동");
        map.put(2, "신사임당");
        
        System.out.println(map.get(2));
    }
}

```

> **Set**
> 
- HashSet 활용
- 중복 제거

```java
package collectiontest;

import java.util.Iterator;
import java.util.Set;
import java.util.HashSet;
public class HashSetTest {
    public static void main(String[] args) {

        Set<String> set = new HashSet<String>();

        set.add("홍길동");
        set.add("신사임당");
        set.add("강감찬");
        set.add("홍길동"); // 추가되지 않는다.

        Iterator<String> iter = set.iterator();
        while (iter.hasNext()) {
            System.out.println(iter.next());
        }
    }
}

```

> Exception
> 
- Java에서는 Exception class를 제공한다.
- 프로그램이 비정상 종료가 되는걸 방지하는 기능
- 특정 상황이 발생하면 JVM이 Exception에 관련된 세부 정보를 가지고 있는 객체 생성
- Exception객체가 처리되지 않으면 program은 비정상 종료
- Java에서 Exception을 처리하는 유일한 방법은 try ~ catch 구문

![image (11)](https://github.com/user-attachments/assets/3c072874-f4bd-4f45-95ae-99cf332b3e25)

```java
package collectiontest;

public class ExceptionTest {

    public static void main(String[] args) {
        System.out.println("Exception 발생");

        Object obj = null;
        try {
            System.out.println(obj.toString());
        } catch (NullPointerException e) {
            System.out.println("객체가 null 입니다.");
        }
        
        try {
            int result = 10 / 0; // ArithmeticException
        } catch (ArithmeticException e) {
            System.out.println("0으로 나눌 수 없습니다");
        }
    }
}

```
