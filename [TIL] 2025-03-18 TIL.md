# TIL: 구조체-공용체-열거형 응용, 메모리 구조 동적 할당

## 학습 날짜: 2025년 3월 18일

## 오늘 배운 내용 정리

### 구조체, 공용체, 열거형 응용

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef enum {
	DEPOSIT, // 정기 예금
	LOAN, // 대출
	STOCK // 주식
} ProductType;

typedef union {
	struct { double interest_rate; int term_months; } deposit;
	struct { double loan_amount; double interest_rate; } loan;
	struct { char ticker[10]; int shares; double price; } stock;
} ProductData;

typedef struct {
	ProductType type;
	ProductData data;
} FinancialProduct;

int main() {
	FinancialProduct depositProduct;
	depositProduct.type = DEPOSIT;
	depositProduct.data.deposit.interest_rate = 0.035;
	depositProduct.data.deposit.term_months = 12;

	FinancialProduct loanProduct;
	loanProduct.type = LOAN;
	loanProduct.data.loan.loan_amount = 500000000;
	loanProduct.data.loan.interest_rate = 0.05;

	FinancialProduct stockProduct;
	stockProduct.type = STOCK;
	sprintf(stockProduct.data.stock.ticker, "AAPL");
	stockProduct.data.stock.shares = 10;
	stockProduct.data.stock.price = 175.50;

	if (depositProduct.type == DEPOSIT) {
		printf("[정기 예금 정보]\n");
		printf("예금 금리 : %.2lf%% | 기간 : %d개월",
			depositProduct.data.deposit.interest_rate*100,
			depositProduct.data.deposit.term_months);
	}
	return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef enum {
	CASH,
	CARD,
	CRYPTO
} TransactionType;
typedef union {
	struct { int amount; } cash;
	struct { char number[20]; int installment; } card;
	struct { char address[50]; double amount; } crypto;
} TransactionData;
typedef struct {
	TransactionType type;
	TransactionData data;
} Transaction;
 
int main() {
	Transaction cashTransaction;
	cashTransaction.type = CASH;
	cashTransaction.data.cash.amount = 10;

	Transaction cardTransaction;
	cardTransaction.type = CARD;
	sprintf(cardTransaction.data.card.number, "1234-5678-9876-5-");
	cardTransaction.data.card.installment = 6;

	Transaction cryptoTransaction;
	cryptoTransaction.type = CRYPTO;
	sprintf(cryptoTransaction.data.crypto.address, "1A21RFKEMDGM1JNFJEDNF788GHENL1");
	cryptoTransaction.data.crypto.amount = 0.7500;
	return 0;
}

```

### 메모리 구조

| Text Segment
코드영역 | 실행 코드(기계어 코드) 저장, 낮은 메모리 주소
프로그램의 실행 코드(기계어 코드)가 저장되는 영역
컴파일러가 생성한 실행 파일(.text section)에 포함
읽기 전용(read-only)으로 설정되어 실행 중 코드 수정 불가능 |
| --- | --- |
|  Data Segment | 초기화된 전역변수 및 정적 변수 저장
초기값이 있는 전역/정적 변수가 저장됨
프로그램 실행 전에 메모리에 로드됨 |
|  BSS Segment | 초기화 되지 않은 전역 변수 및 정적 변수 저장
초기화되지 않은 전역/정적 변수가 저장됨
실행 시 0으로 자동 초기화됨 |
| Heap | 동적 메모리할당 영역 (malloc, calloc, realloc, free) |
| Stack | 함수 호출시 생성되는 지역변수 높은 메모리 주소 |

### 동적 할당

### 1. `malloc()` (Memory Allocation)

- `malloc(size_t size)`는 지정한 바이트만큼의 메모리를 동적으로 할당하는 함수이다.
- 반환된 메모리는 초기화되지 않으며, 이전에 사용된 데이터가 남아 있을 수 있다.
- 성공하면 메모리의 첫 번째 바이트 주소를 반환하고, 실패하면 `NULL`을 반환한다.

**예제:**

```c
c
복사편집
int *ptr = (int *)malloc(5 * sizeof(int)); // 정수 5개 크기의 메모리 할당
if (ptr == NULL) {
    printf("메모리 할당 실패\n");
}

```

---

### 2. `calloc()` (Contiguous Allocation)

- `calloc(size_t num, size_t size)`는 `num * size` 바이트만큼의 메모리를 할당하고, 모든 바이트를 `0`으로 초기화한다.
- `malloc()`과 달리 여러 개의 요소를 할당할 때 유용하다.
- 성공하면 메모리의 첫 번째 바이트 주소를 반환하고, 실패하면 `NULL`을 반환한다.

**예제:**

```c
c
복사편집
int *ptr = (int *)calloc(5, sizeof(int)); // 정수 5개 크기의 메모리 할당 (0으로 초기화)
if (ptr == NULL) {
    printf("메모리 할당 실패\n");
}

```

---

### 3. `realloc()` (Reallocation)

- `realloc(void *ptr, size_t new_size)`는 기존에 할당된 메모리 크기를 변경하는 함수이다.
- 기존의 메모리를 확장하거나 축소할 때 사용한다.
- 확장 시, 기존 데이터는 유지되며, 새로 추가된 공간은 초기화되지 않는다.
- 새로운 크기의 메모리를 확보한 후 기존 데이터를 복사하고, 기존 메모리를 해제하는 방식이므로, 원래 포인터가 무효화될 수 있다.
- 기존 포인터가 `NULL`이면 `malloc()`과 동일하게 동작한다.

**예제:**

```c
c
복사편집
int *ptr = (int *)malloc(5 * sizeof(int)); // 초기 할당
ptr = (int *)realloc(ptr, 10 * sizeof(int)); // 크기 변경
if (ptr == NULL) {
    printf("메모리 재할당 실패\n");
}

```

---

### `malloc`, `calloc`, `realloc` 비교

| 함수 | 할당 방식 | 초기화 여부 | 용도 |
| --- | --- | --- | --- |
| `malloc()` | 단일 블록 할당 | 초기화되지 않음 (쓰레기 값) | 기본적인 동적 메모리 할당 |
| `calloc()` | 여러 블록 할당 | `0`으로 초기화됨 | 여러 개의 요소를 초기화하며 할당 |
| `realloc()` | 기존 메모리 크기 변경 | 추가된 메모리는 초기화되지 않음 | 기존 데이터를 유지하며 크기 조정 |

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_NAME_LENGTH 100

// 학생 구조체 정의
typedef struct {
    char name[MAX_NAME_LENGTH];
    int score;
} Student;

int main() {
    int num_students;

    // 사용자로부터 학생 수 입력 받기
    printf("학생 수를 입력하세요: ");
    scanf("%d", &num_students);

    // 메모리 동적 할당
    Student* students = (Student*)malloc(num_students * sizeof(Student));
    if (students == NULL) {
        fprintf(stderr, "메모리 할당 오류\n");
        return 1;
    }

    // 각 학생의 정보 입력 받기
    for (int i = 0; i < num_students; ++i) {
        printf("학생 %d의 이름을 입력하세요: ", i + 1);
        scanf(" %[^\n]s", students[i].name); // 이름 입력
        printf("학생 %d의 점수를 입력하세요 (0~100): ", i + 1);
        scanf("%d", &students[i].score); // 점수 입력

        // 점수 유효성 검사 (0~100 사이의 값인지 확인)
        while (students[i].score < 0 || students[i].score > 100) {
            printf("유효하지 않은 점수입니다. 다시 입력하세요 (0~100): ");
            scanf("%d", &students[i].score);
        }
    }

    // 각 학생의 정보 출력
    printf("\n입력된 학생 정보:\n");
    for (int i = 0; i < num_students; ++i) {
        printf("학생 %d: %s, 점수: %d\n", i + 1, students[i].name, students[i].score);
    }

    free(students);

    return 0;
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
    char name[20];
    int account;
    double balance;
} CUSTOMER;

int main() {
    int num;
    printf("고객 수를 입력하세요: ");
    scanf("%d", &num);
    CUSTOMER* customer = (CUSTOMER*)malloc(num * sizeof(CUSTOMER));
    for (int i = 0; i < num; i++) {
        printf("고객 %d의 이름을 입력하세요: ", i+1);
        scanf("%s", customer[i].name);
        printf("고객 %d의 계좌 번호를 입력하세요: ", i + 1);
        scanf("%d", &customer[i].account);
        printf("고객 %d의 잔고를 입력하세요: ", i + 1);
        scanf("%lf", &customer[i].balance);
        while (customer[i].balance < 0) {
            printf("잔고는 음수일 수 없습니다. 다시 입력하세요: ");
            scanf("%lf", &customer[i].balance);
        }
    }
    printf("\n입력된 고객 정보:\n");
    for (int i = 0; i < num; i++) {
        printf("고객 %d: %s 계좌번호: %d 잔고: %lf \n", i + 1, customer[i].name, customer[i].account, customer[i].balance);
    }

    return 0;
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX 100
typedef struct {
	char stock_name[MAX];
	int amount;
	double price;
} Transaction;
typedef struct {
	char name[MAX];
	int account_number;
	int transaction_count;
	Transaction* transactions;
} Customer;

int main() {
	int n;
	printf("고객 수를 입력하세요: ");
	scanf("%d", &n);
	Customer* customers = (Customer*)calloc(n, sizeof(Customer));
	for (int i = 0; i < n; i++) {
		printf("고객 %d의 이름을 입력하세요", i + 1);
		scanf("%s", customers[i].name);
		printf("고객 %d의 계좌 번호를 입력하세요", i + 1);
		scanf("%d", &customers[i].account_number);
		printf("고객 %d의 거래내역 수", i + 1);
		scanf("%d", &customers[i].transaction_count);
		customers[i].transactions = (Transaction*)calloc(customers[i].transaction_count, sizeof(Transaction));
		for (int j = 0; j < customers[i].transaction_count; j++) {
			printf("주식 이름을 입력하세요: ");
			scanf("%s", customers[i].transactions->stock_name);
			printf("거래 수량을 입력하세요: ");
			scanf("%d", &customers[i].transactions->amount);
			printf("거래 가격을 입력하세요: ");
			scanf("%d", &customers[i].transactions->price);
		}
	}
	printf("\n");
	for (int i = 0; i < n; i++) {
		printf("고객 %d: %s, 계좌번호: %d\n", i+1, customers[i].name, customers[i].account_number);
		for (int j = 0; j < customers[i].transaction_count; j++) {
			printf("거래 %d: 주식 이름: %s, 거래 수량: %d, 거래 가격: %.2lf\n", j + 1, customers[i].transactions->stock_name, customers[i].transactions->amount, customers[i].transactions->price);
		}
		printf("\n");
	}
	for (int i = 0; i < n; i++) {
		free(customers[i].transactions);  // 대출 정보 메모리 해제
	}

	free(customers);
	return 0;
}

```
