# TIL: C 프로젝트

## 학습 날짜: 2025년 3월 30일

## 오늘 배운 내용 정리

### C에서 메모리 접근 오류 해결

오늘 C 코드에서 발생한 "0xC0000005: Access Violation" 오류를 해결하는 과정을 학습함.

### 1. 오류 발생 원인 분석

- `rows[i]`를 접근할 때 예외가 발생함.
- `rows`는 단일 포인터(`StockJsonRow*`)로 선언되어 있어, 배열처럼 접근하면 오류가 발생함.
- `executeSelectAllWithMapping` 함수가 `rows`를 적절하게 할당하지 않았거나, 반환된 메모리가 올바르게 초기화되지 않았을 가능성이 있음.

### 2. 해결 방법

1. **배열로 선언 및 동적 할당**
    - `malloc`을 사용해 충분한 크기의 메모리를 할당하여 배열처럼 사용할 수 있도록 수정함.
    
    ```
    StockJsonRow* rows = (StockJsonRow*)malloc(sizeof(StockJsonRow) * 100); // 예제: 100개까지 저장 가능
    ```
    
2. **NULL 체크 추가**
    - `rows[i]`를 접근하기 전에 `NULL`인지 확인하여 메모리 접근 오류를 방지함.
    
    ```
    if (rows[i] != NULL) {
        printf("Customer Name: %s\n", rows[i].customer_name ? rows[i].customer_name : "NULL");
    }
    ```
    
3. **메모리 해제 처리**
    - `malloc`으로 할당한 메모리를 사용 후 `free`를 통해 해제하여 메모리 누수를 방지함.
