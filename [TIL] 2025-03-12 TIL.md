# TIL: C언어 포인터와 배열, 이중포인터

## 학습 날짜: 2025년 3월 12일

## 오늘 배운 내용 정리

### 포인터 응용

- 포인터 실습 문제 풀이 진행
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    int main()
    {
    	int score = 100, * pscore, id[5] = { 101, 204, 489, 328, 532 }, * pid;
    	pscore = &score;
    	pid = id; // 배열명은 배열의 시작 주소
    	int s = 20;
    	score = s;
    	// pscore = s; 잘못된 코드. 정수형 vs 포인터 변수 -> 자료형이 맞지 않음
    	s = *pscore;
    	printf("%d", s);
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    int main()
    {
    	int sum = 10, score[] = { 99,80,91,78,85 };
    	int* psum, * pscore1, * pscore2;
    	psum = &sum;
    	pscore1 = score;
    	pscore2 = score;
    	++pscore2;
    	pscore2++;
    	printf("변수 sum의 값 : %d \n", sum); // 10
    	printf("포인터 변수 *psum의 값 : %d \n", *psum); // 10
    	printf("포인터 변수 *psum+1의 값 : %d \n", *psum+1); // 11
    	printf("포인터 변수 ++*psum의 값 : %d \n", ++*psum); // 11
    	printf("포인터 변수 *(psum+1)의 값 : %d \n", *(psum+1)); // 알 수 없는 수
    	printf("포인터 변수 *++psum의 값 : %d \n", *++psum); // 알 수 없는 수
    	printf("=========================================================\n");
    	printf("포인터 변수 *pscore1의 값 : %d \n", *pscore1); // 99
    	printf("포인터 변수 *pscore1+1의 값 : %d \n", *pscore1+1); // 100
    	printf("포인터 변수 *(pscore1+1)의 값 : %d \n", *(pscore1+1)); // 80
    	printf("포인터 변수 *pscore2-*pscore1의 값 : %d \n", *pscore2-*pscore1); // 91-99 = -8
    	printf("포인터 변수 pscore2-pscore1의 값 : %d \n", pscore2-pscore1); // 2
    	printf("포인터 변수 pscore2의 값 : %d \n", pscore2); // 주소값
    	printf("포인터 변수 pscore1의 값 : %d \n", pscore1); // 주소값
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    int main()
    {
    	char city1[] = "seoul"; // 스택 영역	"seoul" 저장됨
    	char* city2 = "seoul"; // 데이터 영역 저장 seoul  city2 스택영역 저장
    	strcpy(city1, "busan");
    	city2 = "busan";
    	printf("%c %c\n", city1[0], city2[0]);
    	printf("%c %c\n", *(city1+2), *(city2+2));
    
    	char city3[] = "seoul";
    	char city4[] = "jeju";
    	char* pcity3 = city3;
    	pcity3[0] = 'k';
    	printf("%s\n", pcity3); // 출력된다. 
    	//printf("%s\n", *pcity3); // 출력되지 않는다. 이유: 주소로부터 몇 바이트를 읽어와야 할 지 몰라서
    	strcpy(pcity3, "busan");
    	printf("%s\n", pcity3);
    	pcity3 = "korea";
    	printf("%s\n", pcity3);
    
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <math.h>
    #define SIZE 5
    int main()
    {
    	int balances[SIZE] = { 1000, 5000, 3000, 7000, 2000 }; // 계좌잔액
    	int* ptr = balances;
    	int amount;
    	int* max = ptr;
    	int* min = ptr;
    	printf("현재 계좌 잔액:");
    	for (int i = 0; i < SIZE; i++) {
    		printf(" %d", *(ptr+i));
    	}
    	printf("\n");
    	printf("\n");
    	for (int i = 0; i < SIZE; i++) {
    		printf("%d번 고객 입금할 금액: ", i+1);
    		scanf("%d", &amount);
    		*(ptr+i) += amount;
    	}
    	printf("\n");
    	printf("업데이트된 계좌 잔액:");
    	for (int i = 0; i < SIZE; i++) {
    		printf(" %d", balances[i]);
    		if (*(ptr+i) > max) max = balances[i];
    		if (*(ptr + i) < min) min = balances[i];
    	}
    	printf("\n");
    	printf("\n");
    	printf("최대 잔액: %d\n", max);
    	printf("최소 잔액: %d", min);
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <math.h>
    #define SIZE 5
    int main()
    {
    	int accounts[SIZE] = { 101, 102, 103, 104, 105 }; // 계좌번호
    	int passwords[SIZE] = { 1234, 4321, 5678, 8765, 3456 }; // 비밀번호
    	int account, password;
    	int idx = SIZE + 1;
    	int* ptr = accounts;
    	int* pas = passwords;
    	printf("계좌번호 입력: ");
    	scanf("%d", &account);
    	for (int i = 0; i < SIZE; i++) {
    		if (*(ptr + i) == account) {
    			idx = i;
    			break;
    		}
    	}
    	printf("비밀번호 입력: ");
    	scanf("%d", &password);
    	if (password == *(pas + idx)) {
    		printf("로그인 성공");
    	}
    	else {
    		printf("비밀번호 오류");
    	}
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <math.h>
    #define SIZE 5
    int main()
    {
    	int students[SIZE][SIZE];
    	int* ptr = &students[0][0];
    	float averages[SIZE];
    	float* avg_ptr = averages;
    	printf("학셍들의 성적을 입력하세요 (각 학생당 5과목): \n");
    	for (int i = 0; i < SIZE; i++){
    		printf("학생 %d 성적 입력: ", i+1);
    		for (int j = 0; j < SIZE; j++) {
    			scanf(" %d", (ptr+i*SIZE+j));
    		}			
    		}
    	for (int i = 0; i < SIZE; i++) {
    		int sum = 0;
    		for (int j = 0; j < SIZE; j++) {
    			sum += *(ptr + i * SIZE + j);
    		}
    		*(avg_ptr + i) = sum / (float)SIZE;
    	}
    	printf("\n");
    	printf("\n");
    	printf("학생들의 평균 성적:\n");
    	for (int i = 0; i < SIZE; i++) {
    		printf("학생 %d 평균: %.1lf\n", i + 1, *(avg_ptr+i));
    	}
    	int top_student = 0;
    	for (int i = 1; i < SIZE; i++) {
    		if (*(avg_ptr + i) > *(avg_ptr + top_student)) {
    			top_student = i;
    		}
    	}
    	printf("가장 높은 평균을 가진 학생은 학생 %d입니다.", top_student);
    	return 0;
    }
    ```
    
    ```c
    #define _CRT_SECURE_NO_WARNINGS
    #include <stdio.h>
    #include <string.h>
    #include <math.h>
    int main(){
    
    	int arr[3][3] = { {1,2,3}, {4,5,6}, {7,8,9} };
    	int* ptr[3] = { arr[0], arr[1], arr[2] };
    	for (int i = 0; i <  3; i++) {
    		for (int j = 0; j < 3; j++) {
    			ptr[i][j] = ptr[i][j] * ptr[i][j];
    			printf("%d ", ptr[i][j]);
    		}
    		printf("\n");
    	}
    	printf("\n");
    	for (int i = 0; i < 2; i++) {
    		for (int j = i+1; j < 3; j++) {
    			int temp = ptr[i][j];
    			ptr[i][j] = ptr[j][i];
    			ptr[j][i] = temp;
    		}
    	}
    	for (int i = 0; i < 3; i++) {
    		for (int j = 0; j < 3; j++) {
    			printf("%d ", ptr[i][j]);
    		}
    		printf("\n");
    	}
    	return 0;
    }
    ```
    

**`포인터 배열`**

- 포인터 자체를 배열로 만들어 사용
- 문자열의 배열을 처리할 때 사용

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <math.h>
int main(){
	char city[3][8] = { "seoul", "jeju", "busan" };
	char* city1[] = {"seoul", "jeju", "busan"};
	return 0;
}
```

**`함수 포인터`** 

- 함수의 시작 주소를 변수에 저장하여 사용
- 함수 시작주소를 포인터 변수에 저장하여 사용하는 방법
- 하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행 파일이 만들어지며, 이 경우 여러 개의 실행 파일들은 시작주소에 의해 구분

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <math.h>
int plus(int, int);
int minus(int, int);
int multiply(int, int);
int divide(int, int);
int main(){
	int (*fpm)(int, int);
	int num1, num2, flag;
	printf("1. 덧셈, 2. 뺄셈, 3. 곱셈, 4. 나눗셈 ");
	scanf_s("%d", &flag);
	printf("두 개의 숫자를 입력 >>");
	scanf_s("%d %d", &num1, &num2);
	if (flag == 1) {
		fpm = plus;		
	}
	else if (flag==2) {
		fpm = minus;
	}
	else if (flag == 3) {
		fpm = multiply;
	}
	else {
		fpm = divide;
	}
	printf("결과: %d\n", fpm(num1, num2));
	return 0;
}

int plus(int a, int b) { return a+b; };
int minus(int a, int b) { return a-b; };
int multiply(int a, int b) { return a*b; };
int divide(int a, int b) { return a/b; };
```

### 메모리 구조 및 동적 메모리 관리

1. 코드 영역
    - 프로그램의 실행 코드가 저장
    - 읽기 전용으로 코드 실행 중에는 수정 안 된다
    - 프로그램 모든 함수와 명령어 포함
2. 전역변수 영역
    - 전역 변수와 정적 변수가 저장
    - 프로그램 시작 시 초기화
3. 스택 영역
    - 함수 호출 시 지역 변수와 매개 변수, 반환 주소 등이 저장되는 영역
    - LIFO 방식으로 작동
4. 힙 영역
    - 동적으로 할당된 메모리가 저장되는 영역
    - malloc, calloc, realloc, free와 같은 함수를 통해 관리
