# TIL: MVC 패턴

## 학습 날짜: 2025년 2월 12일

## 오늘 배운 내용 정리

> **MVC 패턴**
> 

![image (1)](https://github.com/user-attachments/assets/de6546b6-8694-4e2c-8d9c-34ae62878374)

모델-뷰-컨트롤러(model–view–controller, `MVC`)는 소프트웨어 공학에서 사용되는 소프트웨어 디자인 패턴

어플리케이션을 세 개의 영역으로 분할하고 각 구성 요소에게 고유한 역할을 부여하는 개발 방식이다. 

MVC 패턴을 도입하면 도메인(비즈니스 로직 & 데이터) 영역과 UI 영역이 분리되므로 서로 영향을 주지 않고 유지보수가 가능하다. 

- MVC 패턴은 기본적으로 `Layered Architecture`에 기반하고 있다.
    - **Layered Architecture**
        
        ![image](https://github.com/user-attachments/assets/49bccbe2-c42b-46de-8032-64d686e81e8e)

        1. Presentation Layer: 입출력에 관련된 code
        2. Business (Logic) Layer: 로직 처리
        3. Persistence Layer: Database 처리
        4. Database Layer: Oracle, MySQL 등 실제 DB
            
            ⇒ 인접한 Layer를 이용해서만 작업을 처리를 할 수 있다. 그래야지 프로그램이 꼬이지 않고 잘 돌아간다. **유지보수에 용이**
            
    - Model
        - Data Model: VO
        - Service Model: 로직 처리하는 객체. DB 처리는 없음
    - DAO(Data Access Object): DB 처리. 트랜잭션 관련 비즈니스 로직 처리는 없고, DB Access 처리만 허용한다.
    - View: 입출력을 담당하는 컴포넌트(ex. Java FX의 stage, FXML), 로직 처리는 없음
    - Controller: 제어 객체. View로부터 입력 받아서 Service에 연결해주는 객체

- **MVC를 사용하는 이유**
    
    결국 `‘유지보수의 편리성’이라는 하나의 결론으로 수렴`
    
    최초 설계를 꼼꼼하게 진행한 시스템이라도 유지보수가 발생하기 시작하면 각 기능간의 결합도(coupling)가 높아지는 경우가 발생한다. 이는 최초 설계 이념을 정했던 사람들의 부재 혹은 비즈니스 요건 변경으로 인해 필연적으로 발생하게 된다. 
    
    결합도가 높아진 시스템은 유지보수 작업 시 다른 비즈니스 로직에 영향을 미치게 되므로 사소한 코드의 변경이 의도치 않은 버그를 유발할 수 있다. 이런 문제점을 해결하기 위해 만들어진 소프트웨어 디자인 패턴
    

> **DI(Dependency Injection)**
> 

1. Step 1 → 일반적인 DAO(중복 code)
2. Step 2 → 중복된 code를 method로 분리(method extraction). class의 유지보수성 향상
3. Step 3 → 추상 method와 추상 class로 만들어서 제공. 가져가서 사용하는 사람들은 상속해서 사용 가능
    - `Template Method Pattern`: 상위 class에서 기본적인 logic의 흐름을 구현하고, 하위 class에서 구체적인 구현 내용을 변경해서 활용
    - 그러나, 단일 상속 문제 때문에 사용이 안되는 경우도 존재
4. step 4 → 상속을 배제하고 재활용 할 수 있는 방법 탐색
    - UserDAO의 변경해야 하는 부분을 별도의 class로 분리
5. step 5 → Interface와 UserDAO를 제공
    - 구현 class로 만들어서
6. step 6 → 의존성 주입. Main에서 UserDAO 생성할 때, DB Connection 하는 객체를 인자로 주입해주어 사용.
