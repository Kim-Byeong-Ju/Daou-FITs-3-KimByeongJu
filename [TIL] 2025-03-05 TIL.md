# TIL: C언어 입문

## 학습 날짜: 2025년 3월 5일

---

## 오늘 배운 내용 정리

### 1. C언어 개요

C언어는 하드웨어와 밀접하게 동작하는 저수준 언어로, 직접적으로 **CPU에 접근**할 수 있으며 **메모리를 할당하고 관리**할 수 있는 특징을 갖는다.

### C언어 학습 목표

1. **메모리를 효율적으로 관리하는 것**
2. **클린 코드 작성하기**
3. **학습한 내용을 체계적으로 정리하기**

---

### 2. 컴퓨터 구조 개요

### 하드웨어 5대 장치

1. **입력장치** (키보드, 마우스 등)
2. **출력장치** (모니터, 프린터 등)
3. **제어장치** (명령어 해석 및 실행)
4. **연산장치** (산술 및 논리 연산 수행)
5. **기억장치** (RAM, HDD 등 데이터 저장)

제어장치와 연산장치는 **CPU** 내부에 포함됨.

### 폰 노이만 아키텍처

- **프로그램 내장 방식**: 모든 소프트웨어는 반드시 **주기억장치(RAM)** 안에 로드된 후, CPU가 처리한다.

### 레지스터

레지스터는 CPU 내부의 **임시 저장 장치**로, 프로그램 실행 시 데이터 및 명령어를 저장하는 역할을 한다.

### 주요 레지스터 종류

- **프로그램 카운터 (PC)**: 다음에 실행할 명령어의 주소를 저장
- **누산기 (AC)**: 연산 결과를 저장하는 레지스터
- **명령 레지스터 (IR)**: 현재 실행 중인 명령어를 저장
- **기억장치 주소 레지스터 (MAR)**: 접근할 메모리 주소를 저장
- **버퍼 레지스터 (MBR)**: 메모리에서 읽어온 데이터를 저장

---

### 3. C언어 기본 개념

### C언어의 기본 구조

C언어 프로그램은 **헤더 파일 포함 → main() 함수 작성 → 코드 실행**의 구조로 이루어진다.

```
#include <stdio.h>  // 표준 입출력 라이브러리 포함

int main() {  // 프로그램의 시작점
    printf("Hello, World!\n");  // 출력 함수
    return 0;  // 프로그램 종료
}
```

### 기본 자료형

C언어에서 데이터의 종류를 구분하는 **자료형(Data Type)**

| 자료형 | 크기 (byte) | 설명 |
| --- | --- | --- |
| `char` | 1 | 문자 데이터 |
| `int` | 4 | 정수 데이터 |
| `float` | 4 | 실수 (소수점) 데이터 |
| `double` | 8 | 더 정밀한 실수 |

### 입력과 출력

### 1. **출력 (printf)**

```
printf("정수: %d, 문자: %c, 실수: %f\n", 10, 'A', 3.14);
```

| 서식 지정자 | 데이터 유형 |
| --- | --- |
| `%d` | 정수형 (int) |
| `%c` | 문자형 (char) |
| `%f` | 실수형 (float) |
| `%lf` | 실수형 (double) |

### 2. **입력 (scanf, getchar)**

```
int num;
printf("정수를 입력하세요: ");
scanf("%d", &num);  // 정수 입력받기
```

- `scanf` 사용 시 **변수 앞에 `&`(주소 연산자)를 붙여야 함**
- 문자 하나 입력: `char c = getchar();`

### 실습 문제: 2의 보수를 이용한 뺄셈

문제: **10진수 연산 `17 - 5`를 2의 보수를 이용하여 계산**

**풀이 과정**

1. `5`를 2진수로 변환 → `00000101`
2. `5`의 1의 보수 → `11111010`
3. 2의 보수 → `11111011` (`+1` 연산)
4. `17(00010001) + (-5)(11111011) = 00001100` (12, 즉 `17 - 5`)

---

### 자료형 문제 실습

```java
#include <stdio.h>
int main() {
	printf("실습 1번\n");
	printf("학과: %s\n", "컴퓨터공학과");
	printf("이름: %s\n", "김병주");
	printf("학번: %d\n", 20170450);
	printf("강사: %s\n", "정현희");
	printf("\n");
	printf("실습 2번\n");
	printf("이름: %s\n", "김병주");
	printf("목표: %s\n", "좋은 코드 짜는 개발자");
	printf("애칭: %s\n", "긁적긁적");
	return 0;
}
```

```java
#include <stdio.h>

int main() {
	int a, b;
	printf("정수를 입력하세요 >>\n");
	scanf_s("%d %d", &a, &b);
	printf("입력한 정수는 %d, %d입니다.\n", a,b);

	double c, d;
	printf("실수를 입력하세요 >>\n");
	scanf_s("%lf %lf", &c, &d);
	printf("입력한 실수는 %lf, %lf입니다.\n", c, d);

	char e, f;
	printf("문자를 입력하세요 >>");
	e = getchar();
	getchar();
	f = getchar();
	
	printf("%c %c", e, f);

	double d1;
	printf("실수값은? ");
	scanf_s("%lf", &d1);
	printf("입력한 실수는 %.2lf, %e입니다.\n", d1, d1);

	int asci;
	printf("아스키 코드값은? ");
	scanf_s("%d", &asci);
	printf("입력한 아스키 코드는 %c입니다.\n", asci);
	putchar(asci);
	

	char st[10];
	printf("하고 싶은 말은?");
	gets(st);
	printf("하고 싶은 말은 %s입니다.\n", st);
	return 0;
}

```
