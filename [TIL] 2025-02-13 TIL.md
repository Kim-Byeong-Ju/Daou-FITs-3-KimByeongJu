# TIL: MyBatis

## 학습 날짜: 2025년 2월 13일

## 학습 중 어려움을 느낀 부분

<aside>

1. 데이터베이스 Connection Pool 개념
2. SQLSessionFactory를 사용하는 이유
</aside>

 **** 

### Connection Pool이란?

`Connection Pool`은 **데이터베이스(DB)와 연결(Connection)을 미리 생성하여** 관리하는 기법입니다.

DB 연결은 **비용이 크고 느린 작업**이므로, 매번 새로 연결하는 대신 **미리 생성된 연결을 재사용**하여 성능을 최적화합니다.

### 🔹 **Connection Pool의 특징**

1. **미리 생성된 연결을 관리**
    - DB와의 연결(Connection)을 **미리 일정 개수만큼 생성**하여 Pool(풀)에서 관리함.
    - 필요할 때 가져가서 사용하고, 사용이 끝나면 다시 반환(반납).
2. **Connection을 재사용**
    - 매번 DB에 접속하고 끊는 과정이 **불필요**하여 성능이 향상됨.
    - 즉, **한 번 생성한 연결을 여러 번 사용**할 수 있음.
3. **동시 접속 요청을 효율적으로 처리**
    - 여러 개의 연결을 **큐(queue) 형태**로 관리하여 다수의 사용자가 동시에 접속해도 원활하게 처리.
    - 최대 연결 개수를 설정하여 **DB 과부하 방지** 가능.
4. **자동 연결 관리**
    - 사용되지 않는 연결을 **자동으로 회수하거나 닫음**(Idle Connection 관리).
    - 데이터베이스 연결 누수를 방지.

---

### 🔹 **Connection Pool 사용 시 장점**

✅ **성능 향상**

- 매번 `Connection`을 생성 & 종료하는 비용이 줄어들어 **DB 접근 속도가 빨라짐**.
- 다중 사용자 환경에서도 안정적인 성능 유지 가능.

✅ **리소스 관리 효율성**

- 사용하지 않는 `Connection`을 자동으로 반환하여 **불필요한 DB 과부하를 방지**.
- 최대 연결 수를 설정하여 서버 리소스를 절약할 수 있음.

✅ **DB 연결 누수 방지**

- `Connection`을 재사용하기 때문에, 사용 후 **반납하지 않는 문제(Connection Leak)**를 줄일 수 있음.

✅ **멀티스레드 환경에서 안정적**

- 여러 개의 스레드가 동시에 DB에 접근할 때, `Connection Pool`을 이용하면 **데이터베이스 부하를 제어**할 수 있음.

### SQLSessionFactory

MyBatis에서 `SqlSessionFactory`는 중요한 객체이다. 이 객체는 MyBatis의 설정 정보를 로드하고, 데이터베이스와의 연결을 설정하는 역할을 한다. 주로 MyBatis를 사용할 때 `SqlSessionFactory`를 설정하고 초기화하여 데이터베이스와의 세션을 관리하게 된다.

`SqlSessionFactory`의 주요 역할은 다음과 같다:

1. **환경 설정 로드**: MyBatis의 설정 파일을 읽어들여 데이터베이스 연결, 매핑 파일 위치 등의 환경 설정을 초기화한다.
2. **SqlSession 생성**: `SqlSessionFactory`는 `SqlSession` 인스턴스를 생성할 수 있는 팩토리 역할을 한다. `SqlSession`은 실제로 데이터베이스와의 세션을 맺고 SQL 쿼리를 실행할 수 있는 인터페이스이다.
3. **스레드 세이프한 인스턴스 관리**: `SqlSessionFactory`는 스레드 세이프한 방식으로 `SqlSession`을 생성하고 관리한다. 이는 여러 스레드에서 동시에 MyBatis를 사용할 때 데이터베이스 연결과 세션을 안전하게 관리할 수 있게 한다.
4. **캐싱 지원**: `SqlSessionFactory`는 MyBatis의 세션 캐싱을 관리하며, 성능 향상을 위해 쿼리 결과를 캐시할 수 있는 기능을 제공한다.

`SqlSessionFactory`를 구성하려면 MyBatis의 설정 파일 (`mybatis-config.xml`)을 설정하고, 데이터베이스 연결 정보와 매퍼 파일의 위치를 지정해야 한다. 이렇게 설정된 `SqlSessionFactory`는 애플리케이션에서 필요한 `SqlSession`을 생성하여 데이터베이스와의 효율적인 상호작용을 가능하게 한다.

## 오늘 배운 내용 정리

> **MyBatis**
> 

`MyBatis`를 한마디로 정의하자면 Database 연동을 도와주는 Framework이라고 할 수 있다. 

Controller에서 Service를 호출하고 Service가 DAO를 이용하여 Database처리를 할 때 DAO에서 이 MyBatis를 이용하여 Database 처리를 한다.

<aside>
✅ 기존의 JDBC는 연동 과정도 복잡하고 SQL문장이 코드내에 섞여 있어서 유지보수 하기가 상당히 불편했는데 이런점을 개선하여 편의성과 유지보수성을 높이기 위한 목적으로 만들어짐

</aside>

**MyBatis의 특징**

- SQL 구문과 Java code를 분리
- JDBC의 모든 기능을 MyBatis가 대부분 제공. 따라서 `복잡한 JDBC 코드를 걷어낼 수 있으며 깔끔한 소스코드를 유지`
- `SQL 실행결과를 Map객체에 mapping`
- `SQL 문장을 소스코드가 아닌 XML로 분리하여 작성` . 따라서 SQL의 변경이 있을 때마다 source code를 수정하거나 컴파일하지 않아도 된다.
- `DataSource기능과 Transaction처리기능을 제공`

> **Transaction 관리**
> 

MVC 패턴에서 DB 트랜잭션 처리는 **Service 레이어**에서 처리하는 것이 일반적이다. 

**1. 비즈니스 로직의 중앙 집중화**

- Service 레이어는 비즈니스 로직을 담당하므로, DB 트랜잭션을 포함한 로직도 여기서 처리하는 것이 적합
- 트랜잭션을 관리하는 주체가 `Service` 레이어가 되며, 여러 DAO를 호출하거나 복잡한 비즈니스 프로세스를 수행할 때 트랜잭션을 시작하고 커밋하거나 롤백할 수 있다.

**2. DAO는 DB와의 직접적인 상호작용만 담당**

- DAO(Data Access Object)는 DB와의 상호작용을 담당하며, SQL 쿼리를 실행하거나 데이터를 삽입/수정/삭제하는 책임만 진다.
- 트랜잭션의 시작과 종료는 DB 연결과 관련된 책임이므로, DAO는 트랜잭션 처리를 직접 하지 않는다. 대신, 트랜잭션 관리가 필요할 때 이를 상위 Service 레이어로 위임

**3. 트랜잭션 관리의 일관성**

- `Service`에서 트랜잭션을 관리함으로써 트랜잭션의 시작과 종료를 일관되게 처리할 수 있다.
    - 예를 들어, 여러 DAO 메소드를 호출하여 하나의 트랜잭션 내에서 처리해야 할 경우, 트랜잭션을 한 번에 시작하고 종료하는 것이 가능

**Before(Transaction 처리를 DAO에서 했을 때)**

```java
// service
@Override
public void deleteBook(String bisbn) throws Exception {
    SqlSessionFactory factory = MyBatisSessionFactory.getSqlSessionFactory();
    BookDAO dao = new BookDAO(factory);
    dao.delete(bisbn);
}

-----------------------------------------------------------------------------------------

// DAO
public void delete(String bisbn) {
        SqlSession sqlSession = sqlSessionFactory.openSession();
        try {
            sqlSession.delete("example.MyBook.delete", bisbn);
            sqlSession.commit();
            System.out.println("삭제 완료");
        } catch (Exception e) {
            e.printStackTrace();
        } finally {
            sqlSession.close();
        }
    }

```

**After(Transaction 처리를 Service 단으로 변경)**

```java
// Service
@Override
public void deleteBook(String isbn) throws Exception {
  SqlSessionFactory factory = MyBatisSessionFactory.getSqlSessionFactory();
  SqlSession sqlSession = factory.openSession();
  try {
      BookDAO dao = new BookDAO(sqlSession);
      dao.delete(isbn);
      sqlSession.commit();
  } catch (Exception e) {
      sqlSession.rollback();
      throw new Exception("delete 트랜잭션 처리 중 오류 발생", e);
  } finally {
      sqlSession.close();
  }
}

-----------------------------------------------------------------------------------------

// DAO
public void delete(String bisbn) {
  try {
      SqlSession.delete("example.MyBook.delete", bisbn);
      System.out.println("삭제 완료");
  } catch (Exception e) {
      e.printStackTrace();
  }
}
```
