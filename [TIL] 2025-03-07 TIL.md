# TIL: C언어 제어문, 함수

## 학습 날짜: 2025년 3월 7일

## 오늘 배운 내용 정리

### 1. 함수(Function)란?

C 언어에서 함수(Function)는 특정 작업을 수행하는 코드 블록입니다. 함수를 사용하면 코드의 재사용성을 높이고 가독성을 향상시킬 수 있습니다.

### 2. 함수의 기본 구조

```
반환형 함수명(매개변수) {
    // 실행할 코드
    return 반환값;
}
```

**예제: 두 수의 합을 반환하는 함수**

```
#include <stdio.h>

int add(int a, int b) {  // 정수형 반환, 두 개의 정수를 매개변수로 받음
    return a + b;
}

int main() {
    int result = add(3, 5);
    printf("결과: %d\n", result);
    return 0;
}
```

### 3. 함수의 종류

**(1) 반환값과 매개변수가 있는 함수**

```
int multiply(int a, int b) {
    return a * b;
}
```

**(2) 반환값이 없고 매개변수가 있는 함수**

```
void printMessage(char *message) {
    printf("%s\n", message);
}
```

**(3) 반환값이 있고 매개변수가 없는 함수**

```
int getNumber() {
    return 42;
}
```

**(4) 반환값과 매개변수가 없는 함수**

```
void sayHello() {
    printf("Hello, World!\n");
}
```

### 4. 함수의 호출 방식

**(1) 값에 의한 호출 (Call by Value)**

- 함수에 인자로 전달된 값만 복사됨.
- 원본 변수에 영향을 주지 않음.

```
void changeValue(int x) {
    x = 10;
}
```

**(2) 참조에 의한 호출 (Call by Reference)**

- 포인터를 사용하여 원본 데이터의 주소를 전달함.
- 함수 내에서 값을 변경하면 원본 데이터도 변경됨.

```
void changeValue(int *x) {
    *x = 10;
}
```

### 5. 재귀 함수 (Recursive Function)

자기 자신을 호출하는 함수로, 특정 종료 조건이 필요함.

**예제: 팩토리얼 계산**

```
int factorial(int n) {
    if (n == 0) return 1;
    return n * factorial(n - 1);
}
```

### 6. 함수의 선언과 정의

- 함수는 선언(Prototype)과 정의(Definition)으로 구분됨.
- 선언: 함수의 이름, 매개변수, 반환형을 알려줌 (보통 헤더 파일에서 선언함).
- 정의: 실제 함수의 구현부.

```
// 함수 선언
int add(int, int);

// 함수 정의
int add(int a, int b) {
    return a + b;
}
```

### 7. 내장 함수 (Standard Library Functions)

C 언어에서 기본적으로 제공하는 표준 라이브러리 함수.

### 주요 내장 함수 예시

- `printf()`, `scanf()`: 표준 입출력
- `strlen()`, `strcpy()`, `strcmp()`: 문자열 처리
- `malloc()`, `free()`: 동적 메모리 할당
