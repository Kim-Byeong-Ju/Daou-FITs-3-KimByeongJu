# TIL: JavaFX, VO, MVC 패턴

## 학습 날짜: 2025년 2월 11일

## 학습 중 어려움을 느낀 부분

<aside>

Java FX에 대해 처음 접해봐서 구조나 기본 개념들이 익숙치 않아서 복습 진행!

</aside>

### JavaFX 기본 구조 및 절차

1. **Application 클래스**: 모든 JavaFX 애플리케이션은 `javafx.application.Application` 클래스를 상속받아야 한다.
2. **start(Stage primaryStage) 메서드**: 애플리케이션의 진입점으로, UI를 설정하는 역할을 한다.
3. **Stage와 Scene**:
    - `Stage`: JavaFX의 최상위 컨테이너로, 기본적으로 윈도우를 의미한다.
    - `Scene`: 화면을 구성하는 컨테이너로, 여러 개의 `Node`(버튼, 레이블 등)를 포함할 수 있다.
4. **FXML과 컨트롤러 분리**: UI와 로직을 분리하기 위해 FXML 파일을 사용하며, 컨트롤러 클래스에서 이벤트 처리를 담당한다.
5. **이벤트 핸들링**: 버튼 클릭 등 사용자의 입력을 처리하는 방식으로, `setOnAction`이나 `EventHandler`를 사용한다.

### JavaFX 활용 시 유의점

1. **JavaFX SDK 추가 필요**: JavaFX는 JDK 11부터 기본 포함되지 않으므로, 별도로 다운로드하여 설정해야 한다.
2. **UI 스레드 활용**: UI 관련 작업은 반드시 JavaFX Application Thread에서 실행해야 하며, `Platform.runLater()`를 활용해야 한다.
3. **메모리 관리**: Scene과 Stage가 필요 이상으로 유지되지 않도록 관리하여 메모리 누수를 방지해야 한다.
4. **FXML 컨트롤러 설정**: `fx:controller` 속성이 올바르게 지정되지 않으면 컨트롤러 클래스가 정상적으로 동작하지 않는다.
5. **이벤트 핸들링 최적화**: 이벤트 리스너를 과도하게 등록하지 않도록 관리해야 성능 저하를 방지할 수 있다.

### IntelliJ에서 JavaFX 설정 방법

1. **JavaFX 라이브러리 추가**:
    - `File` → `Project Structure` → `Libraries`에서 JavaFX SDK 경로를 추가한다.
2. **VM 옵션 설정**:
    - `Run` → `Edit Configurations` → `VM options`에 `-module-path {JavaFX SDK 경로}/lib --add-modules javafx.controls,javafx.fxml` 추가한다.
3. **FXML 파일 로드 오류 해결**:
    - `FXMLLoader` 사용 시 `getClass().getResource()` 경로를 올바르게 설정해야 한다.
4. **JDK 버전 확인**:
    - JavaFX는 JDK 17 이상에서 안정적으로 동작하므로, 프로젝트의 JDK 버전을 최신으로 유지한다.

## 오늘 배운 내용 정리

> **JavaFX**
> 

JavaFX는 표준 GUI 라이브러리였던 Swing을 대체하기 위해 만들어진 GUI 라이브러리이다.

> **Event 처리**
> 
- **Event Delegation Model**
    1. Event Source: 이벤트가 발생하는 객체이다.
    2. Event 객체: 이벤트가 발생하면 자동으로 생성되며, 이벤트 세부 정보를 담고 있다.
    3. Event 처리 객체(Handler): 이벤트를 처리하는 객체이다.

> **VO (Value Object)**
> 
- **DO (Domain Object)**: 도메인에서 사용하는 데이터를 객체화한 것이다.
- **VO (Value Object)**: 각 테이블의 하나의 row를 담을 수 있는 객체이다.
- **Entity**: VO와 유사하지만 Primary Key를 포함한다.
- **DTO (Data Transfer Object)**: 데이터 전송을 위한 객체이다.

> **Layered Architecture**
> 
1. **Presentation Layer**: 사용자와 직접 상호작용하는 부분으로 입력을 받거나 출력을 담당한다.
2. **Business (Logic) Layer**: 애플리케이션의 핵심 로직을 처리한다.
3. **Persistence Layer**: DB 접근을 담당한다.
4. **Database Layer**: 실제 데이터베이스(MySQL, Oracle 등)를 관리한다.

> **MVC 패턴**
> 

**Model - View - Controller** 패턴은 애플리케이션을 역할별로 분리하여 유지보수성을 높인다.

- **Model**
    - **Data Model (VO)**: 데이터를 표현하는 객체이다.
    - **Service Model**: 로직을 처리하는 객체이다.
        - 순수 로직을 처리하는 객체이다.
        - DB 관련 로직 처리는 Model에서 직접 수행하지 않으며, DAO (Data Access Object)가 담당한다.
- **View**
    - 사용자와 상호작용하는 화면을 담당하며, Presentation Layer에 해당한다.
- **Controller**
    - View와 Model을 연결하여 사용자 요청을 처리한다.
