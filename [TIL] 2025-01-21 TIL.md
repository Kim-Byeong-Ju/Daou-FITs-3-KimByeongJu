# TIL: SQL 중급

## 학습 날짜: 2025년 1월 21일

## 학습 중 어려움을 느낀 부분

- 계층 쿼리
    - 순방향 VS 역방향
        - CONNECT BY 절에서 PRIOR 키워드가 어디 앞에 붙었는지를 확인하면 됨
        - 자식 앞에 PRIOR가 붙으면 순방향, 부모 앞에 PRIOR가 붙으면 역방향!
        
        ```sql
        SELECT CATEGORY_ID, 
               PARENT_CATEGORY_ID, 
               LEVEL
          FROM SHOPPING_CATEGORIES
         START WITH PARENT_CATEGORY_ID IS NULL
        CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID;
        ```
        
    - LEVEL에 조건을 걸려면?
        - **WHERE절을 START WITH절 앞에 위치**시키면 된다!
        
        ```sql
        SELECT CATEGORY_ID, CATEGORY_NAME, LEVEL
          FROM SHOPPING_CATEGORIES
         WHERE LEVEL = 2
        START WITH PARENT_CATEGORY_ID IS NULL
        CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID;
        ```
        

- 집계함수 COUNT에 조건 설정하기
    - 인자에 CASE WHEN 구문 활용하여 설정 가능
    
    ```sql
    SELECT DEPARTMENT_ID, 
           COUNT(CASE WHEN MANAGER_ID IS NULL THEN 1 END) AS NO_MANAGER, 
           COUNT(CASE WHEN MANAGER_ID IS NOT NULL THEN 1 END) AS HAVE_MANAGER
      FROM EMPLOYEES E
     WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID;
    ```
    

## 오늘 배운 내용 정리

- 윈도우 함수 옵션
    - ROWS: 물리적 행을 기준으로 범위를 지정
        - EX) 현재 행을 기준으로 이전 2개 행
    - RANGE: 논리적 값을 기준으로 범위를 지정
        - EX) 현재 값보다 작거나 같은 모든 값
    - UNBOUNDED PRECEDING: 윈도우의 시작 범위를 첫번째 행으로 지정
        - EX) 현재 행까지의 누적 합계
    - CURRENT ROW: 현재 행을 기준으로 윈도우를 설정
        - EX) 현재 행부터 이후의 합계
    - FOLLOWING: 현재 행 이후의 특정 행 범위를 설정
        - EX) 현재 행 이후 두 개의 행까지 합계
    - 옵션 사용 시 유의사항
        - ROWS와 RANGE는 용도에 따라 신중히 선택
        - 데이터셋이 클 경우 성능에 유의
        - UNBOUNDED PRECEDING, FOLLOWING 옵션의 범위를 명확히 이해
    
- 계층 쿼리
    - 데이터가 부모-자식 관계로 구성된 계층적 구조를 조회하기 위한 쿼리
    - 트리 구조 데이터를 표현하고 탐색하는 데 사용
    - 주요 키워드
        - START WITH: 계층 구조의 시작점 지정
        - CONNETC BY: 부모-자식 관계를 정의
        - PRIOR: 계층의 방향성 설정
        - ORDER SIBLINGS BY: 동일 레벨의 데이터 정렬
        - LEVEL: 계층 깊이를 나타내는 가상 컬럼
    - 기본 문법
        
        ```sql
        SELECT 컬러명, LEVEL
        	FROM 테이블명
         START WITH 시작 조건
        CONNECT BY PRIOR 부모컬럼 = 자식컬럼;
        ```
