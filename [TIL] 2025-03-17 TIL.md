# TIL: 구조체 응용

## 학습 날짜: 2025년 3월 17일

## 오늘 배운 내용 정리

### 구조체 응용

- 구조체가 함수의 인수로 사용될 때, 구조체 포인터, 구조체 배열, 구조체 리스트 등으로 활용된다.
- 함수와 구조체
    - 함수의 인수로 구조체를 전달할 수 있다.
    - 함수의 인수로 구조체를 사용하려면 구조체를 전역 변수 선언 영역에 선언해야 한다.
    - 함수의 인수로 구조체가 전달되면 구조체의 모든 멤버가 복사되어 함수로 전달된다.
    - 즉, 구조체는 하나의 형으로 취급되기 때문에 주소에 의한 호출이 아니라 값에 의한 호출로 처리된다.
- 구조체 포인터
    - 함수의 매개변수로 구조체 주소를 이용한 구조체 포인터를 사용 가능
    - 장점: 구조체의 주소만 넘겨주면 되므로 멤버의 복사에 시간이 걸리지 않는다
    - 주소만 전달, 호출된 함수에서 구조체의 값이 바뀌면 호출한 함수의 구조체 값도 바뀐다
- 일반 구조체와 구조체 포인터를 사용할 때 멤버에 접근하는 연산자의 차이
    - 일반 구조체의 멤버 접근 연산자는 .(마침표) 연산자 사용
    - 구조체 포인터를 사용한 멤버의 접근에는 →(화살표) 연산자 사용

```c
// 실습과제 30

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

typedef struct student {
    char name[20];
    char sex;
    int stid;
    int sub1;
    int sub2;
    int sub3;
    double avg;
} ST;

int input(ST* st);
int main() {
    int i;
    ST st1 = {"kdhong", 'm', 1508001, 0, 0, 0, 0};
    input(st1); // 함수호출 인수로 구조체
    printf("\nmain 함수에서 세 과목의 점수는 %d %d %d", st1.sub1, st1.sub2, st1.sub3);
    return 0;
}
int input(ST st) {
    printf("%s 학생의 3과목 성적을 입력하세요", st.name);
    scanf("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
    printf("input 함수에서 입력한 값은 %d, %d, %d", st.sub1, st.sub2, st.sub3);
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

typedef struct student {
    char name[20];
    char sex;
    int stid;
    int sub1;
    int sub2;
    int sub3;
    double avg;
} ST;

int input(ST* st);
int main() {
    int i;
    ST st[3] = { {"kdhong", 'm', 1508001, 0,0,0,0}, {"yhkim", 'f',1608001,0,0,0,0 }, {"cskim", 'm',1608026,0,0,0,0 } };
    for (i = 0; i < 3; i++) {
        input(&st[i]);
        printf("main 함수에서 결과 출력 %d %d %d\n", st->sub1, st->sub2, st->sub3);
    }
    return 0;
}
int input(ST* st) {
    printf("%s 학생의 성적을 입력하세요 ", st->name);
    scanf("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
    printf("input 함수에서 결과 출력 %d %d %d\n", st->sub1, st->sub2, st->sub3);
}
```

```c
// 예제 1
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

typedef struct Account {
    int accountID;
    double balance;
} ACC;

typedef struct Customer {
    char name[20];
    int age;
    char customerID[20];
    ACC ac[10];
    int accountCount;
} CUS;

void addACC(CUS* customer);
void deleteACC(CUS* customer);
void customerInfo(const CUS* customer);
int main() {
    int flag;
    CUS customer = { "홍길동", 25, "gdhong", {0}, 0 };
    do {
        printf("\n1. 계좌 추가\n2. 계좌 삭제\n3. 고객 정보 보기\n4. 종료");
        printf("\n");
        printf("원하는 작업을 선택하세요: ");
        scanf("%d", &flag);
        switch (flag) {
            case 1:
                addACC(&customer);
                break;
            case 2:
                deleteACC(&customer);
                break;
            case 3:
                customerInfo(&customer);
                break;
            case 4:
                printf("프로그램을 종료합니다\n");
                return 0;
            default:
                printf("잘못된 입력입니다. 다시 선택하세요\n");
        }
    } while (1);
    return 0;
}

void addACC(CUS* customer) {
    int acc;
    double balance;
    printf("추가할 계좌 번호를 입력하세요: ");
    scanf("%d", &acc);
    printf("계좌의 초기 잔액을 입력하세요: ");
    scanf("%lf", &balance);
    customer->ac[customer->accountCount].accountID = acc;
    customer->ac[customer->accountCount].balance = balance;
    customer->accountCount++;
    printf("계좌가 추가되었습니다.\n");
}

void deleteACC(CUS* customer) {
    int acc, found = -1;
    printf("삭제할 계좌번호를 입력하세요: ");
    scanf("%d", &acc);
    for (int i = 0; i < customer->accountCount; i++) {
        if (customer->ac[i].accountID == acc) {
            found = i;
            break;
        }
    }
    if (found == -1) {
        printf("해당 계좌를 찾을 수 없습니다.\n");
        return;
    }
    for (int i = found; i < customer->accountCount - 1; i++) {
        customer->ac[i] = customer->ac[i + 1];
    }
    customer->accountCount--;
}

void customerInfo(CUS* customer) {
    printf("관리자: %s\n계좌 수: %d\n", customer->name, customer->accountCount);
    for (int i = 0; i < customer->accountCount; i++) {
        printf("계좌 %d: 잔액 %.2f\n", customer->ac[i].accountID, customer->ac[i].balance);
    }
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
typedef struct  {
	char name[20];
	double investment_amount;
	double return_rate;
} Investment;

typedef struct  {
	char name[20];
	int num_investment;
	Investment* investments;
} Customer;

void profit(Customer* customer) {
	for (int i = 0; i < customer->num_investment; i++) {
		double p = customer->investments[i].investment_amount * customer->investments[i].return_rate;
		printf("%s 투자 상품의 수익: %.2lf", customer->investments[i].name, p);
	}
}
int main() {
	char name[20];
	int amount;
	printf("고객 이름은? ");
	scanf("%s", name);
	printf("투자 상품 개수는? ");
	scanf("%d", &amount);
	Customer c1;
	strcpy(c1.name, name);
	c1.num_investment = amount;
	c1.investments = (Investment*)malloc(amount * sizeof(Investment));
	if (c1.investments == NULL) {
		printf("메모리할당 실패\n");
		return 1;
	}
	for (int i = 0; i < amount; i++) {
		printf("투자 상품 %d을 입력하세요(회사명, 투자금액, 수익률) ", i + 1);
		scanf("%s %lf %lf", c1.investments[i].name, &c1.investments[i].investment_amount, &c1.investments[i].return_rate);
	}
	printf("\n각 투자 상품의 수익:\n");
	profit(&c1);
	free(c1.investments);
	return 0;																					
}

```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <time.h>
#define MAX_DEPOSITS 5

// 정기 예금 구조체 정의
typedef struct {
    char name[30];             // 고객 이름
    char account_number[11];   // 계좌 번호 (10자리 + NULL)
    struct tm start_date;      // 예금 시작 날짜
    int duration_months;       // 예치 기간 (개월 단위)
    double amount;             // 예치 금액
} FixedDeposit;

// 예금 만기일 계산 함수
void calculate_maturity_date(struct tm start_date, int months, struct tm* maturity_date) {
    *maturity_date = start_date;
    maturity_date->tm_mon += months;
    mktime(maturity_date); // 날짜 조정
}

// 정기 예금을 만기일이 빠른 순으로 정렬
void sort_by_maturity_date(FixedDeposit deposits[], int size) {
    for (int i = 0; i < size - 1; i++) {
        for (int j = i + 1; j < size; j++) {
            struct tm maturity_i, maturity_j;
            calculate_maturity_date(deposits[i].start_date, deposits[i].duration_months, &maturity_i);
            calculate_maturity_date(deposits[j].start_date, deposits[j].duration_months, &maturity_j);

            if (mktime(&maturity_i) > mktime(&maturity_j)) {
                FixedDeposit temp = deposits[i];
                deposits[i] = deposits[j];
                deposits[j] = temp;
            }
        }
    }
}

// 만기 여부 확인
void check_maturity(FixedDeposit deposit) {
    struct tm maturity_date;
    calculate_maturity_date(deposit.start_date, deposit.duration_months, &maturity_date);

    time_t now_time = time(NULL);
    struct tm* current_time = localtime(&now_time);

    if (mktime(&maturity_date) <= now_time) {
        printf("만기됨\n");
    }
    else {
        printf(" 미만기\n");
    }
}

// 예금 정보 출력
void display_deposits(FixedDeposit deposits[], int size) {
    printf("\n=== 정기 예금 리스트 (만기일 빠른 순) ===\n");
    printf("이름\t계좌번호\t예금 시작\t예치금액\t만기일\t\t상태\n");
    printf("--------------------------------------------------\n");

    for (int i = 0; i < size; i++) {
        struct tm maturity_date;
        calculate_maturity_date(deposits[i].start_date, deposits[i].duration_months, &maturity_date);

        printf("%s\t%s\t%d-%02d-%02d\t%.2f\t%d-%02d-%02d\t",
            deposits[i].name,
            deposits[i].account_number,
            deposits[i].start_date.tm_year + 1900, deposits[i].start_date.tm_mon + 1, deposits[i].start_date.tm_mday,
            deposits[i].amount,
            maturity_date.tm_year + 1900, maturity_date.tm_mon + 1, maturity_date.tm_mday);
        check_maturity(deposits[i]);
    }
}

int main() {
    // 예금 정보 초기화
    FixedDeposit deposits[MAX_DEPOSITS] = {
        {"김철수", "1234567890", {0, 0, 0, 10, 0, 124}, 12, 5000000},
        {"이영희", "9876543210", {0, 0, 0, 1, 6, 123}, 24, 3000000},
        {"박민수", "5678901234", {0, 0, 0, 15, 2, 123}, 18, 2000000},
        {"조수빈", "3456789012", {0, 0, 0, 5, 8, 123}, 6, 1000000},
        {"정하나", "6789012345", {0, 0, 0, 20, 10, 123}, 12, 7000000}
    };

    // 만기일 기준 정렬
    sort_by_maturity_date(deposits, MAX_DEPOSITS);

    // 예금 리스트 출력
    display_deposits(deposits, MAX_DEPOSITS);

    return 0;
}

```

```c
 #define _CRT_SECURE_NO_WARNINGS
 #include <stdio.h>
 #include <math.h>
 #define DAYS 30
#define STOCKS 3
 // 주식 정보 구조체 정의
typedef struct {
    char name[20];
    char ticker[7];
    int shares;
    double prices[DAYS];
    double avg_price;
    double volatility;
 } Stock
 // 30일 평균 가격 계산
double calculate_average(double prices[]) {
    double sum = 0.0;
    for (int i = 0; i < DAYS i++) {
        sum += prices[i];
    }
    return sum / DAYS
 }
 // 30일 표준편차(변동성) 계산
double calculate_volatility(double prices[], double avg_price) {
    double variance = 0.0;
    for (int i = 0; i < DAYS i++) {
        variance += pow(prices[i] - avg_price, 2);
    }
    variance /= DAYS
    return sqrt(variance);
 }
 // 위험 수준 평가
const char* categorize_risk(double volatility) {
    if (volatility <= 5) return "낮음"
    if (volatility <= 10) return "보통"
    return "높음"
 }
 // 주식 포트폴리오 출력
void display_portfolio(Stock stocks[], int n) {
    printf("\n[ 고객 주식 포트폴리오 위험 분석 ]\n");
    for (int i = 0; i < n i++) {
        stocks[i].avg_price = calculate_average(stocks[i].prices);
        stocks[i].volatility = calculate_volatility(stocks[i].prices, stocks[i].avg_price);
        printf("종목: %s (%s) | 보유 주식: %d | 변동성: %.2lf | 위험 수준: %s\n",
            stocks[i].name, stocks[i].ticker, stocks[i].shares, stocks[i].volatility,
            categorize_risk(stocks[i].volatility));
    }
 }
 int main() {
    Stock stocks[STOCKS] = {
        {"삼성전자", "005930", 100, {65400, 65200, 65000, 64800, 65500, 66000, 66200, 
66100, 66300, 65900, 65550, 65800, 66050, 66350, 66700, 66800, 67000, 66900, 67200, 
67300, 67100, 67050, 67250, 67400, 67600, 67500, 67450, 67650, 67800, 67900}},
        {"테슬라", "TSLA", 50, {210, 215, 220, 218, 222, 225, 230, 228, 232, 235, 238, 240, 
245, 250, 248, 246, 244, 241, 238, 235, 233, 231, 228, 226, 224, 223, 220, 218, 215, 212}},
        {"애플", "AAPL", 80, {180, 182, 184, 183, 185, 186, 188, 187, 189, 191, 193, 195, 
198, 200, 202, 203, 205, 207, 209, 210, 212, 213, 214, 215, 217, 218, 219, 220, 221, 222}}
    };
    display_portfolio(stocks, STOCKS);
    return 0; }
```

### 구조체, 공용체, 열거형

1. 구조체 vs 공용체 vs 열거형 개요
① 구조체 (struct)
구조체는 여러 서로 다른 타입의 데이터를 하나의 단위로 묶을 수 있습니다. 구조체 내의
각 멤버는 서로 독립적으로 메모리 공간을 차지하므로, 여러 데이터 항목을 묶어 관리하고자
할 때 유용합니다. 
    
    ② 공용체 (union)
    공용체는 같은 메모리 공간을 공유하는 멤버들을 갖습니다. 즉, 여러 멤버 중에서 하나의
    값만 유효하며, 메모리 공간을 절약할 수 있지만 동시에 값을 한 번에 하나만 저장할 수 있습
    니다. 메모리 절약이 중요한 경우 사용됩니다. 
    
    ③ 열거형 (enum)
    열거형은 관련된 상수 값들을 의미 있는 이름으로 정의하는 데 사용됩니다. 열거형을 사용
    하면 코드에서 숫자 대신 의미 있는 이름을 사용할 수 있어 가독성이 높아집니다. 주로 상태
    코드나, 설정 값 등을 정의할 때 유용합니다. 
    

2. 구조체 vs 공용체 vs 열거형 비교
![image (2)](https://github.com/user-attachments/assets/3e5cebed-f77d-42b5-beac-26d8fe89729b)
