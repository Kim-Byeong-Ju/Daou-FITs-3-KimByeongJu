# TIL: 알고리즘, 자료구조

## 학습 날짜: 2025년 1월 7일

## 학습 중 어려움을 느낀 부분

자료 구조에서 연결 리스트의 구조가 머릿속으로 잘 그려지지 않아서 이해가 잘 되지 않았다. 

## 어려움을 해결한 과정

연결리스트에 대해 설명한 그림을 통해서 이해하려고 노력했다. 단순 연결리스트, 이중 연결리스트에서의 각 노드 모양이 어떻게 생겼는지 확인하고 데이터 간의 연결이 어떻게 이뤄지는지 파악했다. 

또한, 파이썬으로 연결리스트를 구현한 코드를 직접 작성해보며 Append, Delete 과정을 이해할 수 있었다. 


## 오늘 배운 내용 정리

### 알고리즘

어떤 문제를 해결하기 위해 구성된 일련의 절차

- 시간 복잡도
    - 알고리즘이 얼마나 효율적으로 동작하는지 평가하는 척도
    - 입력 크기(n)에 대해 실행되는 시간을 수학적으로 표현
    - 점근적 표기법
        - 시간 복잡도를 근사치로 표현한 것
        - 표기법 종류
            - 오메가 표기법: 최선의 경우에서의 시간 복잡도
            - 세타 표기법: 평균적인 경우에서의 시간 복잡도
            - 빅오 표기법: 최악의 경우에서의 시간 복잡도
    - 문제해결에 따른 시간복잡도
        - 상수시간: O(1)
        - 로그시간: O(logn)
        - 직선적 시간: O(n)
        - 선형 로그형:  O(nlogn)
        - 2차 시간: O(n^2)
        - 지수 시간: O(C^n)
- 선형 탐색
    - 주어진 데이터 집합에서 원하는 데이터를 처음부터 순차적으로 비교하며 찾는 방법

- 이진 탐색
    - **정렬된** 데이터 집합을 반으로 쪼개 가면서 탐색하는 방법

- 정렬 알고리즘
    - 선택 정렬: 정렬되지 않은 데이터 중에서 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환 해나가는 방식
    
    ```python
    def selction_sort(ds):
    	for a in range(0, len(ds)-1):
    		min_ind = a
    		for b in range(a+1, len(ds)):
    			if ds[b] < ds[min_ind]:
    				min_ind = b
    		ds[a], ds[min_ind] = ds[min_ind], ds[a]
    ```
    
    - 삽입 정렬: 아직 정렬되지 않은 임의의 데이터를 이미 정렬된 부분의 적절한 위치에 삽입해가며 정렬하는 방식
    
    ```python
    def insertion(ds):
    	for a in range(1, len(ds)):
    		key = ds[a]
    		b = a-1
    		while b >=0 and ds[b]>key:
    			ds[b+1] = ds[b]
    			b -= 1
    		ds[b+1] = key
    ```
    
    - 버블 정렬: 서로 이웃한 데이터들을 비교해 가장 큰 데이터를 맨 뒤로 보내며 정렬하는 방식
    
    ```python
    def bubble(ds):
    	for a in range(0, len(ds)-1):
    		for b in range(0, len(ds)-1-a):
    			if ds[b] > ds[b+1]:
    				ds[b], ds[b+1] = ds[b+1], ds[b]
    ```
    
    - 퀵 정렬: 기준키를 중심으로 작거나 같은 값을 지닌 데이터는 앞으로, 큰 값을 지닌 데이터는 뒤로 가도록 하여 작은 값을 갖는 데이터와 큰 값을 갖는 데이터로 분리해가며 정렬하는 방법
        - 피봇을 선택하는 방법: 1) 첫번째나 마지막 요소, 2) 임의로 선택, 3) 중간값을 선택
        
        ```python
        def quick_sort(ds):
        	if len(ds) < 2:
        		return ds
        	else:
        		key = ds[0]
        		left = [data for data in ds[1:] if data <= key]
        		right = [data for data in ds[1:] if data > key]
        		# ds의 길이가 2 미만이 될 때까지 재귀적으로 쪼갠다. 
        		# key를 기준으로 작은 애는 왼쪽 더 큰 애는 오른쪽에 위치시킴.
        		return quick_sort(left) + [key] + quick(right)
        ```
        
    - 병합 정렬: 배열을 반으로 나누고, 나눈 배열들을 재귀적으로 정렬한 뒤 정렬된 배열들을 병합하여 최종적으로 정렬된 배열을 만듦
        - 분할정복 방식으로 작동
    - 힙 정렬: 비교 기반의 정렬 알고리즘으로 힙 자료구조를 사용하여 정렬 수행
- 깊이 우선 탐색(DFS)
    - 시작 정점에서 시작하여, 그 정점과 연결된 방문하지 않은 한 정점을 방문하고 다음에는 방문한 정점에서 다시 연결된 방문하지 않은 한 정점 순으로 방문함.
    
    ```python
    # 재귀 방식으로 구현한 DFS
    def dfs(graph, v, visited):
        # 현재 정점을 방문
        visited[v] = True
        print(v, end=" ")
    
        # 현재 정점과 인접한 정점들 중 방문하지 않은 정점 방문
        for neighbor in graph[v]:
            if not visited[neighbor]:
                dfs(graph, neighbor, visited)
    
    # 그래프와 호출 예시
    graph = {
        0: [1, 2],
        1: [0, 3, 4],
        2: [0],
        3: [1],
        4: [1]
    }
    visited = [False] * 5  # 5개의 정점에 대해 방문 여부 기록
    dfs(graph, 0, visited)
    
    # 스택을 이용한 반복 방식으로 구현한 DFS
    
    def dfs(graph, start):
        visited = [False] * len(graph)  # 방문 여부 기록
        stack = [start]  # 시작 정점은 스택에 넣고 시작
    
        while stack:
            v = stack.pop()  # 스택에서 정점을 꺼냄
            if not visited[v]:  # 방문하지 않은 정점만 방문
                visited[v] = True
                print(v, end=" ")
    
            # 인접한 정점 중에서 방문하지 않은 정점만 스택에 넣음
            for neighbor in reversed(graph[v]):  # LIFO 순서로 탐색
                if not visited[neighbor]:
                    stack.append(neighbor)
    
    # 그래프와 호출 예시
    graph = {
        0: [1, 2],
        1: [0, 3, 4],
        2: [0],
        3: [1],
        4: [1]
    }
    
    dfs(graph, 0)
    
    ```
    

- 너비 우선 탐색(BFS)
    - 시작 정점을 먼저 방문하고, 시작 정점과 연결된 모든 정점을 차례대로 방문
    
    ```python
    # 자료구조 큐를 활용
    
    from collections import deque
    
    def bfs(graph, start):
        visited = [False] * len(graph)  # 방문 여부 기록
        queue = deque([start])  # 시작 정점을 큐에 넣고 시작
        visited[start] = True  # 시작 정점 방문 처리
    
        while queue:
            v = queue.popleft()  # 큐에서 정점 하나를 꺼냄
            print(v, end=" ")
    
            # 인접한 정점 중 방문하지 않은 정점 큐에 넣음
            for neighbor in graph[v]:
                if not visited[neighbor]:
                    visited[neighbor] = True
                    queue.append(neighbor)
    # 그래프와 호출 예시
    graph = {
        0: [1, 2],
        1: [0, 3, 4],
        2: [0],
        3: [1],
        4: [1]
    }
    
    bfs(graph, 0)
    
    ```
    
- 재귀 알고리즘
    - 자기 자신을 호출하는 방식으로 문제를 해결하는 알고리즘
    - 문제를 더 작은 문제로 나누어 해결하는 방식
    - 피보나치 수열 구현에 활용
        
        ```python
        def fibonacci(n):
        	if n <= 2:
        		return 1
        	return fibonacci(n-1) + fibonacci(n-2)
        	
        ```
        
- 그리디 알고리즘
    - 매 순간 최적의 선택을 하는 방법으로 문제를 해결하는 알고리즘
    - ex) 동전 교환, 최소 신장 트리(주어진 그래프에서 모든 노드를 연결할 수 있는 트리 구조를 찾고, 그 드리의 간선들의 가중치 합이 최소가 되도록 만드는 문제)
    - 다익스트라 알고리즘: 단일 출발지 최단 경로 문제 해결. 가중치가 있는 그래프에서 동작하며, 그래프의 모든 노드에 대해 최단 경로를 계산
- 동적 프로그래밍(DP)
    - 복잡한 문제를 작은 부분 문제로 나누어 해결하고, 그 결과를 저장하여 중복 계산을 방지하는 알고리즘 설계 기법
    - 메모이제이션: 상향싱 접근 방식. 재귀 사용하여 문제 풀고 중간 계산 결과 저장해서 중복 계산 방지
    - 타뷸레이션: 하향식 접근 방식. 반복문을 통해 작은 부분 문제부터 차례로 풀어 문제 해결

### 자료구조

프로그램에서 쉽게 이용할 수 있도록 구성된 데이터 간의 논리적인 관계

- 배열
    
    같은 자료형의 데이터를 순서대로 나열한 구조
    
    - 1차원 배열
        - 인덱스를 하나만 사용하는 배열
        - 1차원 배열의 크기를 구하는 공식
    - 2차원 배열
        - 두 개의 인덱스를 사용하는 배열
        - 2차원 배열의 크기를 구하는 공식: (n-a+1) x (m-b+1)
- 연결리스트
    
    각 데이터들을 포인터로 연결하여 관리하는 구조
    
    떨어진 곳에 존재하는 데이터를 화살표로 연결해서 관리하는 데이터 구조이다.
    
    - 단순 연결리스트
    - 이중 연결리스트
- 스택
    - 선입후출
    - 데이터의 삽입과 삭제가 한 쪽 방향에서만 일어나는 구조
- 큐
    - 선입선출
    - 데이터가 한 쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조
    - 우선순위 큐
        - 각 요소가 우선순위를 가지며, 큐에서 데이터를 꺼낼 때 우선순위가 높은 요소부터 꺼내는 자료구조
        - 삽입할 때마다 배열을 정렬하여 우선순위가 높은 값이 먼저 나가도록 구현. 다만, 매번 삭제하거나 삽입 시 배열을 정렬해야 하기 때문에 비효율적이다
        - 우선 순위가 가장 낮은 최소힙, 높은 최대힙 2가지로 구현할 수 있다
        
        ```python
        # 최소힙
        import heapq
        
        # 빈 우선순위 큐 생성
        pq = []
        
        # 요소 삽입 (우선순위, 데이터 형식으로 삽입)
        heapq.heappush(pq, (2, "task 2"))
        heapq.heappush(pq, (1, "task 1")) #우선순위가 가장 작음
        heapq.heappush(pq, (3, "task 3"))
        
        # 우선순위가 가장 작은 요소 꺼내기 (최소 힙이므로 가장 작은 우선순위가 먼저 나옵니다)
        priority, task = heapq.heappop(pq)
        print(f"Task with priority {priority}: {task}")
        
        ```
        

- 그래프
    - 정점과 간선으로 구성된 자료구조
    - 그래프의 종류
        - 무방향 그래프: 방향성이 없는 간선으로 이뤄진 그래프
        - 방향 그래프: 방향성이 있는 간선으로 이뤄진 그래프
        - 완전 그래프: 모든 정점 사이에 간선이 있는 그래프
        - 가중 그래프: 간선마다 가중치를 부여한 그래프
        - 다중 그래프: 두 정점을 연결하는 다중 간선
        - 유사 그래프: 다중 간선과 루프를 포함
        - 루프: 자기를 연결한 정점
    - 인접 또는 이웃: 무방향 그래프에서 간선으로 연결된 두 정점
    - 차수: 정점에 접한 간선의 수
    - 인접리스트, 인접행렬: 그래프에서 나타난 정점들 사이의 관계를 나타내는 자료형
    - 근접행렬: 정점과 간선의 관계를 나타내는 행렬
    - 경로: 간선을 연결하는 정점들을 나열한 것

- 트리
    - 배열이나 연결 리스트를 이용해서 구현할 수 있다
    - 서브 트리: 아래에 있는 노드들이 다시 트리가 되는 구조
    - 부모 노드: 임의의 노드 바로 위에 있는 노드
    - 자식 노드: 임의의 노드 바로 아래에 있는 노드
    - 형제 노드: 같은 부모 노드를 가지는 노드
    - 레벨: 루트 노드에서 임의의 노드까지 방문한 노드의 수
    - 깊이: 트리의 최대 레벨
    - 이진 트리
        - 모든 노드들의 자식 노드가 두 개 이하인 트리
        - 왼쪽 서브 트리와 오른쪽 서브 트리로 구분
        - 배열이나 연결리스트를 이용해서 구현할 수 있음
        - 완전 이진 트리: 이진 트리 중에서 단말 노드를 제외한 나머지 노드가 모두 두 개의 자식 노드를 가지고 있는 트리
        - 이진 트리 순회
            - 전위 순회: 루트 → 왼쪽 → 오른쪽
            - 중위 순회: 왼쪽 → 루트 → 오른쪽
            - 후위 순회: 왼쪽 → 오른쪽 → 루트
        - 이진 탐색 트리
            - 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 작고, 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 큰 구조
        - AVL 트리: 균형 이진 탐색 트리, 균형 인수는 왼쪽 서브트리의 높이와 오른쪽 서브트리의 높이의 차이
        - 레드-블랙 트리: 각 노드에 색깔을 부여하여 트리의 균형을 유지하는 자가 균형 이진 탐색 트리. 높이 균형을 유지하기 위한 조건들을 따르며 AVL 트리보다 구현이 간단함. 삽입/삭제 성능이 중요한 경우에 유리
    - 힙
        - 완전 이진 트리를 기반으로 하는 자료구조로, 우선순위 큐 구현에 주로 사용
        - 최소 힙: 각 부모 노드의 값이 자식 노드들의 값보다 작거나 같아야 함. 최소 힙의 루트 노드는 항상 가장 작은 값을 가짐.
        - 최대 힙: 각 부모 노드의 값이 자식 노드들의 값보다 크거나 같아야 함.
        - 힙 정렬: 힙을 이용하여 배열을 오름차순 또는 내림차순으로 정렬. 힙 정렬의 시간 복잡도는 O(n log n)
- 해시테이블
    - 키를 사용하여 데이터를 빠르게 검색할 수 있도록 설계된 자료구조
    - 해시테이블은 일반적으로 배열을 기반으로 하며, 키를 해시 함수를 통해 인덱스로 변환하여 데이터를 저장하고 검색