# TIL: 전처리기, 모듈화

## 학습 날짜: 2025년 3월 24일

## 오늘 배운 내용 정리

### 1. C언어 전처리

- C언어에서 전처리기는 컴파일 전에 소스 코드에 특정 작업을 수행하는 기능이다. 이를 통해, 코드의 유지보수성을 높이고 중복을 줄이며 효율적인 컴파일을 할 수 있다.
- 전처리기는 `#` 기호로 시작하는 **전처리 지시문(Preprocessor Directives)** 을 사용하여 코드의 일부를 변경하거나, 다른 파일을 포함하는 등의 작업을 수행한다.

### `#include` – 헤더 파일 포함

`#include` 지시문은 외부 파일(주로 헤더 파일)을 현재 소스 코드에 포함하는 역할을 한다.

```c
#include <stdio.h>  // 표준 입출력 함수 사용을 위해 포함
#include "myheader.h"  // 사용자 정의 헤더 파일 포함

```

- `< >`를 사용하면 **시스템 디렉터리**에서 파일을 찾는다.
- `" "`를 사용하면 **현재 디렉터리**에서 먼저 파일을 찾는다.

### `#define` – 매크로 정의

`#define`은 **매크로를 정의**하여 코드에서 특정 값을 간단한 이름으로 대체할 수 있도록 한다.

```c
#define PI 3.141592  // 상수 정의
#define SQUARE(x) ((x) * (x))  // 매크로 함수 정의

```

- `PI`를 코드에서 사용하면 `3.141592`로 치환된다.
- `SQUARE(x)`는 `x * x`로 대체되며, 복잡한 연산을 간단하게 표현할 수 있다.

### `#undef` – 매크로 해제

이미 정의된 매크로를 삭제할 때 사용한다.

```c
#define TEMP 100
#undef TEMP  // TEMP 매크로 삭제

```

`TEMP`가 더 이상 사용되지 않도록 제거된다.

### `#ifdef`, `#ifndef`, `#endif` – 조건부 컴파일

특정 매크로가 정의되었는지 확인하고, 해당 여부에 따라 코드를 포함할지 결정할 수 있다.

```c
c
#define DEBUG

#ifdef DEBUG
    printf("디버그 모드 활성화\n");
#endif

```

- `DEBUG`가 정의되어 있으면 `printf` 문이 실행된다.
- `#ifndef`는 **매크로가 정의되지 않았을 때** 코드를 포함한다.

### `#if`, `#elif`, `#else`, `#endif` – 조건부 컴파일 (논리 연산)

정수 값이나 매크로 값을 기반으로 조건을 평가하여 특정 코드를 포함할 수 있다.

```c
#define VERSION 2

#if VERSION == 1
    printf("버전 1 실행\n");
#elif VERSION == 2
    printf("버전 2 실행\n");
#elseprintf("알 수 없는 버전\n");
#endif

```

- `#if`는 조건이 `true`일 때만 해당 코드를 포함한다.
- `#elif`는 `else if`와 같은 개념이다.
- `#else`는 위 조건이 모두 거짓일 때 실행된다.

### `#pragma` – 컴파일러 지시문

컴파일러에 특정 설정을 적용할 때 사용한다.

```c
#pragma once  // 헤더 파일이 중복 포함되지 않도록 설정
```

- `#pragma once`는 같은 헤더 파일이 여러 번 포함되는 것을 방지한다.
- `#pragma warning(disable: 4996)` 같은 지시문을 사용해 특정 경고를 무시할 수도 있다.

C 프로그램의 전처리기는 다음과 같은 단계로 실행된다.

1. **전처리 단계** – `#include`, `#define` 등 전처리 지시문이 적용됨.
2. **컴파일 단계** – 전처리된 코드를 기계어로 번역.
3. **어셈블 단계** – 기계어 코드가 어셈블리 코드로 변환됨.
4. **링크 단계** – 여러 개의 오브젝트 파일을 하나의 실행 파일로 연결.
