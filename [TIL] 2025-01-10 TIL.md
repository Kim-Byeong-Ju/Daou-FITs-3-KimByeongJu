# TIL: CS실습

## 학습 날짜: 2025년 1월 10일

## 학습 중 어려움을 느낀 부분

네트워크에서 중요한 개념들이 반복해서 등장하는데 머릿 속에서 완벽히 그려지지 않았음. 쉽게 풀어 설명하는 자료들을 참고하여 개념 완벽 이해

## 어려움을 해결한 과정

- 프로토콜이란? 쉽게 풀어서 설명하자면 **통신 규약**이다. 컴퓨터나 전자 기기 간 원활한 통신을 위해서 지키기로 약속한 것.
    - 프로토콜의 종류: 네트워크 계층(IP), 전송계층(TCP), 응용계층(HTTP, HTTPS, SMTP, SSH, TLS, FTP)
- 라우터 - 여러 가지 네트워크를 묶어서 교통정리를 해주는 장비를 라우터라 부른다
- TCP/IP 모델
    ![image (1)](https://github.com/user-attachments/assets/f0106f6e-7792-42fa-be1f-a610cc26c0bb)
    
    - 물리 계층(이더넷 케이블, 광섬유) → 데이터링크 계층(와이파이, 이더넷) → 네트워크 계층(IP) → 전송 계층(TCP, UDP) → 응용 계층(WWW, FTP, Email, telnet)

## 오늘 배운 내용 정리

### 컴퓨터 통신

- 컴퓨터 네트워크: 컴퓨터 여러 대가 데이터를 주고 받는 통신 시스템
    - 종류
        - 근거리 네트워크(LAN): 짧게는 수 미터에서 길게는 1KM내의 컴퓨터 연결
            - LAN 토폴로지: 컴퓨터를 연결하는 LAN 구조, 위상 배치에 따라 3가지로 나눔
                - 스타형: 중앙에 위치한 한 장치에 모든 장치 연결, 항상 중앙 장치를 통해 데이터 전송이 이루어짐.
                - 링형: 장치들을 원형으로 연결, 데이터 전송을 위한 토큰 사용
                - 버스형: 하나의 통신 회선에 장치 연결, 데이터를 목적지 주소와 함께 버스에 실어 보냄. 규모가 어느 정도 있을 때 사용함.
                - 전체적으로 메인 장치 or 회선에 문제가 발생하면 네트워크 연결에 문제가 생기는 단점이 존재함. Failure에 대한 대비가 필요함!
        - 장거리 네트워크(WAN): 국가 전체 or 국가 간 연결. 100KM 이상
        - 중거리 네트워크(MAN): 대도시 간 연결
        - 개인 네트워크(PAN): 가까운 주변 장치 연결
        - 초근거리 네트워크(NFC): 수 센티미터 거리 통신
- 가상 사설망(VPN): 인터넷과 같은 공용 네트워크를 통해 사설 네트워크에 안전하게 연결할 수 있도록 하는 기술. 사용자가 원격으로 연결된 네트워크에 안전하게 접근할 수 있게 해주며, 데이터가 암호화되어 전송되기 때문에 정보 유출이나 해킹으로부터 보호
- 통신 방향에 따른 분류
    - 단방향 통신: 모스 부호, 방송
    - 양방향 통신: 대부분의 통신 시스템
    - 반양방향 통신: 무전기
- 통신 대상에 따른 분류
    - 유니캐스트: 일대일 연결
    - 브로드캐스트: 보내는 기기 한 대와 불특정 다수 연결
    - 멀티캐스트: 보내는 기기 한 대와 받는 기기 여러 대 연결
- LAN 계층과 인터넷 계층
    - 각 계층 → 물리적 연결 계층 → 프로토콜이 있는 계층
- 각 계층의 역할
    - 물리 계층: LAN에서 통신기기의 연결 담당
    - 데이터 링크 계층: 인접 기기 간 오류 없이 데이터 전송하도록 제어
    - 네트워크 계층: 여러 LAN을 하나로 묶어서 인터넷 만드는 핵심 계층
    - 전송 계층: 네트워크 계층에서 목적지까지 오면서 생기는 오류 보정
    - 응용 계층: 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층
- 계층의 독립성: 각 계층은 자신보다 상위 계층의 작업을 알지 못함. 각 계층은 같은 계층끼리만 통신
- OSI 7 Layer
    - 물리 → 데이터링크 → 네트워크 → 전송 → 세션 → 표현 → 응용
- 헤더: 데이터 전송을 위해 추가되는 자료구조. 상위 계층으로 전송할 때, 헤더를 떼고 보냄
- 패킷: 네트워크 계층에서 다루는 데이터 단위
- ***프로토콜이란***? 컴퓨터 또는 전자 기기 간의 원활한 통신을 위해 지키기로 약속한 규약. **프로토콜**에는 신호 처리법, 오류 처리, 암호, 인증, 주소 등을 포함한다.

- TCP/IP 모델
    - 현재 가장 많이 사용되는 네트워크 계층 모델
    - 로컬 네트워크 → IP → TCP → HTTP, FTP
    - TCP/IP는 인터넷의 기초 프로토콜, 네트워크로 연결된 컴퓨터 간에 메시지를 안정적으로 전달하는 데 사용됨
    - IP: 인터넷 프로토콜. 네트워크 사이에서 데이터를 주고 받는 규칙을 정한 프로토콜
    - TCP: 데이털르 전송할 때 생기는 오류를 바로잡고, 데이터를 목적지 프로그램에 전달
    - 물리 계층: 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜. 통신선은 길어질수록 신호의 세기가 감소
    - 데이터 링크 계층: 같은 네트워크 내에서 패킷을 물리적 주소로 전송.
        - 프레임: 데이터 링크 헤더 + 패킷
        - 전송 방식: 이더넷(유선 LAN에서 널리 사용되는 데이터링크 프로토콜, MAC 주소 사용해 장치 간 데이터 전송), 와이파이(무선 네트워크에서 사용되는 프로토콜)
- MAC 주소
    - 데이터링크 계층에서 사용되는 물리적 주소
    - 네트워크 인터페이스 카드나 네트워크 장치에 고유하게 할당
    - 48비트 길이로 12자리의 16진수 표현
    - cmd에서 ipconfig/all 명령어 입력하면 “물리적 주소”로 MAC 주소 확인할 수 있음
- 오류를 처리하는 방식
    - 데이터 중복 전송: 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
    - 중복 정보 전송: 전송할 데이터에 대한 중복 정보를 데이터와 함께 보내는 방법
- 오류 처리 코드
    - 오류 탐색: 패리티 비트, 체크섬, CRC코드 ⇒ 오류 찾기만 가능
    - 오류 보정 허밍 코드 ⇒ 오류 찾고 보정까지. 단, 오버헤드가 크다
    - 수직 중복 검사
        - 데이터에 대한 패리티 비트라는 중복 정보를 구해 데이터와 함께 수신측으로 전송
        - 패리티 비트: 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식. 오류가 짝수 개 발생하면 오류를 찾지 못함
    - 세로 중복 검사
        - 데이터를 행렬의 형태로 구성해 오류 검출
    - 순환 중복 검사
        - 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함
    - 체크섬
        - 보내려는 데이터를 일정 크기로 자르고, 이를 모두 더하여 코드를 만듦
- 네트워크 계층
    - 데이터를 목적지까지 라우팅하고 전달하는 역할
    - 라우팅: 어떤 길로 패킷을 보낼지 결정
    - 라우터: 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
    - IP 주소: 인터넷에 연결된 여러 장치를 식별하는 번호
        - 4바이트로 구성
        - 네 개의 10진수로 구성. 각 10진수는 여덟 자리 2진수를 의미
        - 총 2의 32승만큼 주소 가능
        - IP주소는 네트워크를 통해 장치를 찾고, MAC 주소는 로컬 네트워크에서 해당 장치와 직접 통신
        - 왼쪽부터 2바이트는 네트워크 주소, 나머지 2바이트는 호스트 주소
        - 한 네트워크에 포함된 모든 장치들은 같은 네트워크 주소를 갖지만, 한 네트워크 내의 호스트 주소는 모두 다름
        - 클래스: 주어진 IP주소의 첫번째 자리, A~E까지 다섯 가지 클래스로 구분
        - 서브넷 마스크: IP네트워크에서 서브넷을 나누기 위해 사용. 32비트 이진 숫자로 구성되며, IP 주소와 유사한 형식
            - 네트워크 주소와 호스트 주소 구분
            - 처음 24비트가 네트워크 주소, 나머지 8비트가 호스트 주소
        - IPv4: 32비트 주소 체계 사용
            - 0.0.0.0 ~ 255.255.255.255까지 범위에서 주소를 할당할 수 있음
            - 기기가 움직이면 지역 코드가 변경되어 IP주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
        - IPv6: 128비트 주소 체계 사용
            - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
        - IP 헤더: 20바이트 크기
        - IP 주소 유형
            - 사설 IP 주소: 내부 네트워크에서 사용되며, 공인 IP 주소와 겹칠 수 없음. 사설 IP 주소는 외부 인터넷과 직접 통신할 수 없으며, NAT 기술을 통해 공인 IP로 변환되어 인터넷과 연결
            - 공인 IP 주소: 인터넷에서 고유한 주소로, 전 세계적으로 유일. ISP가 할당
                - ex) 8.8.8.8 → Google의 DNS 서버 주소
    - DNS: 도메인 네임 시스템
        - 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버
        - 도메인 네임: IP 주소에 이름을 부여한 것 ex) 웹 브라우저에서 IP 주소 대신 도메인 이름 입력해서 접속 www.daou.com
        - 도메인 네임 규칙: 마침표로 구분하여 계층화, 오른쪽에 있는 이름이 상위 계층
        - DNS 서버의 계층 구조
            - 하위 계층의 도메인 이름에 대한 정보를 바로 위의 상위 계층의 DNS 서버에서 등록하여 관리함
            - 루트 DNS → 국가 DNS → 지역 DNS
    - 라우팅 테이블
        - 데이터를 어디로 보낼지 나타내는 표
    - 게이트웨이
        - 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
        - 라우터는 주로 같은 네트워크 내의 데이터를 관리하고, 게이트웨이는 다른 네트워크와 연결하는 기능
    - 허브
        - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
        - 네트워크에 연결된 장치들 간에 데이터를 모든 장치로 전달
        - 소규모 네트워크, 기본 용도로만 사용
    - 스위치
        - 네트워크 장치들 간에 데이터 프레임을 전송하는 지능형 장비
        - 데이터를 필요한 장치로만 전송, 네트워크 트래픽을 효율적으로 관리
        - 허브는 모든 노드가 10mbps의 속도를 나눠서 쓰고, 한 번에 한 노드만이 보낼 수 있음. 스위치는 각각의 노드가 10mbps의 속도를 가지고, 여러 개의 노드에서 동시 통신이 가능한 발전된 장비이다.
    - 동적 라우팅 프로토콜
        - AS:  인터넷은  각  기관이나  단체가  독립적으로  운영하는  네트워크들이  라우터에 의해  연결된  복잡한  네트워크이다. 이와  같이  독립적인  네트워크를  자율  시스템(AS: Autonomous System)이라고  부른다.
        - RIP: 거리 벡터 기반의 라우팅 프로토콜, 목적지까지의 거리를 hop 수로 측정.
        - OSPF: 링크 상태 라우팅 프로토콜로 계층적 라우팅을 지원, 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
        - BGP: 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜, 서로 다른 AS 간에 라우팅 정보 교환, 경로 벡터를 이용하여 네트워크 간의 데이터 경로 결정
    
    - 전송 계층
        - 데이터 전송의 정확성과 순서를 보장하는 계층
        - 네트워크 계층에서 올라온 데이터를 정리하고 데이터를 프로그램에 전달
        - TCP는 일련번호를 매겨 패킷 전송 → 패킷 누락, 순서 뒤바뀜 방지
        - 전송 과정
            - TCP 프로토콜이 데이터를 패킷으로 쪼갠다 → 패킷이 라우터를 통해 이동 → TCP 프로토콜이 패킷을 조합함
        - TCP(Transmission Cotrol protocol)
            - 송신측과 수신측의 연결이 이루어진 것을 확인한 다음에 데이터를 전송하는 프로토콜 신뢰성이 요구되는 데이터 전송 시 사용
            - 전송 시 데이터는 TCP 계층을 거쳐 IP 계층으로 전달됨
            - TCP 헤더
                - 크기 20바이트
                - 제어 비트
                    - 송신측과 수신측 통신
                    - SYN 비트를 1로 하면 연결을 요청하는 것
                    - ACK 비트를 1로 하면 ‘Yes’라고 응답하는 것
            - TCP 데이터 분할
                - TCP는 전송할 데이터가 큰 경우 데이터를 적절한 크기로 분할하여 각각의 분할된 부분에 TCP 헤더를 추가
        - UDP(User Datagram Protocol)
            - 송신측과 수신측의 연결 과정을 거치지 않고 일방적으로 데이터를 보내고 수신측으로부터 응답도 따로 받지 않는 프로토콜
            - 신뢰성이 요구되지 않고, 간단한 데이터를 빠른 속도로 전송할 때 사용
            - 데이터가 사라지거나 뒤바뀔 수는 있지만, 처리 속도가 매우 빠름
            - UDP 헤더
                - 크기 8바이트
        - 포트
            - 응용 프로그램 구분을 위해 전송 계층이 사용하는 주소
            - 포트 번호: 수신측에서 동작하는 여러 애플리케이션 중 데이터를 수신할 애플리케이션을 식별하는 번호
            - 잘 알려진 포트
                - HTTP → 80번 포트
                - HTTPS → 443번 포트
                - MYSQL → 3306번 포트
    - 응용 계층
        - 사용자가 직접 상호작용하는 네트워크의 최상위 계층. 사용자 요청을 처리
        - WWW
            - 인터넷에서 정보를 쉽게 찾을 수 있도록 제공하는 서비스
            - HTTP: 웹에서 데이터를 전송하기 위한 비보안 프로토콜
            - HTTPS: 보안을 추가한 프로토콜. SSL 또는 TLS 암호화를 사용하여 데이터 전송을 보호
        - SSL(보안 소켓층)
            - 웹 클라이언트와 서버 사이의 보안 연결
            - 정보 암호화
            - 인터넷으로 정보 전송
        - 암호화
            - 맞는 해독키가 없으면 읽을 수 없도록 파일의 내용을 바꿔놓는 것
            - 공중키 암호화: 두 개의 키를 이용하는데, 모든 사람을 위해서는 공중키를, 암호화된 정보를 받는 사람을 위해서는 개인키를 사용
        - TLS: 인터넷을 통한 통신을 안전하게 보호하는 프로토콜
            - 데이터 기밀성, 무결성, 인증을 제공하여 인터넷 상의 데이터 전송을 암호화
            - SSL의 후속 프로토콜로, SSL의 보안 취약점을 개선하고 더 강력한 암호화와 보안을 제공
            - 웹 브라우징, 이메일, VoIP 및 기타 인터넷 서비스에서 보안을 강화하는 데 사용됨
        - 텔넷: 원격지의 컴퓨터를 인터넷을 통해 접속하여 자신의 컴퓨터처럼 사용할 수 있는 원격 접속 서비스
        - FTP: 인터넷을 통해 컴퓨터 간 파일 송수신을 위한 프로토콜. 파일 업로드, 다운로드 가능
        - SSH: 네트워크 상에서 컴퓨터나 서버와 안전하게 통신하고 원격으로 관리할 수 있는 프로토콜.
            - 유닉스 시스템 셸에 원격 접속하기 위해 사용하던 텔넷은 암호화가 이루어지지 않아 계정 정보가 탈취될 위험이 높으므로, 여기에 암호화 기능을 추가하여 1995년에 나온 프로토콜
            - 암호화된 연결을 사용하여 주로 보안이 중요한 환경에서 원격 로그인, 파일 전송, 그리고 다른 원격 명령어 실행 등을 위해 사용
        - E-mail: 인터넷을 통해 편지를 교환하는 서비스
            - SMTP: 이메일 전송에 사용되는 프로토콜
            - POP3: 이메일 수신에 사용되는 프로토콜
            - IMAP: POP3와 비슷하지만, 이메일을 서버에서 읽고 관리하는 데 사용하는 프로토콜
    - 방화벽
        - 네트워크 보안을 강화하기 위해 네트워크 트래픽을 제어하는 장치 또는 시스템
        - 소프트웨어, 하드웨어 2가지 측면에서 보호
        - 패킷 필터링을 사용하여 트래픽을 검사하고, 설정된 규칙에 따라 허용하거나 차단
        - 침입 탐지 및 차단 시스템 포함하는 등 응용 계층에서 고급 보호 기능을 제공함
        - 악의적인 공격이나 불법 접근으로부터 시스템 보호
