# TIL: JAVA 기초

## 학습 날짜: 2025년 2월 4일

## 학습 중 어려움을 느낀 부분

> **this 키워드의 개념 및 활용**
> 

JAVA의 this 키워드는 인스턴스화 되었을 때 자기자신의 메모리 주소를 담고있는 키워드이다.

**this 키워드의 세 가지 역할**

① 자기 자신의 메모리를 가르킨다.
② 생성자에서 다른 생성자를 호출할 경우 사용한다.
③ 인스턴스 자신의 주소를 반환할 때 사용한다.

## 오늘 배운 내용 정리

### 1. JAVA 언어의 특징

- 플랫폼 운영체제에 독립적
- 자바는 컴파일러를 통해서 바이트코드로 변경
    - 이렇게 변경된 바이트코드를 JVM이 읽어들여서 머신코드로 변경해서 실행
    - .java → .javac(java compiler) → .class → (JVM)
    - 일반적으로, 다른 언어들은 하이레벨 랭귀지로 작성한 코드를 컴파일러를 통해서 머신코드로 변경
- JVM, JRE, JDK 차이
    - JVM: bytecode를 읽어들여서 실행하는 실행 주체
    - JRE(Java Runtime Environment): 자바실행환경 ⇒ JVM + JAVA 클래스 라이브러리
    - JDK(Java Development Kit): 자바개발도구 ⇒ JRE + JAVA 개발 도구(javac, java, javadoc, etc… )
    

### 2. 객체지향 언어 vs 절차적 언어

- **절차적 언어**
    - 프로그램의 기본 단위가 함수
    - 각 기능으로 프로그램을 세분화
    - 더이상 분할할 수 없는 단위기능이 나오게 된다
    - 단위 기능을 함수로 구현
    - 장점
        - 요구사항 분석이 쉽고, 누가하든 간에 설계가 거의 똑같다’
        - 프로그램을 빠르고 쉽게 비용 절약하며 구현이 가능하다
    - 단점
        - 유지보수가 힘들다
    - 대표적으로, C언어
- **객체지향 프로그래밍**
    - 유지보수 문제를 해결하기 위해 등장
    - 확장성을 고려하며 프로그래밍 해야 함
    - 현실세계의 문제를 프로그램으로 옮긴 것
    - 구성요소 모델링, 구성요소 간 데이터 주고 받는 방식 설정
    - 장점
        - 유지보수 쉽다
    - 단점
        - 설계가 힘들다
    - 추상화를 거쳐 나온 여러가지 정보를 **상태**와 **행위**로 구분
        - 상태는 변수(fields)로, 행위는 함수(method)로 표현
        - field와 method는 class라는 단위로 묶여서 표현
        - 즉, 자바 프로그램은 class의 집합으로 표현될 수 있다!
        - 코드로 표현할 때, 클래스 1개 당 파일 1개
        - 그러면서 클래스 이름을 파일이름으로 명명
        - 클래스 naming convention은 Pascal case
    - 클래스로부터 저장 공간을 생성 → 저장 공간을 이용해서 프로그래밍 진행!
        - 이 저장 공간을 instance라고 부른다
    - 클래스의 의미
        1. 객체 모델링의 수단
        2. ADT(Abstract Data Type)
            - 클래스는 인스턴스를 어떻게 만드느냐에 따라서 다르게 저장됨. 데이터 타입의 역할을 수행함
            - 고정된 데이터 타입이 아니기 때문에 추상 데이터 타입이라고 한다
        3. instance를 생성하는 수단

### 3. Java Data Type

- Java는 Strong type 언어이다.
- 대소문자 구분하고, 변수 길이의 제한이 없다
- 예약어로는 변수를 만들 수 없다
- 숫자로 시작하는 변수는 만들 수 없다
- 특수문자는 _와 $만 허용
- 클래스 이름은 pascal case, 일반 변수 이름은 camel case
    - ex) myfriend
        - pascal: MyFriend
        - camel: myFriend
- java의 데이터 타입은 크게 2가지로 나뉜다
    - primitive data type(8가지)
        - 해당 타입으로 선언된 변수 안에 직접 데이터가 저장
        - 스택 또는 힙 특정 메모리에만 저장되는 것이 아니다!
        - 직접 값을 저장하는지, 주소를 참조하는지를 기준으로 primitive와 reference가 나뉘는 것
        - 정수형: byte(1 byte), short(2 byte), int(4 byte), long(8 byte)
        - 실수형: float(4 byte), double(8 byte)
        - 문자형: char(2 byte)
        - 논리형: boolean(1 byte)
    - reference data type(class)
        - 클래스로 선언된 변수를 지칭
        - 이 타입으로 선언된 변수 안에는 메모리 주소(실제로는 해쉬값)가 저장된다
        - class는 ADT, 추상 데이터 타입이다
    - 변수 이외에 상수도 존재한다
        - final 키워드로 상수를 표현(상수 만들 때만 사용하는 건 아님)
        - `final int MY_AGE = 20;`
    - 형 변환: 하나의 데이터 타입이 다른 데이터 타입으로 변환하는 걸 의미
        - 묵시적 형변환(implicit)
            - `double k = 10 + 20.0;`
        - 명시적 형변환(casting)
            - `int myData = (int)3.14159265358979; // casting`

### 4. JVM 메모리 구조

- JVM 동작 원리
    
    ```sql
    package mypackage.com;
    public class Customer {
        // int는 4byte 정수형
        int age;
        public Customer() {
        }
    
        public void myprint() {
            System.out.println("안녕안녕!");
        }
    
        public static void main(String[] args) {
            int age = 100; // 지역변수
    
            // class로부터 instance를 생성하기 위한 keyword
            // new라는 키워드 뒤에는 반드시 생성자가 온다
            Customer c = new Customer();
        }
    }
    
    ```
    
    - 다음과 같은 코드를 실행했을 때, 발생하는 일
        - Method Area에 Customer Class 전체가 저장된다.
        - JVM이 class 내에 main이 있는지 확인하고 실행
            - main 없으면 에러 발생
        - main 메서드가 호출되면 Stack 메모리에 공간이 생긴다
        - 지역변수가 stack 메모리 공간에 저장된다
        - Customer라는 클래스를 기반으로 인스턴스가 만들어지면, heap 메모리에 저장된다
            - main 메서드의 stack 메모리 내에서 생성된 인스턴스는 heap 메모리 주소 값을 가리킴
        - 실행이 종료되면 stack 메모리가 비워진다
            - 이 때, heap에 저장된 instance 메모리는 garbage collector에 의해서 날라간다
            - 이후, Method Area도 날라가고 프로그램이 종료된다
    
- static, this
    
    ```sql
    package mypackage;
    
    public class InstanceTest {
    
        // static field or static variable
        static int a = staticCall("1번");
    
        // instance variable
        int b = staticCall("2번");
    
        // static method
        static public int staticCall(String msg){
          System.out.println(msg);
          return 100;
        }
    
        // constructor
        // 기본 생성자는 무조건 작성하는게 좋다
        public InstanceTest(){
            this.b = staticCall("3번");
        }
    
        // entry point
        public static void main(String[] args) {
            System.out.println("4번");
            int c = staticCall("5번");
            InstanceTest d= new InstanceTest();
        }
    }
    
    => 1번 4번 5번 2번 3번
    ```
    
    - **static**
        - 변수 앞에 **static**을 붙이면 생성자를 만들지 않아도 사용할 수 있다
        - 이 때, static 변수는 메모리 공간에서 Method Area에 저장된다
        - 만약, staticCall 메스드가 static으로 만들어지지 않았다면, static 변수 a는 인스턴스가 만들어지기 전에는 사용될 수 없기 때문에 논리적으로 에러가 발생
        - int a는 static이라서 Method Area에 바로 값이 저장되었지만, int b는 메서드에 대한 정보만 올라가 있는 상태이다.
    - **this**
        - 현재 참조하고 있는 instance
        - 시점에 따라 다르다
    - 2번이 3번보다 빨리 출력되는 이유
        - main에서 new 키워드로 인스턴스 생성
        - heap 공간에 인스턴스 공간이 만들어짐
        - Method Area에 있는 field 값들을 저장(”2번”)
        - 이후 기본생성자 실행(”3번”)

### 5. Overloading

- 같은 이름이나 같은 형태로 여러 개의 기능을 수행하는 경우
- 연산자 오버로딩
    - ex) + 기호
        - 앞과 뒤에 숫자가 나옴
        - 앞과 뒤 둘 중 하나 혹은 둘 다 문자열이면 문자열끼리 더함. 문자로 형변환
- 메서드 오버로딩
- 생성자 오버로딩
    
    ```sql
    public class InstanceTest {
        // constructor
        // 기본 생성자는 무조건 작성하는게 좋다
        // 생성자는 인스턴스를 생성하기 위해서 반드시 호출되고
        // 만들어진 인스턴스를 초기화하는 역할을 한다
        public InstanceTest(){
        }
    
        public InstanceTest(int a){
        }
    
        // entry point
        public static void main(String[] args) {
             InstanceTest d= new InstanceTest();
        }
    }
    
    ```
    
    - 인자를 다르게 주는 등 다른 방식으로 정의함
    - 둘의 차이는 인스턴스 초기화하는 방식이 다름
