# TIL: JAVA 객체지향 상속

## 학습 날짜: 2025년 2월 6일

## 학습 중 어려움을 느낀 부분

<aside>
💡

오늘은 배운 내용이 많아서 용어들의 개념이 헷갈렸다. 오버로딩, 오버라이딩, shadow 개념 정리 비교하며 다시 한 번 복습!

</aside>

- `shadowing`
    
    변수나 필드가 **동일한 이름**을 가질 때, 더 가까운 범위(Scope)의 변수가 우선 사용되어 기존 변수를 가리는 현상을 의미.
    
    ### 🔹 Shadowing의 주요 개념
    
    1. **멤버 변수 vs. 지역 변수**
        - 클래스의 **멤버 변수(필드)**와 **지역 변수(메서드 내부에서 선언된 변수)**가 같은 이름을 가질 경우, 지역 변수가 멤버 변수를 가립니다.
        - 이를 해결하려면 `this` 키워드를 사용하여 멤버 변수를 명확히 지정해야 합니다.
    2. **부모 클래스의 필드 vs. 자식 클래스의 필드**
        - 상속 관계에서 부모 클래스의 필드와 자식 클래스의 필드가 **같은 이름**을 가지면, 자식 클래스에서 정의한 변수가 부모 클래스의 변수를 가립니다.
        - 이를 해결하려면 `super` 키워드를 사용하여 부모 클래스의 변수를 참조할 수 있습니다.
    
    ### 1️⃣ 멤버 변수 vs. 지역 변수
    
    ```java
    java
    복사편집
    class Example {
        int x = 10; // 멤버 변수
    
        void setX(int x) { // 매개변수 x가 멤버 변수 x를 가림 (Shadowing)
            this.x = x;  // this를 사용하여 멤버 변수 x에 접근
        }
    
        void printX() {
            System.out.println("x: " + x);
        }
    }
    
    public class ShadowTest {
        public static void main(String[] args) {
            Example e = new Example();
            e.setX(20);
            e.printX(); // 출력: x: 20
        }
    }
    
    ```
    
    ### 2️⃣ 부모 클래스 필드 vs. 자식 클래스 필드
    
    ```java
    java
    복사편집
    class Parent {
        String name = "부모 클래스";
    }
    
    class Child extends Parent {
        String name = "자식 클래스"; // 부모 클래스의 name을 가림 (Shadowing)
    
        void printNames() {
            System.out.println("name: " + name);         // 자식 클래스의 name
            System.out.println("super.name: " + super.name); // 부모 클래스의 name
        }
    }
    
    public class ShadowTest {
        public static void main(String[] args) {
            Child c = new Child();
            c.printNames();
            // 출력:
            // name: 자식 클래스
            // super.name: 부모 클래스
        }
    }
    
    ```
    

- `overriding`  vs `overloading`
    
    **Overloading (오버로딩)**:
    
    - **정의**: 같은 이름의 메서드를 여러 개 정의하되, 매개변수의 타입, 개수, 순서가 다른 경우를 말합니다.
    - **특징**:
        - 메서드 이름은 같지만 매개변수가 다르기 때문에 컴파일러가 구별할 수 있습니다.
        - 반환 타입이나 접근 제어자는 오버로딩에 영향을 주지 않습니다.
        - 예를 들어, `calculate(int a, int b)`와 `calculate(double a, double b)`는 오버로딩의 예입니다.
    
    **Overriding (오버라이딩)**:
    
    - **정의**: 부모 클래스가 가진 메서드를 자식 클래스에서 재정의하는 것을 말합니다. 즉, 메서드의 내용을 자식 클래스에서 변경할 수 있습니다.
    - **특징**:
        - 상속 관계에서 발생하며, 메서드 이름, 매개변수 타입, 개수, 반환 타입이 모두 같아야 한다.
        - 부모 클래스의 메서드를 자식 클래스에서 덮어씌워서(override) 자신의 동작을 구현할 수 있다.
        - 예를 들어, `run()` 메서드를 자식 클래스에서 오버라이딩하여 다른 실행 내용을 추가할 수 있다.
    
    <aside>
    💡
    
    요약:  오버로딩은 같은 이름의 메서드를 매개변수를 달리하여 여러 개 정의하는 것이고, 오버라이딩은 상속 관계에서 부모 클래스의 메서드를 자식 클래스에서 재정의하는 것이다. Java에서 다형성(polymorphism)을 구현하는 데 중요한 역할을 한다.
    
    </aside>
    

## 오늘 배운 내용 정리

### Java 상속

> **상속과 관련된 constructor 특징**
> 
- constructor는 상속되지 않는다.
- 프로그래머가 명시하지 않아도 constructor가 호출될 때 자동적으로 상위 class의 constructor를 호출한다.
- sub class의 객체를 생성할 경우 먼저 super class의 객체가 만들어 져야 하기 때문에 super class의 constructor가 호출
- 주의할 것은 이때 자동적으로 호출되는 상위 class의 constructor는 인자가 없는 constructor가 호출된다는 것이다.

![image](https://github.com/user-attachments/assets/0676e32f-f232-41f1-8738-8330ecbd0116)

> **method overriding**
> 
- overriding은 부모로부터 물려받은 method의 내용을 하위 class에서 다시 정의하는 것.
- method overriding의 조건
    1. method의 이름이 같아야 한다.
    2. method의 return type이 같아야 한다.
    3. method의 parameter개수와 type이 같아야 한다.
    4. access modifier는 부모의 것과 같거나 더 넓어야 한다.

```java
package inheritance;

public class Student extends Person {

    String dept;

    public Student() {
        // 상위 class의 constructor를 호출
        super();
        System.out.println("Student.Student");
    }

    // method
    public void study() {
    }

    @Override
    public void sleep() {
        super.sleep();
    }
}
```

> **is-a Relationship**
> 
- `Sub class is a Super class`
    - 역은 성립하지 않음
    - ex) 사람은 포유류이다(O) / 포유류는 사람이다(X)
- sub class type을 써야 하는 자리에 super class의 type을 사용해도 된다.
- Type casting

```java
package inheritance;

public class Main {
    public static void main(String[] args) {
        Person s = new Student();
    }
}

```

- 이 때, 객체는 같아도 reference variable이 가리키는 주소가 달라짐. Student 클래스에서만 정의된 필드, 메서드는 사용하지 못함
- **`다형성`** : **하나의 객체가 여러 가지 타입을 가질 수 있다.**

> **this, super, this(), super()**
> 
- `this` : this keyword가 사용된 instance에 대한 reference
- `super` : this와 같은 의미이지만 실제로 가리키는 객체는 상위 클래스의 객체
- `this()` : constructor 내에서 overloading된 다른 constructor를 호출하기 위해 사용
- `super()` : constructor 내에서 상위 class의 constructor를 호출하기 위해 사용

![image (1)](https://github.com/user-attachments/assets/e351a731-c05f-412a-b5f4-2612970e4c2b)

![image (2)](https://github.com/user-attachments/assets/39e81156-2fd5-4b6f-8c77-736472bf7661)

나의 예상: 10번 → 7번 → 2번 → 3번 → 4번 → 1번 → 6번 → 8번 → 5번 → 9번

정답: 2번 → 7번 → 10번 → 1번 → 3번 → 4번 → 6번 → 8번 → 5번 → 9번

java Exam01_InheritanceOrder 실행 → 하위 클래스에 대한 정보를 method Area 올리려고 함 → 상속 받았기 때문에 상위클래스에 대한 정보가 먼저 존재해야 됨 → 상위클래스 정보 method Area 올림(static 필드 2번 출력) → 하위클래스 정보 method Area 올림(static 필드 7번 출력) → 하위클래스의 main 메서드 실행(10번 출력) → 객체 생성/생성자 호출 → 상위클래스의 constructor를 우선 생성해야 하는데, super(100)을 통해 default constructor가 아닌 것을 호출하려 함 → 상위클래스에서 this()를 통해 인자가 없는 생성자를 호출하게 됨 → 이제 초기화 코드를 실행하게 되는데 이는 필드가 먼저 만들어져야 함 → 그래서, a라는 필드가 저장됨(1번 출력) → default 생성자(3분 출력) 초기화 이후 실행(4번 출력) → 하위클래스 생성자로 돌아와서 실행 전에 초기화 진행(6번 출력) → 원래 생성자로 돌아옴(8번 출력)

> **추상화**
> 
- abstraction은 객체들의 공통적인 특징(속성과 기능)을 뽑아내서 이름을 붙이는 것.
- C의 구조체 역시 추상화 ( 자료의 추상화 )
- C의 function, Java의 method 역시 추상화 ( 기능의 추상화 )
- class를 정의하는 과정 그 자체가 추상화.

> **Encapsulation**
> 

- 데이터 구조와 데이터를 다루는 방법을 결합시켜 묶은 것을 지칭.
- class로 부터 파생된 객체가 독립적으로 역할을 수행하기 위해 데이터(field)와 기능(method)을 묶어서 관리
- Encapsulation을 하는 이유: class의 재활용성을 높이고 에러발생을 최소화

> **Final keyword**
> 
- class 앞에 final이 붙었을 경우 : 해당 class를 상속할 수 없음을 의미
- field 앞에 final이 붙었을 경우 : 해당 field의 값을 변경할 수 없음을 의미 ( 상수화 )
- method 앞에 final이 붙었을 경우 : 해당 method를 하위 class에서 overriding할 수 없음을 의미

> **abstract class**
> 
- abstract class의 특징 : abstract class는 instance를 생성할 수 없다
- abstract class를 상속받은 하위 class에서 abstract method를 overriding 한 후 instance 생성

```java
package inheritance;

public abstract class MyClass {

    int kk;

    public MyClass() {

    }

    // 메서드의 정의
    public void printInfo() {
        System.out.println("로직 처리");
    }

    public abstract void myFunc();
}

```

> **polymorphism & dynamic binding**
> 

**`다형성`**은 부모의 객체 타입으로 다양한 형태의 자식 객체를 생성, 사용할 수 있는 것을 의미한다.

![image (3)](https://github.com/user-attachments/assets/7a9afb8d-5d97-41d8-ba74-ccbd3e118b8f)

다형성을 이용하여 상위 데이터 타입으로 객체의 타입을 통일해서 사용

![image (4)](https://github.com/user-attachments/assets/c5248d71-f5d1-4f16-b748-72c35e8814d9)

다형성을 이용해 객체들을 상위 데이터 타입으로 통일하면 프로그램의 처리가 쉬워짐.

![image (5)](https://github.com/user-attachments/assets/7d433f10-95ee-4f99-9c70-1d711181e524)

다음은 동적 바인딩에 대한 내용입니다.

![image (6)](https://github.com/user-attachments/assets/858b21b7-662a-4610-bc28-059f163f29c4)

![image (7)](https://github.com/user-attachments/assets/090618fa-91b5-4f0a-a191-98efec407f87)

다음과 같은 경우 에러가 발생하기 때문에 조심해야 합니다.

![image (8)](https://github.com/user-attachments/assets/c3d28f84-6baf-4aa1-a444-6c3aca79198c)

![image (9)](https://github.com/user-attachments/assets/8e91a602-f2c2-45aa-8753-402615af5c36)

> **interface**
> 

- 동일한 기능에 대해서는 같은 method 명을 사용하도록 강제
- method명은 지정해 주지만, 내부 구현은 각자에 맞게 구현
- interface는 abstract method와 public static final field로 구성되어 있다.
- interface 내부에 선언하는 field는 static final 이다. ( 상수 )
- interface 내부에 선언하는 method는 모두 abstract method 이다.
- interface를 구현하는 class에서 `implements` keyword를 이용하여 구현
- interface간의 상속도 존재하며 `extends` keyword로 상속을 구현
- interface와 interface를 구현한 class 간에도 `is-a relationship`이 성립한다.
- interface는 다중 구현을 허용

![image (10)](https://github.com/user-attachments/assets/58edf174-67e9-49f0-a605-b277bb2dda7a)


### Java 주요 Class

> **1.  Object 클래스**
> 

***모든 객체의 최상위 클래스***

```java
package inheritance;

public class MyObjectClass {

    String name;
    @Override
    public boolean equals(Object obj) {
        MyObjectClass target = (MyObjectClass) obj;
        if (this.name == target.name) {
            return true;
        }
        return false;
    }
    public static void main(String[] args) {
        MyObjectClass a = new MyObjectClass();
        MyObjectClass b = new MyObjectClass();

        a.name = "홍길동";
        b.name = "홍길동";
        System.out.println(a == b);
        System.out.println(a.equals(b));
    }

}
```

```java
package javaexam1.tv;

public class Tv {

    private String name;
    private int price;
    private String description;
    public Tv() {
    }

    public Tv(String name, int price, String description) {
        this.name = name;
        this.price = price;
        this.description = description;
    }

    @Override
    public String toString() {
        return  this.getName() + " " + this.getPrice() + " " + this.getDescription();
    }

    // getter
    public String getName() {
        return this.name;
    }
    public int getPrice() {
        return this.price;
    }
    public String getDescription() {
        return this.description;
    }

    // setter
    public void setName(String name) {
        this.name = name;
    }
    public void setPrice(int price) {
        this.price = price;
    }
    public void setDescription(String description) {
        this.description = description;
    }

}

package javaexam1.tv;

public class TvTest {
    public static void main(String[] args) {

        Tv[] tvArray = new Tv[3];
        tvArray[0] = new Tv("INFINIA", 1500000, "LED TV");
        tvArray[1] = new Tv("XCANVAS", 1000000, "LCD TV");
        tvArray[2] = new Tv("CINEMA", 2000000, "3D TV");

        int sum = 0;
        for (Tv tv : tvArray) {
            System.out.println(tv);
            sum += tv.getPrice();
        }
        System.out.println("가격의 합: " + sum);
    }
}
```
