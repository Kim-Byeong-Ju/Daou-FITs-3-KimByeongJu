# TIL: C 프로젝트

## 학습 날짜: 2025년 3월 27일

## 오늘 배운 내용 정리

### **1. SQL 쿼리 및 업데이트 관련**

- **목표:** `accounts` 테이블의 `interest_rate` 필드를 `HAVINGS` 테이블의 정보에 따라 업데이트.

**문제:** `HAVINGS` 테이블에 데이터가 없는 `accounts` 데이터는 `interest_rate`를 기본 0으로 설정해야 함.

**해결 방법:**

**`MERGE` 문 사용**

- `MERGE INTO`를 사용하여 `accounts` 테이블을 업데이트.
- `NVL`을 사용하여 `HAVINGS` 테이블에 데이터가 없는 경우 기본값을 `0`으로 설정.

```sql
sql
복사편집
MERGE INTO accounts a
USING (
    SELECT a.account_id,
           NVL(ROUND((a.total_amount - h.purchase_price) / h.purchase_price * 100, 2), 0) AS interest
    FROM accounts a
    LEFT JOIN havings h ON a.account_id = h.account_id
) t
ON (a.account_id = t.account_id)
WHEN MATCHED THEN
UPDATE SET a.interest_rate = t.interest;

```

### **2. 코드 관련 에러 및 해결 방법**

- **문제:** `strdup` 관련 오류 발생 (메모리 액세스 위반)
    - **해결:** `strdup` 대신 `malloc`을 사용해 동적 메모리 할당 후 문자열 복사 진행.
    
    ```c
    c
    복사편집
    char* banned_word_copy = (char*)malloc(strlen(rows[i].banned_word) + 1);
    if (banned_word_copy) {
        strcpy(banned_word_copy, rows[i].banned_word);
    }
    
    ```
    
- **문제:** 배열에 금지어를 저장하고 그 값을 다른 곳에서 사용
    - **해결:** `get_banned_words()` 함수에서 `rows[i].banned_word`를 `banned_words` 배열에 저장하는 방법을 구현.
    
    ```c
    c
    복사편집
    banned_words[i] = strdup(rows[i].banned_word);
    
    ```
    

---

### **3. SQL 문법 및 쿼리 활용**

- **SQL 쿼리 작성**
    - `LEFT JOIN`을 사용하여 두 테이블을 결합하고 조건에 맞는 데이터를 선택.
    - `NVL`을 사용하여 `NULL` 값을 대체하여 기본값을 설정.

---
