# TIL: Java 객체지향

## 학습 날짜: 2025년 2월 5일

## 학습 중 어려움을 느낀 부분

> *실습 문제를 풀면서 Java에서는 입력 값을 어떻게 받는지, max 값 구하는 내장 함수가 있는지 등등 기본 기능을 잘 몰라서 시간이 오래 걸렸다.*
> 
- JAVA 입출력
    - 기본적인 데이터 타입들(primitive)을 `SCANNER` 클래스를 활용하여 입력받을 수 있다.
    - SCANNER 활용 시, util 클래스의 scanner 클래스 import를 해줘야 한다.
    - **공백(띄어쓰기) 또는 개행(줄 바꿈)을 기준으로 읽는다.**
    - 예시
        
        ```sql
        import java.util.Scanner;	// Scanner 클래스 호출
         
        public class Main {
        	public static void main(String[] args) {
         
        		Scanner in = new Scanner(System.in);	// Scanner 객체 생성
         
        		byte a = in.nextByte(); 		// byte 형 입력 및 리턴
        		short b = in.nextShort(); 		// short 형 입력 및 리턴
        		int c = in.nextInt(); 			// int 형 입력 및 리턴
        		long d = in.nextLong(); 		// long 형 입력 및 리턴
         
        		float e = in.nextFloat(); 		// float 형 입력 및 리턴
        		double f = in.nextDouble(); 	// double 형 입력 및 리턴
         
        		boolean g = in.nextBoolean(); 	// boolean 형 입력 및 리턴
         
        		String h = in.next(); 			// String 형 입력 및 리턴 (공백을 기준으로 한 단어를 읽음)
        		String i = in.nextLine(); 		// String 형 입력 및 리턴 (개행을 기준으로 한 줄을 읽음)
        	}
         
        }
        ```
        
- `MATH 클래스`
    - 코드에서 수학 연산 처리를 쉽게 할 수 있도록 도와주는 클래스
    - Math 클래스의 다양한 메소드들은 전부 static으로 구현되어 있으므로 따로 객체를 생성하지 않고 사용할 수 있다.
    - 메서드의 종류: 1) abs(), 2) random(), 3) max() min(), etc…
    - 예시
        
        ```sql
        public class Sample
        {
            public static void main(String[] args)
            {
                System.out.println(Math.abs(-10));   // 출력값 : 10
                System.out.println(Math.abs(0));     // 출력값 : 0
                System.out.println(Math.abs(10));    // 출력값 : 10
                System.out.println((int)(Math.random() * 10));     // 0~9 사이 난수 발생
                System.out.println((int)(Math.random() * 100));  // 0~99 사이 난수 발생   
                System.out.println((int)(Math.random() * 1000)); // 0~999 사이 난수 발생
                System.out.println(Math.max(10,100)); // 100
                System.out.println(Math.min(10,100)); // 10
            }
        }
        
        ```
        

## 오늘 배운 내용 정리

---

> **access modifier (접근제어자)**
> 
- `public` : 어디에서나 접근 가능한 field, method, constructor, class 선언에 사용
    - java file 1개 내에 public class는 2개 이상 존재할 수 없다. ***최대 1개***
    
    ```sql
    package aaa;
    
    public class PackageTest {
    
        // instance variable
        int k;
    
        // default constructor(기본 생성자)
        public PackageTest() {
        }
    
        // main method
        public static void main(String[] args) {
            MyClass a = new MyClass();
        }
    
    }
    
    // 하나의 자바 파일 안에 public class는 많아야 1개만 존재할 수 있다.
    //컴파일 했을 때, class 단위로. class 파일(바이트코드)가 생성
    class MyClass {
    
    }
    ```
    
- `protected` : 기본적으로 default와 같지만 상속관계에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언
- `default` : 같은 package내의 다른 class에서 접근 가능한 field, method, constructor, class 선언
- `private` : 다른 **class**에서는 접근할 수 없는 field, method, constructor 선언
- **패키지**
    - 외부로부터의 접근 권한을 제어하는 단위
    - public → 외부 패키지 등 어디에서나 접근 가능
    - default → 내부 패키지에서만 접근 가능

> **static block**
> 

**`static block`은 class가 로드된 후 main thread에 의해서 main method가 호출되기 이전에 실행. 주로 JNI 같은것을 이용할 때 사용됨**

```java
package lecture;

class MyTest {

	int aaa;
	static int bbb = statciCall();
	
	public static int statciCall() {
		System.out.println("static method call()");
		return 100;
	}

	static {
		System.out.println("이것은 Static block의 연습입니다. bbb의 값은 : " + bbb);
	}

	public static void main(String args[]) {
		System.out.println("main() 호출");
	}
}
```

> **Inheritance(상속)**
> 

**`상속이란?` 간단하게, 부모 클래스(Parent class, Super class, 상위 클래스)가 가진 내용을 자식 클래스(Child class, sub class, 하위 클래스)가 물려받는 것**

- Java는 모호성의 문제 때문에 다중상속을 지원하지 않는다. **`단일 상속`**만 지원
- tree 구조로 만들어지게 된다
- Java의 모든 클래스는 상속관계에 있다

![image.png](attachment:58b850f6-6079-439b-b095-3e7e635d362a:image.png)

![image.png](attachment:a226495d-a978-4d26-82a7-4fc257f520b0:image.png)

```sql
package inheritance;

// 상위 클래스
public class Person {

    String name;
    String phone;
    String addr;
    public Person() {

    }

    public void sleep() {

    }

    public void eat() {

    }

}

```

```sql
package inheritance;
// 하위 클래스
public class Student extends Person {

    String dept;

    public Student() {

    }

    // method
    public void study() {
    }
}
```
