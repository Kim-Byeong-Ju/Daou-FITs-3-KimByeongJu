1. FullCalendar에서 월 판별 방식
	•	FullCalendar의 view 객체에는 start와 end 값이 존재한다.
	•	하지만 start는 이전 달의 일부 날짜, end는 다음 달의 일부 날짜를 포함한다.
	•	그러면 FullCalendar는 어떻게 현재 월을 판단할까?
	•	view.title을 확인하면 "2025년 3월" 같은 값이 나온다.
	•	이 값을 파싱하여 현재 월을 얻을 수 있다.

2. JavaScript에서 “YYYY년 M월” 파싱하는 방법

const text = "2025년 4월";
const match = text.match(/(\d{4})년 (\d{1,2})월/);

if (match) {
  const year = parseInt(match[1], 10);
  const month = parseInt(match[2], 10);
  console.log(year, month); // 2025 4
}

✅ 정규식 활용하여 연도와 월 추출 가능.
✅ match()를 이용하면 깔끔하게 year, month 값이 나온다.

3. Git 설정 (git config)

(1) 사용자 정보 설정

git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

✅ --global을 붙이면 모든 프로젝트에 적용.
✅ 특정 프로젝트에서만 설정하려면 --global 제거.

(2) 설정 확인

git config --list
git config user.name
git config user.email

✅ 현재 설정을 확인할 수 있다.

(3) 기본 브랜치 변경 (main으로 설정)

git config --global init.defaultBranch main

(4) 기본 에디터 변경

git config --global core.editor "code --wait"  # VS Code
git config --global core.editor "vim"  # Vim
git config --global core.editor "nano"  # Nano

(5) Git Alias 설정 (단축 명령어 추가)

git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.cm "commit -m"
git config --global alias.last "log -1 HEAD"

✅ 이제 git status 대신 git st만 입력하면 된다!

(6) Git 설정 초기화

rm ~/.gitconfig  # 전역 설정 초기화
rm .git/config   # 특정 프로젝트 설정 초기화

오늘 FullCalendar와 Git 설정을 배우면서 실무에서 바로 적용할 수 있는 개념들을 익혔다.
특히 FullCalendar에서 월을 판별하는 방법과 Git Alias 설정이 유용했다.