# TIL: SQL 고급

## 학습 날짜: 2025년 1월 23일

## 오늘 배운 내용 정리

- 인덱스
    - 데이터베이스의 테이블의 검색 성능을 향상시키기 위해 사용되는 특별한 데이터 구조
    - 도서관의 책 목차와 같은 역할
    - 특정 데이터를 빠르게 찾아갈 수 있는 경로 제공
    - 인덱스 동작 원리
        - 테이블 스캔: 인덱스가 없는 경우 테이블의 모든 데이터를 처음부터 끝까지 읽어야 함. 데이터 양이 많아질수록 성능 저하 발생
        - 인덱스 스캔: 인덱스를 사용하면 필요한 데이터가 저장된 위치를 빠르게 탐색 가능. 데이터 일부만 읽기 때문에 성능 향상
    - B-Tree 인덱스
        - 균형 이진 트리 형태로 데이터 검색에 최적화
        - 데이터가 정렬된 상태로 유지되며, 검색 삽입 삭제 시 효율적
        - 루트 노드 → 중간 노드 → 리프 노드 순서로 탐색
        - 리프 노드에는 실제 데이터 위치 정보 저장
    - Sequential Access
        - 데이터를 순서대로 읽거나 쓰는 방식. 인덱스 Leaf Block을 읽는 방식으로 Table Full Scan
    - Random Access
        - 논리적, 물리적 순서와 무관. 한 건을 읽기 위해 한 Block씩 접근. 인덱스 스캔
    - 인덱스 비용
        - 테이블 Random Access > 수직적 탐색 > 수평적 탐색
    - 인덱스 단점
        - 추가 저장 공간 필요: 인덱스는 테이블 외에도 별도의 공간을 차지
        - 쓰기 작업 성능 저하: INSERT, UPDATE, DELETE 작업 시 인덱스도 함께 갱신되어야 함
        - 인덱스 과다 생성 문제: 너무 많은 인덱스는 관리 비용 증가와 성능 저하를 초래
    - 인덱스 유형
        - 단일 컬럼 인덱스: 하나의 컬럼만 기준으로 인덱스를 생성
        - 결합 인덱스: 두 개 이상의 컬럼을 조합하여 생성
        - 유니크 인덱스: 중복을 허용하지 않는 인덱스
    - 인덱스 확인 및 삭제
        - 함부로 인덱스를 삭제하거나 생성하면 소프트 파싱하는 실행 계획이 날아갈 수 있기 때문에 신중해야 함.
    - 인덱스 관리 팁
        - 자주 사용되는 컬럼에 생성: WHERE 조건, JOIN, ORDER BY, GROUP BY에 자주 등장하는 컬럼
        - 적절한 인덱스 개수 유지: 과도한 인덱스는 성능 저하를 초래
        - 쓰기와 읽기 비율 고려: 읽기 작업이 많은 테이블에 적합
        - 데이터 중복도 파악: 중복도가 낮은 컬럼은 B-Tree, 높은 컬럼은 Bitmap 인덱스 추천
    
    - INDEX RANGE SCAN
        - 튜닝 전략
            - 인덱스 스캔 범위 줄이기
            - 테이블 액세스 빈도 줄이기
            - 인덱스를 구성하는 선행 컬럼을 조건절에 사용
            - Equal 조건으로 자주 쓰이는 컬럼을 선행 컬럼으로 설정
                - EX) WHERE NAME = ‘D’인 경우, id + name → name + id로 바꾼다
    
    - 인덱스 설계 시 유의 사항
        - 인덱스 컬럼의 순서
            - 결합 인덱스에서 SELECT 조건에 자주 사용되는 컬럼을 우선적으로 배치
            - EX) WHERE NAME = ‘John’ AND AGE > 30 → (NAME, AGE) 순서로 인덱스 생성
        - 쓰기 작업 최적화
            - 데이터 변경 작업이 빈번한 테이블은 최소 개의 인덱스 유지
        - 카디널리티
            - 값의 고유도가 높은 컬럼에 인덱스를 생성
            - EX) 주민등록번호 > 성별
        - 데이터 크기와 접근 빈도
            - 대용량 데이터 테이블에서 인덱스는 필수
        - 인덱스를 타지 못한다는 것?
            - 데이터베이스에서 인덱스를 타지 못한다는 것은 활용할 수 있는 인덱스가 있음에도 불구하고 SQL 쿼리가 인덱스를 활용하지 않고 테이블 전체를 스캔하여 데이터를 조회하는 경우를 의미
            - 결과: 불필요한 데이터 검색으로 성능 저하 발생
            - 주요 원인
                - 인덱스 컬럼에 함수 또는 연산 사용
                    - Function Based Index 활용하면 해결 가능
                - 데이터 타입 불일치
                - 와일드카드 사용 위치
                - NULL 값 처리
                - OR 조건이 포함된 경우
    
    - SQL 수행 과정
        - 파싱 작업: 문법 오류
        - 시맨틱 체크: 권한
        - 쉐어드 풀 체크: DB 메모리 체크. 이전에 했던 기록이 있으면 바로 실행. 없으면 실행 계획 옵티마이저로 넘어감
        - 옵티마이저: 여러 방식으로 변환해서 코스트 계산하고 가장 가성비가 좋다고 생각하는 코드를 로우소스 제너레이션으로 전달
        - 소프트 파싱: 쉐어드풀에서 바로 실행으로 넘어가는 것
        - 하드 파싱: 모든 절차들을 다 겪는 것
