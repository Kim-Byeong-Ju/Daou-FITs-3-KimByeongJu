# TIL: C언어 연산자, 제어문

## 학습 날짜: 2025년 3월 6일

## 학습 중 어려움을 느낀 부분

**`~(not) 연산자`** 

**2의 보수 표현이란?**

컴퓨터는 **음수를 2의 보수(2’s complement) 형태로 저장**해.

2의 보수를 구하는 공식은 간단해:

1. **숫자의 모든 비트를 반전 (0 → 1, 1 → 0)**
2. **그 값에 1을 더하면 음수가 됨**

---

**5를 32비트 이진수로 표현**

우리가 다루는 `int`는 보통 32비트(4바이트) 크기.

즉, `5`를 32비트로 나타내면:

```

0000 0000 0000 0000 0000 0000 0000 0101  (5)
```

(맨 왼쪽 비트는 부호 비트인데, 0이면 양수, 1이면 음수.)

---

**`~5`를 계산 (비트 NOT 연산)**

`~` 연산자는 **비트 반전(0 ↔ 1)**이야.

```

0000 0000 0000 0000 0000 0000 0000 0101   (5)
------------------------------------------------
1111 1111 1111 1111 1111 1111 1111 1010   (~5)

```

이 결과를 **십진수로 변환하면 -6**

---

**`1111 1111 1111 1111 1111 1111 1111 1010`을 십진수로 변환**

이 숫자는 **2의 보수 형태의 음수**야.

2의 보수로 표현된 음수를 원래 십진수로 변환하는 방법:

**비트를 다시 반전 (1 → 0, 0 → 1)**

**1을 더하면 원래 숫자가 됨**

```
0000 0000 0000 0000 0000 0000 0000 0110  (6)
```

**부호 비트가 1이므로, 음수로 해석**

```
-6
```

## 오늘 배운 내용 정리

### **연산자**

**프로그램을 작성할 때 변수나 상수의 연산을 위해 사용되는 부호**

**`산술연산자`**

- +  -  *  / %(나머지 정수만 사용할 수 있다) -(부호변경)
int + int = int, int + float = float, int + char = int, char+ char = char,
char + float = float, float + float = float

**`대입연산자`**

- a=1, a+=2, a-=4, a*=7, a/=3, a%=5

**`논리연산자`**

- a&&b, a||b, !a

**`비교연산자`** : 결과로 참, 거짓으로 나타남

- ==(같다), <. >, <=, >=, != 같지 않다

**`증감연산자`**

- ++ : a++, ++a 변수 a에 1을 더하여 a에 기억시킨다. -- : a--, --a 변수 a에 1을 빼서 a에 기억시킨다.

**`비트 연산자`**

- 하드웨어의 제어에 사용됨
- 종류 : 비트논리연산자, 시프트연산자
    - 비트논리연산자
    정수형변수와 정수형 상수만 사용할 수 있음
    a &b : 논리곱 T &T = T , T &F = F, F &T = F, F &F = F (참고 : T는 참, F는
    거짓)
    a | b : 논리합 T | T = T , T | F = T, F | T = T, F | F = F (참고 : T는 참, F
    는 거짓)
    a ^ b : 배타논리합 T ^ T = F , T ^ F = T, F ^ T = T, F ^ F = F (참고 : T
    는 참, F는 거짓)
    ~a : 논리부정 T = F, F = T
    - 시프트연산자
    왼쪽 시프트연산자 : a의 내용으로 왼쪽으로 n비트 이동하여 연산함 a * 2의 n승
    오른쪽 시프트연산자 : a의 내용으로 오른쪽으로 n비트 이동하여 연산함 a / 2의 n승(소
    수점이하는 손실됨)

**`콤마연산자`**

- 항목을 왼쪽부터 오른쪽으로 연결해 주는 역할
int a,b;

**`삼항연산자`**

- 형식
조건 ? 항목1:항목2
조건 : 참과 거짓으로 구분할 수 있는 비교연산, 논리 연산 등
항목1 : 조건이 참인 경우 실행할 연산이나 함수
항목2: 조건이 거짓인 경우 실행할 연산이나 함수

**`형변환연산자`**

- 이미 정해진 자료형을 일시적으로 바꾸는 연산임. 연산에만 사용할 수 있는 연산
자로 변수 자체의 자료형을 바꾸는 것은 아님
(자료형) 변수명;

**`sizeof() 크기 연산자`**

- 자료형이나 데이터의 크기를 가르쳐 주는 연산자
sizeof(int); sizeof(float)

### 제어문

**제어문**은 프로그램의 흐름을 제어하는 문장으로, 실행 순서를 변경하거나 특정 조건에서만 실행되도록 한다.

**`조건문 (if문, switch문)`**

**if문**

주어진 조건이 참이면 특정 코드 블록을 실행하는 구조

**형식:**

```c
c
복사편집
if (조건) {
    // 조건이 참일 때 실행되는 코드
}

```

✅ **if-else 문**

```c
c
복사편집
if (조건) {
    // 조건이 참일 때 실행
} else {
    // 조건이 거짓일 때 실행
}

```

✅ **다중 if-else 문**

```c
c
복사편집
if (조건1) {
    // 조건1이 참일 때 실행
} else if (조건2) {
    // 조건2가 참일 때 실행
} else {
    // 모든 조건이 거짓일 때 실행
}

```

---

**switch문**

하나의 변수 값을 여러 경우와 비교하여 해당하는 코드 블록을 실행

**형식:**

```c
c
복사편집
switch (변수) {
    case 값1:
        // 값1일 때 실행
        break;
    case 값2:
        // 값2일 때 실행
        break;
    default:
        // 모든 경우에 해당하지 않을 때 실행
}

```

✅ `break` 문을 사용하여 실행 종료 (없으면 다음 case로 넘어감)

---

**`반복문 (for문, while문, do-while문)`**

**for문**

반복 횟수가 정해져 있을 때 사용

**형식:**

```c
c
복사편집
for (초기화; 조건; 증감) {
    // 반복 실행할 코드
}

```

✅ 예제:

```c
c
복사편집
for (int i = 0; i < 5; i++) {
    printf("반복 %d\n", i);
}

```

---

**while문**

조건이 참이면 계속 반복

**형식:**

```c
c
복사편집
while (조건) {
    // 조건이 참이면 반복 실행
}

```

✅ 예제:

```c
c
복사편집
int i = 0;
while (i < 5) {
    printf("반복 %d\n", i);
    i++;
}

```

---

**do-while문**

일단 한 번 실행한 후 조건을 검사하여 반복 여부 결정

**형식:**

```c
c
복사편집
do {
    // 무조건 한 번 실행 후 조건 확인
} while (조건);

```

✅ 예제:

```c
c
복사편집
int i = 0;
do {
    printf("반복 %d\n", i);
    i++;
} while (i < 5);

```

✅ **while문과 차이점:**

- `while` 문은 조건이 처음부터 거짓이면 **한 번도 실행되지 않음**
- `do-while` 문은 조건과 상관없이 **최소 한 번은 실행됨**

**`반복문 제어 (break, continue)`**

**break문**

- 반복문을 강제로 종료
- `switch` 문에서도 사용

```c
c
복사편집
for (int i = 0; i < 10; i++) {
    if (i == 5) {
        break;  // i가 5일 때 반복문 종료
    }
    printf("%d ", i);
}

```

**continue문**

- 현재 반복을 건너뛰고 다음 반복 실행

```c
c
복사편집
for (int i = 0; i < 10; i++) {
    if (i == 5) {
        continue;  // i가 5일 때 출력하지 않고 다음 반복 실행
    }
    printf("%d ", i);
}

```
